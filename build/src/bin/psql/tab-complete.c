/*-------------------------------------------------------------------------
 *
 * tab-complete.c
 *    Preprocessed tab-completion code.
 *
 * Portions Copyright (c) 1996-2026, PostgreSQL Global Development Group
 * Portions Copyright (c) 1994, Regents of the University of California
 *
 * NOTES
 *  ******************************
 *  *** DO NOT EDIT THIS FILE! ***
 *  ******************************
 *
 *  It has been GENERATED by src/bin/psql/gen_tabcomplete.pl
 *
 *-------------------------------------------------------------------------
 */

#define SWITCH_CONVERSION_APPLIED

#line 1 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
/*
 * psql - the PostgreSQL interactive terminal
 *
 * Copyright (c) 2000-2026, PostgreSQL Global Development Group
 *
 * src/bin/psql/tab-complete.in.c
 *
 * Note: this will compile and work as-is if SWITCH_CONVERSION_APPLIED
 * is not defined.  However, the expected usage is that it's first run
 * through gen_tabcomplete.pl, which will #define that symbol, fill in the
 * tcpatterns[] array, and convert the else-if chain in match_previous_words()
 * into a switch.  See comments for match_previous_words() and the header
 * comment in gen_tabcomplete.pl for more detail.
 */

/*----------------------------------------------------------------------
 * This file implements a somewhat more sophisticated readline "TAB
 * completion" in psql. It is not intended to be AI, to replace
 * learning SQL, or to relieve you from thinking about what you're
 * doing. Also it does not always give you all the syntactically legal
 * completions, only those that are the most common or the ones that
 * the programmer felt most like implementing.
 *
 * CAVEAT: Tab completion causes queries to be sent to the backend.
 * The number of tuples returned gets limited, in most default
 * installations to 1000, but if you still don't like this prospect,
 * you can turn off tab completion in your ~/.inputrc (or else
 * ${INPUTRC}) file so:
 *
 *	 $if psql
 *	 set disable-completion on
 *	 $endif
 *
 * See `man 3 readline' or `info readline' for the full details.
 *
 * BUGS:
 * - Quotes, parentheses, and other funny characters are not handled
 *	 all that gracefully.
 *----------------------------------------------------------------------
 */

#include "postgres_fe.h"

#include "input.h"
#include "tab-complete.h"

/* If we don't have this, we might as well forget about the whole thing: */
#ifdef USE_READLINE

#include <ctype.h>
#include <sys/stat.h>

#include "catalog/pg_am_d.h"
#include "catalog/pg_class_d.h"
#include "common.h"
#include "common/keywords.h"
#include "libpq-fe.h"
#include "mb/pg_wchar.h"
#include "pqexpbuffer.h"
#include "settings.h"
#include "stringutils.h"

/*
 * Ancient versions of libedit provide filename_completion_function()
 * instead of rl_filename_completion_function().  Likewise for
 * [rl_]completion_matches().
 */
#ifndef HAVE_RL_FILENAME_COMPLETION_FUNCTION
#define rl_filename_completion_function filename_completion_function
#endif

#ifndef HAVE_RL_COMPLETION_MATCHES
#define rl_completion_matches completion_matches
#endif

/*
 * Currently we assume that rl_filename_dequoting_function exists if
 * rl_filename_quoting_function does.  If that proves not to be the case,
 * we'd need to test for the former, or possibly both, in configure.
 */
#ifdef HAVE_RL_FILENAME_QUOTING_FUNCTION
#define USE_FILENAME_QUOTING_FUNCTIONS 1
#endif

/* word break characters */
#define WORD_BREAKS		"\t\n@><=;|&() "

/*
 * Since readline doesn't let us pass any state through to the tab completion
 * callback, we have to use this global variable to let get_previous_words()
 * get at the previous lines of the current command.  Ick.
 */
PQExpBuffer tab_completion_query_buf = NULL;

/*
 * In some situations, the query to find out what names are available to
 * complete with must vary depending on server version.  We handle this by
 * storing a list of queries, each tagged with the minimum server version
 * it will work for.  Each list must be stored in descending server version
 * order, so that the first satisfactory query is the one to use.
 *
 * When the query string is otherwise constant, an array of VersionedQuery
 * suffices.  Terminate the array with an entry having min_server_version = 0.
 * That entry's query string can be a query that works in all supported older
 * server versions, or NULL to give up and do no completion.
 */
typedef struct VersionedQuery
{
	int			min_server_version;
	const char *query;
} VersionedQuery;

/*
 * This struct is used to define "schema queries", which are custom-built
 * to obtain possibly-schema-qualified names of database objects.  There is
 * enough similarity in the structure that we don't want to repeat it each
 * time.  So we put the components of each query into this struct and
 * assemble them with the common boilerplate in _complete_from_query().
 *
 * We also use this struct to define queries that use completion_ref_object,
 * which is some object related to the one(s) we want to get the names of
 * (for example, the table we want the indexes of).  In that usage the
 * objects we're completing might not have a schema of their own, but the
 * reference object almost always does (passed in completion_ref_schema).
 *
 * As with VersionedQuery, we can use an array of these if the query details
 * must vary across versions.
 */
typedef struct SchemaQuery
{
	/*
	 * If not zero, minimum server version this struct applies to.  If not
	 * zero, there should be a following struct with a smaller minimum server
	 * version; use catname == NULL in the last entry if we should do nothing.
	 */
	int			min_server_version;

	/*
	 * Name of catalog or catalogs to be queried, with alias(es), eg.
	 * "pg_catalog.pg_class c".  Note that "pg_namespace n" and/or
	 * "pg_namespace nr" will be added automatically when needed.
	 */
	const char *catname;

	/*
	 * Selection condition --- only rows meeting this condition are candidates
	 * to display.  If catname mentions multiple tables, include the necessary
	 * join condition here.  For example, this might look like "c.relkind = "
	 * CppAsString2(RELKIND_RELATION).  Write NULL (not an empty string) if
	 * not needed.
	 */
	const char *selcondition;

	/*
	 * Visibility condition --- which rows are visible without schema
	 * qualification?  For example, "pg_catalog.pg_table_is_visible(c.oid)".
	 * NULL if not needed.
	 */
	const char *viscondition;

	/*
	 * Namespace --- name of field to join to pg_namespace.oid when there is
	 * schema qualification.  For example, "c.relnamespace".  NULL if we don't
	 * want to join to pg_namespace (then any schema part in the input word
	 * will be ignored).
	 */
	const char *namespace;

	/*
	 * Result --- the base object name to return.  For example, "c.relname".
	 */
	const char *result;

	/*
	 * In some cases, it's difficult to keep the query from returning the same
	 * object multiple times.  Specify use_distinct to filter out duplicates.
	 */
	bool		use_distinct;

	/*
	 * Additional literal strings (usually keywords) to be offered along with
	 * the query results.  Provide a NULL-terminated array of constant
	 * strings, or NULL if none.
	 */
	const char *const *keywords;

	/*
	 * If this query uses completion_ref_object/completion_ref_schema,
	 * populate the remaining fields, else leave them NULL.  When using this
	 * capability, catname must include the catalog that defines the
	 * completion_ref_object, and selcondition must include the join condition
	 * that connects it to the result's catalog.
	 *
	 * refname is the field that should be equated to completion_ref_object,
	 * for example "cr.relname".
	 */
	const char *refname;

	/*
	 * Visibility condition to use when completion_ref_schema is not set.  For
	 * example, "pg_catalog.pg_table_is_visible(cr.oid)".  NULL if not needed.
	 */
	const char *refviscondition;

	/*
	 * Name of field to join to pg_namespace.oid when completion_ref_schema is
	 * set.  For example, "cr.relnamespace".  NULL if we don't want to
	 * consider completion_ref_schema.
	 */
	const char *refnamespace;
} SchemaQuery;


/*
 * Store maximum number of records we want from database queries
 * (implemented via SELECT ... LIMIT xx).
 */
static int	completion_max_records;

/*
 * Communication variables set by psql_completion (mostly in COMPLETE_WITH_FOO
 * macros) and then used by the completion callback functions.  Ugly but there
 * is no better way.
 */
static char completion_last_char;	/* last char of input word */
static const char *completion_charp;	/* to pass a string */
static const char *const *completion_charpp;	/* to pass a list of strings */
static const VersionedQuery *completion_vquery; /* to pass a VersionedQuery */
static const SchemaQuery *completion_squery;	/* to pass a SchemaQuery */
static char *completion_ref_object; /* name of reference object */
static char *completion_ref_schema; /* schema name of reference object */
static bool completion_case_sensitive;	/* completion is case sensitive */
static bool completion_verbatim;	/* completion is verbatim */
static bool completion_force_quote; /* true to force-quote filenames */

/*
 * A few macros to ease typing. You can use these to complete the given
 * string with
 * 1) The result from a query you pass it. (Perhaps one of those below?)
 *	  We support both simple and versioned queries.
 * 2) The result from a schema query you pass it.
 *	  We support both simple and versioned schema queries.
 * 3) The items from a null-pointer-terminated list (with or without
 *	  case-sensitive comparison); if the list is constant you can build it
 *	  with COMPLETE_WITH() or COMPLETE_WITH_CS().  The QUERY_LIST and
 *	  QUERY_PLUS forms combine such literal lists with a query result.
 * 4) The list of attributes of the given table (possibly schema-qualified).
 * 5) The list of arguments to the given function (possibly schema-qualified).
 *
 * The query is generally expected to return raw SQL identifiers; matching
 * to what the user typed is done in a quoting-aware fashion.  If what is
 * returned is not SQL identifiers, use one of the VERBATIM forms, in which
 * case the query results are matched to the user's text without double-quote
 * processing (so if quoting is needed, you must provide it in the query
 * results).
 */
#define COMPLETE_WITH_QUERY(query) \
	COMPLETE_WITH_QUERY_LIST(query, NULL)

#define COMPLETE_WITH_QUERY_LIST(query, list) \
do { \
	completion_charp = query; \
	completion_charpp = list; \
	completion_verbatim = false; \
	matches = rl_completion_matches(text, complete_from_query); \
} while (0)

#define COMPLETE_WITH_QUERY_PLUS(query, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_QUERY_LIST(query, list); \
} while (0)

#define COMPLETE_WITH_QUERY_VERBATIM(query) \
	COMPLETE_WITH_QUERY_VERBATIM_LIST(query, NULL)

#define COMPLETE_WITH_QUERY_VERBATIM_LIST(query, list) \
do { \
	completion_charp = query; \
	completion_charpp = list; \
	completion_verbatim = true; \
	matches = rl_completion_matches(text, complete_from_query); \
} while (0)

#define COMPLETE_WITH_QUERY_VERBATIM_PLUS(query, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_QUERY_VERBATIM_LIST(query, list); \
} while (0)

#define COMPLETE_WITH_VERSIONED_QUERY(query) \
	COMPLETE_WITH_VERSIONED_QUERY_LIST(query, NULL)

#define COMPLETE_WITH_VERSIONED_QUERY_LIST(query, list) \
do { \
	completion_vquery = query; \
	completion_charpp = list; \
	completion_verbatim = false; \
	matches = rl_completion_matches(text, complete_from_versioned_query); \
} while (0)

#define COMPLETE_WITH_VERSIONED_QUERY_PLUS(query, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_VERSIONED_QUERY_LIST(query, list); \
} while (0)

#define COMPLETE_WITH_SCHEMA_QUERY(query) \
	COMPLETE_WITH_SCHEMA_QUERY_LIST(query, NULL)

#define COMPLETE_WITH_SCHEMA_QUERY_LIST(query, list) \
do { \
	completion_squery = &(query); \
	completion_charpp = list; \
	completion_verbatim = false; \
	matches = rl_completion_matches(text, complete_from_schema_query); \
} while (0)

#define COMPLETE_WITH_SCHEMA_QUERY_PLUS(query, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_SCHEMA_QUERY_LIST(query, list); \
} while (0)

#define COMPLETE_WITH_SCHEMA_QUERY_VERBATIM(query) \
do { \
	completion_squery = &(query); \
	completion_charpp = NULL; \
	completion_verbatim = true; \
	matches = rl_completion_matches(text, complete_from_schema_query); \
} while (0)

#define COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(query) \
	COMPLETE_WITH_VERSIONED_SCHEMA_QUERY_LIST(query, NULL)

#define COMPLETE_WITH_VERSIONED_SCHEMA_QUERY_LIST(query, list) \
do { \
	completion_squery = query; \
	completion_charpp = list; \
	completion_verbatim = false; \
	matches = rl_completion_matches(text, complete_from_versioned_schema_query); \
} while (0)

#define COMPLETE_WITH_VERSIONED_SCHEMA_QUERY_PLUS(query, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_VERSIONED_SCHEMA_QUERY_LIST(query, list); \
} while (0)

/*
 * Caution: COMPLETE_WITH_CONST is not for general-purpose use; you probably
 * want COMPLETE_WITH() with one element, instead.
 */
#define COMPLETE_WITH_CONST(cs, con) \
do { \
	completion_case_sensitive = (cs); \
	completion_charp = (con); \
	matches = rl_completion_matches(text, complete_from_const); \
} while (0)

#define COMPLETE_WITH_LIST_INT(cs, list) \
do { \
	completion_case_sensitive = (cs); \
	completion_charpp = (list); \
	matches = rl_completion_matches(text, complete_from_list); \
} while (0)

#define COMPLETE_WITH_LIST(list) COMPLETE_WITH_LIST_INT(false, list)
#define COMPLETE_WITH_LIST_CS(list) COMPLETE_WITH_LIST_INT(true, list)

#define COMPLETE_WITH(...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_LIST(list); \
} while (0)

#define COMPLETE_WITH_CS(...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_LIST_CS(list); \
} while (0)

#define COMPLETE_WITH_ATTR(relation) \
	COMPLETE_WITH_ATTR_LIST(relation, NULL)

#define COMPLETE_WITH_ATTR_LIST(relation, list) \
do { \
	set_completion_reference(relation); \
	completion_squery = &(Query_for_list_of_attributes); \
	completion_charpp = list; \
	completion_verbatim = false; \
	matches = rl_completion_matches(text, complete_from_schema_query); \
} while (0)

#define COMPLETE_WITH_ATTR_PLUS(relation, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_ATTR_LIST(relation, list); \
} while (0)

/*
 * libedit will typically include the literal's leading single quote in
 * "text", while readline will not.  Adapt our offered strings to fit.
 * But include a quote if there's not one just before "text", to get the
 * user off to the right start.
 */
#define COMPLETE_WITH_ENUM_VALUE(type) \
do { \
	set_completion_reference(type); \
	if (text[0] == '\'' || \
		start == 0 || rl_line_buffer[start - 1] != '\'') \
		completion_squery = &(Query_for_list_of_enum_values_quoted); \
	else \
		completion_squery = &(Query_for_list_of_enum_values_unquoted); \
	completion_charpp = NULL; \
	completion_verbatim = true; \
	matches = rl_completion_matches(text, complete_from_schema_query); \
} while (0)

/*
 * Timezone completion is mostly like enum label completion, but we work
 * a little harder since this is a more common use-case.
 */
#define COMPLETE_WITH_TIMEZONE_NAME() \
do { \
	static const char *const list[] = { "DEFAULT", NULL }; \
	if (text[0] == '\'') \
		completion_charp = Query_for_list_of_timezone_names_quoted_in; \
	else if (start == 0 || rl_line_buffer[start - 1] != '\'') \
		completion_charp = Query_for_list_of_timezone_names_quoted_out; \
	else \
		completion_charp = Query_for_list_of_timezone_names_unquoted; \
	completion_charpp = list;							  \
	completion_verbatim = true; \
	matches = rl_completion_matches(text, complete_from_query); \
} while (0)

#define COMPLETE_WITH_FUNCTION_ARG(function) \
do { \
	set_completion_reference(function); \
	completion_squery = &(Query_for_list_of_arguments); \
	completion_charpp = NULL; \
	completion_verbatim = true; \
	matches = rl_completion_matches(text, complete_from_schema_query); \
} while (0)

#define COMPLETE_WITH_FILES_LIST(escape, force_quote, list) \
do { \
	completion_charp = escape; \
	completion_charpp = list; \
	completion_force_quote = force_quote; \
	matches = rl_completion_matches(text, complete_from_files); \
} while (0)

#define COMPLETE_WITH_FILES(escape, force_quote) \
	COMPLETE_WITH_FILES_LIST(escape, force_quote, NULL)

#define COMPLETE_WITH_FILES_PLUS(escape, force_quote, ...) \
do { \
	static const char *const list[] = { __VA_ARGS__, NULL }; \
	COMPLETE_WITH_FILES_LIST(escape, force_quote, list); \
} while (0)

#define COMPLETE_WITH_GENERATOR(generator) \
	matches = rl_completion_matches(text, generator)

/*
 * Assembly instructions for schema queries
 *
 * Note that toast tables are not included in those queries to avoid
 * unnecessary bloat in the completions generated.
 */

static const SchemaQuery Query_for_constraint_of_table = {
	.catname = "pg_catalog.pg_constraint con, pg_catalog.pg_class c1",
	.selcondition = "con.conrelid=c1.oid",
	.result = "con.conname",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};

static const SchemaQuery Query_for_constraint_of_table_not_validated = {
	.catname = "pg_catalog.pg_constraint con, pg_catalog.pg_class c1",
	.selcondition = "con.conrelid=c1.oid and not con.convalidated",
	.result = "con.conname",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};

static const SchemaQuery Query_for_constraint_of_type = {
	.catname = "pg_catalog.pg_constraint con, pg_catalog.pg_type t",
	.selcondition = "con.contypid=t.oid",
	.result = "con.conname",
	.refname = "t.typname",
	.refviscondition = "pg_catalog.pg_type_is_visible(t.oid)",
	.refnamespace = "t.typnamespace",
};

static const SchemaQuery Query_for_index_of_table = {
	.catname = "pg_catalog.pg_class c1, pg_catalog.pg_class c2, pg_catalog.pg_index i",
	.selcondition = "c1.oid=i.indrelid and i.indexrelid=c2.oid",
	.result = "c2.relname",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};

static const SchemaQuery Query_for_unique_index_of_table = {
	.catname = "pg_catalog.pg_class c1, pg_catalog.pg_class c2, pg_catalog.pg_index i",
	.selcondition = "c1.oid=i.indrelid and i.indexrelid=c2.oid and i.indisunique",
	.result = "c2.relname",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};

static const SchemaQuery Query_for_list_of_aggregates[] = {
	{
		.min_server_version = 110000,
		.catname = "pg_catalog.pg_proc p",
		.selcondition = "p.prokind = 'a'",
		.viscondition = "pg_catalog.pg_function_is_visible(p.oid)",
		.namespace = "p.pronamespace",
		.result = "p.proname",
	},
	{
		.catname = "pg_catalog.pg_proc p",
		.selcondition = "p.proisagg",
		.viscondition = "pg_catalog.pg_function_is_visible(p.oid)",
		.namespace = "p.pronamespace",
		.result = "p.proname",
	}
};

static const SchemaQuery Query_for_list_of_arguments = {
	.catname = "pg_catalog.pg_proc p",
	.result = "pg_catalog.oidvectortypes(p.proargtypes)||')'",
	.refname = "p.proname",
	.refviscondition = "pg_catalog.pg_function_is_visible(p.oid)",
	.refnamespace = "p.pronamespace",
};

static const SchemaQuery Query_for_list_of_attributes = {
	.catname = "pg_catalog.pg_attribute a, pg_catalog.pg_class c",
	.selcondition = "c.oid = a.attrelid and a.attnum > 0 and not a.attisdropped",
	.result = "a.attname",
	.refname = "c.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.refnamespace = "c.relnamespace",
};

static const SchemaQuery Query_for_list_of_attribute_numbers = {
	.catname = "pg_catalog.pg_attribute a, pg_catalog.pg_class c",
	.selcondition = "c.oid = a.attrelid and a.attnum > 0 and not a.attisdropped",
	.result = "a.attnum::pg_catalog.text",
	.refname = "c.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.refnamespace = "c.relnamespace",
};

static const char *const Keywords_for_list_of_datatypes[] = {
	"bigint",
	"boolean",
	"character",
	"double precision",
	"integer",
	"real",
	"smallint",

	/*
	 * Note: currently there's no value in offering the following multiword
	 * type names, because tab completion cannot succeed for them: we can't
	 * disambiguate until somewhere in the second word, at which point we
	 * won't have the first word as context.  ("double precision" does work,
	 * as long as no other type name begins with "double".)  Leave them out to
	 * encourage users to use the PG-specific aliases, which we can complete.
	 */
#ifdef NOT_USED
	"bit varying",
	"character varying",
	"time with time zone",
	"time without time zone",
	"timestamp with time zone",
	"timestamp without time zone",
#endif
	NULL
};

static const SchemaQuery Query_for_list_of_datatypes = {
	.catname = "pg_catalog.pg_type t",
	/* selcondition --- ignore table rowtypes and array types */
	.selcondition = "(t.typrelid = 0 "
	" OR (SELECT c.relkind = " CppAsString2(RELKIND_COMPOSITE_TYPE)
	"     FROM pg_catalog.pg_class c WHERE c.oid = t.typrelid)) "
	"AND t.typname !~ '^_'",
	.viscondition = "pg_catalog.pg_type_is_visible(t.oid)",
	.namespace = "t.typnamespace",
	.result = "t.typname",
	.keywords = Keywords_for_list_of_datatypes,
};

static const SchemaQuery Query_for_list_of_composite_datatypes = {
	.catname = "pg_catalog.pg_type t",
	/* selcondition --- only get composite types */
	.selcondition = "(SELECT c.relkind = " CppAsString2(RELKIND_COMPOSITE_TYPE)
	" FROM pg_catalog.pg_class c WHERE c.oid = t.typrelid) "
	"AND t.typname !~ '^_'",
	.viscondition = "pg_catalog.pg_type_is_visible(t.oid)",
	.namespace = "t.typnamespace",
	.result = "t.typname",
};

static const SchemaQuery Query_for_list_of_domains = {
	.catname = "pg_catalog.pg_type t",
	.selcondition = "t.typtype = 'd'",
	.viscondition = "pg_catalog.pg_type_is_visible(t.oid)",
	.namespace = "t.typnamespace",
	.result = "t.typname",
};

static const SchemaQuery Query_for_list_of_enum_values_quoted = {
	.catname = "pg_catalog.pg_enum e, pg_catalog.pg_type t",
	.selcondition = "t.oid = e.enumtypid",
	.result = "pg_catalog.quote_literal(enumlabel)",
	.refname = "t.typname",
	.refviscondition = "pg_catalog.pg_type_is_visible(t.oid)",
	.refnamespace = "t.typnamespace",
};

static const SchemaQuery Query_for_list_of_enum_values_unquoted = {
	.catname = "pg_catalog.pg_enum e, pg_catalog.pg_type t",
	.selcondition = "t.oid = e.enumtypid",
	.result = "e.enumlabel",
	.refname = "t.typname",
	.refviscondition = "pg_catalog.pg_type_is_visible(t.oid)",
	.refnamespace = "t.typnamespace",
};

/* Note: this intentionally accepts aggregates as well as plain functions */
static const SchemaQuery Query_for_list_of_functions[] = {
	{
		.min_server_version = 110000,
		.catname = "pg_catalog.pg_proc p",
		.selcondition = "p.prokind != 'p'",
		.viscondition = "pg_catalog.pg_function_is_visible(p.oid)",
		.namespace = "p.pronamespace",
		.result = "p.proname",
	},
	{
		.catname = "pg_catalog.pg_proc p",
		.viscondition = "pg_catalog.pg_function_is_visible(p.oid)",
		.namespace = "p.pronamespace",
		.result = "p.proname",
	}
};

static const SchemaQuery Query_for_list_of_procedures[] = {
	{
		.min_server_version = 110000,
		.catname = "pg_catalog.pg_proc p",
		.selcondition = "p.prokind = 'p'",
		.viscondition = "pg_catalog.pg_function_is_visible(p.oid)",
		.namespace = "p.pronamespace",
		.result = "p.proname",
	},
	{
		/* not supported in older versions */
		.catname = NULL,
	}
};

static const SchemaQuery Query_for_list_of_routines = {
	.catname = "pg_catalog.pg_proc p",
	.viscondition = "pg_catalog.pg_function_is_visible(p.oid)",
	.namespace = "p.pronamespace",
	.result = "p.proname",
};

static const SchemaQuery Query_for_list_of_sequences = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_SEQUENCE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_foreign_tables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_FOREIGN_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_tables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_partitioned_tables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_PARTITIONED_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_tables_for_constraint = {
	.catname = "pg_catalog.pg_class c, pg_catalog.pg_constraint con",
	.selcondition = "c.oid=con.conrelid and c.relkind IN ("
	CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
	.use_distinct = true,
	.refname = "con.conname",
};

static const SchemaQuery Query_for_list_of_tables_for_policy = {
	.catname = "pg_catalog.pg_class c, pg_catalog.pg_policy p",
	.selcondition = "c.oid=p.polrelid",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
	.use_distinct = true,
	.refname = "p.polname",
};

static const SchemaQuery Query_for_list_of_tables_for_rule = {
	.catname = "pg_catalog.pg_class c, pg_catalog.pg_rewrite r",
	.selcondition = "c.oid=r.ev_class",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
	.use_distinct = true,
	.refname = "r.rulename",
};

static const SchemaQuery Query_for_list_of_tables_for_trigger = {
	.catname = "pg_catalog.pg_class c, pg_catalog.pg_trigger t",
	.selcondition = "c.oid=t.tgrelid",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
	.use_distinct = true,
	.refname = "t.tgname",
};

static const SchemaQuery Query_for_list_of_ts_configurations = {
	.catname = "pg_catalog.pg_ts_config c",
	.viscondition = "pg_catalog.pg_ts_config_is_visible(c.oid)",
	.namespace = "c.cfgnamespace",
	.result = "c.cfgname",
};

static const SchemaQuery Query_for_list_of_ts_dictionaries = {
	.catname = "pg_catalog.pg_ts_dict d",
	.viscondition = "pg_catalog.pg_ts_dict_is_visible(d.oid)",
	.namespace = "d.dictnamespace",
	.result = "d.dictname",
};

static const SchemaQuery Query_for_list_of_ts_parsers = {
	.catname = "pg_catalog.pg_ts_parser p",
	.viscondition = "pg_catalog.pg_ts_parser_is_visible(p.oid)",
	.namespace = "p.prsnamespace",
	.result = "p.prsname",
};

static const SchemaQuery Query_for_list_of_ts_templates = {
	.catname = "pg_catalog.pg_ts_template t",
	.viscondition = "pg_catalog.pg_ts_template_is_visible(t.oid)",
	.namespace = "t.tmplnamespace",
	.result = "t.tmplname",
};

static const SchemaQuery Query_for_list_of_views = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_VIEW) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_matviews = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_MATVIEW) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_indexes = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_INDEX) ", "
	CppAsString2(RELKIND_PARTITIONED_INDEX) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_partitioned_indexes = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind = " CppAsString2(RELKIND_PARTITIONED_INDEX),
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_propgraphs = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_PROPGRAPH) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "pg_catalog.quote_ident(c.relname)",
};


/* All relations */
static const SchemaQuery Query_for_list_of_relations = {
	.catname = "pg_catalog.pg_class c",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/* partitioned relations */
static const SchemaQuery Query_for_list_of_partitioned_relations = {
	.catname = "pg_catalog.pg_class c",
	.selcondition = "c.relkind IN (" CppAsString2(RELKIND_PARTITIONED_TABLE)
	", " CppAsString2(RELKIND_PARTITIONED_INDEX) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_operator_families = {
	.catname = "pg_catalog.pg_opfamily c",
	.viscondition = "pg_catalog.pg_opfamily_is_visible(c.oid)",
	.namespace = "c.opfnamespace",
	.result = "c.opfname",
};

/* Relations supporting INSERT, UPDATE or DELETE */
static const SchemaQuery Query_for_list_of_updatables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_FOREIGN_TABLE) ", "
	CppAsString2(RELKIND_VIEW) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/* Relations supporting MERGE */
static const SchemaQuery Query_for_list_of_mergetargets = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_VIEW) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ") ",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/* Relations supporting SELECT */
static const SchemaQuery Query_for_list_of_selectables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_SEQUENCE) ", "
	CppAsString2(RELKIND_VIEW) ", "
	CppAsString2(RELKIND_MATVIEW) ", "
	CppAsString2(RELKIND_FOREIGN_TABLE) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/* Relations supporting TRUNCATE */
static const SchemaQuery Query_for_list_of_truncatables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_FOREIGN_TABLE) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/* Relations supporting GRANT are currently same as those supporting SELECT */
#define Query_for_list_of_grantables Query_for_list_of_selectables

/* Relations supporting ANALYZE */
static const SchemaQuery Query_for_list_of_analyzables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ", "
	CppAsString2(RELKIND_MATVIEW) ", "
	CppAsString2(RELKIND_FOREIGN_TABLE) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/*
 * Relations supporting COPY TO/FROM are currently almost the same as
 * those supporting ANALYZE. Although views with INSTEAD OF INSERT triggers
 * can be used with COPY FROM, they are rarely used for this purpose,
 * so plain views are intentionally excluded from this tab completion.
 */
#define Query_for_list_of_tables_for_copy Query_for_list_of_analyzables

/* Relations supporting index creation */
static const SchemaQuery Query_for_list_of_indexables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ", "
	CppAsString2(RELKIND_MATVIEW) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

/*
 * Relations supporting VACUUM are currently same as those supporting
 * indexing.
 */
#define Query_for_list_of_vacuumables Query_for_list_of_indexables

/* Relations supporting CLUSTER */
static const SchemaQuery Query_for_list_of_clusterables = {
	.catname = "pg_catalog.pg_class c",
	.selcondition =
	"c.relkind IN (" CppAsString2(RELKIND_RELATION) ", "
	CppAsString2(RELKIND_PARTITIONED_TABLE) ", "
	CppAsString2(RELKIND_MATVIEW) ")",
	.viscondition = "pg_catalog.pg_table_is_visible(c.oid)",
	.namespace = "c.relnamespace",
	.result = "c.relname",
};

static const SchemaQuery Query_for_list_of_constraints_with_schema = {
	.catname = "pg_catalog.pg_constraint c",
	.selcondition = "c.conrelid <> 0",
	.namespace = "c.connamespace",
	.result = "c.conname",
};

static const SchemaQuery Query_for_list_of_statistics = {
	.catname = "pg_catalog.pg_statistic_ext s",
	.viscondition = "pg_catalog.pg_statistics_obj_is_visible(s.oid)",
	.namespace = "s.stxnamespace",
	.result = "s.stxname",
};

static const SchemaQuery Query_for_list_of_collations = {
	.catname = "pg_catalog.pg_collation c",
	.selcondition = "c.collencoding IN (-1, pg_catalog.pg_char_to_encoding(pg_catalog.getdatabaseencoding()))",
	.viscondition = "pg_catalog.pg_collation_is_visible(c.oid)",
	.namespace = "c.collnamespace",
	.result = "c.collname",
};

static const SchemaQuery Query_for_partition_of_table = {
	.catname = "pg_catalog.pg_class c1, pg_catalog.pg_class c2, pg_catalog.pg_inherits i",
	.selcondition = "c1.oid=i.inhparent and i.inhrelid=c2.oid and c2.relispartition",
	.viscondition = "pg_catalog.pg_table_is_visible(c2.oid)",
	.namespace = "c2.relnamespace",
	.result = "c2.relname",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};

static const SchemaQuery Query_for_rule_of_table = {
	.catname = "pg_catalog.pg_rewrite r, pg_catalog.pg_class c1",
	.selcondition = "r.ev_class=c1.oid",
	.result = "r.rulename",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};

static const SchemaQuery Query_for_trigger_of_table = {
	.catname = "pg_catalog.pg_trigger t, pg_catalog.pg_class c1",
	.selcondition = "t.tgrelid=c1.oid and not t.tgisinternal",
	.result = "t.tgname",
	.refname = "c1.relname",
	.refviscondition = "pg_catalog.pg_table_is_visible(c1.oid)",
	.refnamespace = "c1.relnamespace",
};


/*
 * Queries to get lists of names of various kinds of things, possibly
 * restricted to names matching a partially entered name.  Don't use
 * this method where the user might wish to enter a schema-qualified
 * name; make a SchemaQuery instead.
 *
 * In these queries, there must be a restriction clause of the form
 *		output LIKE '%s'
 * where "output" is the same string that the query returns.  The %s
 * will be replaced by a LIKE pattern to match the already-typed text.
 *
 * There can be a second '%s', which will be replaced by a suitably-escaped
 * version of the string provided in completion_ref_object.  If there is a
 * third '%s', it will be replaced by a suitably-escaped version of the string
 * provided in completion_ref_schema.  NOTE: using completion_ref_object
 * that way is usually the wrong thing, and using completion_ref_schema
 * that way is always the wrong thing.  Make a SchemaQuery instead.
 */

#define Query_for_list_of_template_databases \
"SELECT d.datname "\
"  FROM pg_catalog.pg_database d "\
" WHERE d.datname LIKE '%s' "\
"   AND (d.datistemplate OR pg_catalog.pg_has_role(d.datdba, 'USAGE'))"

#define Query_for_list_of_databases \
"SELECT datname FROM pg_catalog.pg_database "\
" WHERE datname LIKE '%s'"

#define Query_for_list_of_database_vars \
"SELECT conf FROM ("\
"       SELECT setdatabase, pg_catalog.split_part(pg_catalog.unnest(setconfig),'=',1) conf"\
"         FROM pg_db_role_setting "\
"       ) s, pg_database d "\
" WHERE s.setdatabase = d.oid "\
"   AND conf LIKE '%s'"\
"   AND d.datname LIKE '%s'"

#define Query_for_list_of_tablespaces \
"SELECT spcname FROM pg_catalog.pg_tablespace "\
" WHERE spcname LIKE '%s'"

#define Query_for_list_of_encodings \
" SELECT DISTINCT pg_catalog.pg_encoding_to_char(conforencoding) "\
"   FROM pg_catalog.pg_conversion "\
"  WHERE pg_catalog.pg_encoding_to_char(conforencoding) LIKE pg_catalog.upper('%s')"

#define Query_for_list_of_languages \
"SELECT lanname "\
"  FROM pg_catalog.pg_language "\
" WHERE lanname != 'internal' "\
"   AND lanname LIKE '%s'"

#define Query_for_list_of_schemas \
"SELECT nspname FROM pg_catalog.pg_namespace "\
" WHERE nspname LIKE '%s'"

/* Use COMPLETE_WITH_QUERY_VERBATIM with these queries for GUC names: */
#define Query_for_list_of_alter_system_set_vars \
"SELECT pg_catalog.lower(name) FROM pg_catalog.pg_settings "\
" WHERE context != 'internal' "\
"   AND pg_catalog.lower(name) LIKE pg_catalog.lower('%s')"

#define Query_for_list_of_set_vars \
"SELECT pg_catalog.lower(name) FROM pg_catalog.pg_settings "\
" WHERE context IN ('user', 'superuser') "\
"   AND pg_catalog.lower(name) LIKE pg_catalog.lower('%s')"

#define Query_for_list_of_show_vars \
"SELECT pg_catalog.lower(name) FROM pg_catalog.pg_settings "\
" WHERE pg_catalog.lower(name) LIKE pg_catalog.lower('%s')"

#define Query_for_list_of_roles \
" SELECT rolname "\
"   FROM pg_catalog.pg_roles "\
"  WHERE rolname LIKE '%s'"

/* add these to Query_for_list_of_roles in OWNER contexts */
#define Keywords_for_list_of_owner_roles \
"CURRENT_ROLE", "CURRENT_USER", "SESSION_USER"

/* add these to Query_for_list_of_roles in GRANT contexts */
#define Keywords_for_list_of_grant_roles \
Keywords_for_list_of_owner_roles, "PUBLIC"

#define Query_for_all_table_constraints \
"SELECT conname "\
"  FROM pg_catalog.pg_constraint c "\
" WHERE c.conrelid <> 0 "\
"       and conname LIKE '%s'"

#define Query_for_list_of_fdws \
" SELECT fdwname "\
"   FROM pg_catalog.pg_foreign_data_wrapper "\
"  WHERE fdwname LIKE '%s'"

#define Query_for_list_of_servers \
" SELECT srvname "\
"   FROM pg_catalog.pg_foreign_server "\
"  WHERE srvname LIKE '%s'"

#define Query_for_list_of_user_mappings \
" SELECT usename "\
"   FROM pg_catalog.pg_user_mappings "\
"  WHERE usename LIKE '%s'"

#define Query_for_list_of_user_vars \
"SELECT conf FROM ("\
"       SELECT rolname, pg_catalog.split_part(pg_catalog.unnest(rolconfig),'=',1) conf"\
"         FROM pg_catalog.pg_roles"\
"       ) s"\
"  WHERE s.conf like '%s' "\
"    AND s.rolname LIKE '%s'"

#define Query_for_list_of_access_methods \
" SELECT amname "\
"   FROM pg_catalog.pg_am "\
"  WHERE amname LIKE '%s'"

#define Query_for_list_of_index_access_methods \
" SELECT amname "\
"   FROM pg_catalog.pg_am "\
"  WHERE amname LIKE '%s' AND "\
"   amtype=" CppAsString2(AMTYPE_INDEX)

#define Query_for_list_of_table_access_methods \
" SELECT amname "\
"   FROM pg_catalog.pg_am "\
"  WHERE amname LIKE '%s' AND "\
"   amtype=" CppAsString2(AMTYPE_TABLE)

#define Query_for_list_of_extensions \
" SELECT extname "\
"   FROM pg_catalog.pg_extension "\
"  WHERE extname LIKE '%s'"

#define Query_for_list_of_available_extensions \
" SELECT name "\
"   FROM pg_catalog.pg_available_extensions "\
"  WHERE name LIKE '%s' AND installed_version IS NULL"

#define Query_for_list_of_available_extension_versions \
" SELECT version "\
"   FROM pg_catalog.pg_available_extension_versions "\
"  WHERE version LIKE '%s' AND name='%s'"

#define Query_for_list_of_prepared_statements \
" SELECT name "\
"   FROM pg_catalog.pg_prepared_statements "\
"  WHERE name LIKE '%s'"

#define Query_for_list_of_event_triggers \
" SELECT evtname "\
"   FROM pg_catalog.pg_event_trigger "\
"  WHERE evtname LIKE '%s'"

#define Query_for_list_of_tablesample_methods \
" SELECT proname "\
"   FROM pg_catalog.pg_proc "\
"  WHERE prorettype = 'pg_catalog.tsm_handler'::pg_catalog.regtype AND "\
"        proargtypes[0] = 'pg_catalog.internal'::pg_catalog.regtype AND "\
"        proname LIKE '%s'"

#define Query_for_list_of_policies \
" SELECT polname "\
"   FROM pg_catalog.pg_policy "\
"  WHERE polname LIKE '%s'"

#define Query_for_values_of_enum_GUC \
" SELECT val FROM ( "\
"   SELECT name, pg_catalog.unnest(enumvals) AS val "\
"     FROM pg_catalog.pg_settings "\
"    ) ss "\
"  WHERE val LIKE '%s'"\
"        and pg_catalog.lower(name)=pg_catalog.lower('%s')"

#define Query_for_list_of_channels \
" SELECT channel "\
"   FROM pg_catalog.pg_listening_channels() AS channel "\
"  WHERE channel LIKE '%s'"

#define Query_for_list_of_cursors \
" SELECT name "\
"   FROM pg_catalog.pg_cursors "\
"  WHERE name LIKE '%s'"

#define Query_for_list_of_timezone_names_unquoted \
" SELECT name "\
"   FROM pg_catalog.pg_timezone_names() "\
"  WHERE pg_catalog.lower(name) LIKE pg_catalog.lower('%s')"

#define Query_for_list_of_timezone_names_quoted_out \
"SELECT pg_catalog.quote_literal(name) AS name "\
"  FROM pg_catalog.pg_timezone_names() "\
" WHERE pg_catalog.lower(name) LIKE pg_catalog.lower('%s')"

#define Query_for_list_of_timezone_names_quoted_in \
"SELECT pg_catalog.quote_literal(name) AS name "\
"  FROM pg_catalog.pg_timezone_names() "\
" WHERE pg_catalog.quote_literal(pg_catalog.lower(name)) LIKE pg_catalog.lower('%s')"

#define Query_for_list_of_publications \
"SELECT pubname "\
"  FROM pg_catalog.pg_publication "\
" WHERE pubname LIKE '%s'"

#define Query_for_list_of_subscriptions \
"SELECT s.subname "\
"  FROM pg_catalog.pg_subscription s, pg_catalog.pg_database d"\
" WHERE s.subname LIKE '%s' "\
"   AND d.datname = pg_catalog.current_database() "\
"   AND s.subdbid = d.oid"

/* Privilege options shared between GRANT and REVOKE */
#define Privilege_options_of_grant_and_revoke \
"SELECT", "INSERT", "UPDATE", "DELETE", "TRUNCATE", "REFERENCES", "TRIGGER", \
"CREATE", "CONNECT", "TEMPORARY", "EXECUTE", "USAGE", "SET", "ALTER SYSTEM", \
"MAINTAIN", "ALL"

/* ALTER PROCEDURE options */
#define Alter_procedure_options \
"DEPENDS ON EXTENSION", "EXTERNAL SECURITY", "NO DEPENDS ON EXTENSION", \
"OWNER TO", "RENAME TO", "RESET", "SECURITY", "SET"

/* ALTER ROUTINE options */
#define Alter_routine_options \
Alter_procedure_options, "COST", "IMMUTABLE", "LEAKPROOF", "NOT LEAKPROOF", \
"PARALLEL", "ROWS", "STABLE", "VOLATILE"

/* ALTER FUNCTION options */
#define Alter_function_options \
Alter_routine_options, "CALLED ON NULL INPUT", "RETURNS NULL ON NULL INPUT", \
"STRICT", "SUPPORT"

/* COPY options shared between FROM and TO */
#define Copy_common_options \
"DELIMITER", "ENCODING", "ESCAPE", "FORMAT", "HEADER", "NULL", "QUOTE"

/* COPY FROM options */
#define Copy_from_options \
Copy_common_options, "DEFAULT", "FORCE_NOT_NULL", "FORCE_NULL", "FREEZE", \
"LOG_VERBOSITY", "ON_ERROR", "REJECT_LIMIT"

/* COPY TO options */
#define Copy_to_options \
Copy_common_options, "FORCE_QUOTE", "FORCE_ARRAY"

 /* Known command-starting keywords. */
static const char *const sql_commands[] = {
	"ABORT", "ALTER", "ANALYZE", "BEGIN", "CALL", "CHECKPOINT", "CLOSE", "CLUSTER",
	"COMMENT", "COMMIT", "COPY", "CREATE", "DEALLOCATE", "DECLARE",
	"DELETE FROM", "DISCARD", "DO", "DROP", "END", "EXECUTE", "EXPLAIN",
	"FETCH", "GRANT", "IMPORT FOREIGN SCHEMA", "INSERT INTO", "LISTEN", "LOAD", "LOCK",
	"MERGE INTO", "MOVE", "NOTIFY", "PREPARE",
	"REASSIGN", "REFRESH MATERIALIZED VIEW", "REINDEX", "RELEASE", "REPACK",
	"RESET", "REVOKE", "ROLLBACK",
	"SAVEPOINT", "SECURITY LABEL", "SELECT", "SET", "SHOW", "START",
	"TABLE", "TRUNCATE", "UNLISTEN", "UPDATE", "VACUUM", "VALUES",
	"WAIT FOR", "WITH",
	NULL
};

/*
 * This is a list of all "things" in Pgsql, which can show up after CREATE or
 * DROP; and there is also a query to get a list of them.
 */

typedef struct
{
	const char *name;
	/* Provide at most one of these three types of query: */
	const char *query;			/* simple query, or NULL */
	const VersionedQuery *vquery;	/* versioned query, or NULL */
	const SchemaQuery *squery;	/* schema query, or NULL */
	const char *const *keywords;	/* keywords to be offered as well */
	const uint32 flags;			/* visibility flags, see below */
} pgsql_thing_t;

#define THING_NO_CREATE		(1 << 0)	/* should not show up after CREATE */
#define THING_NO_DROP		(1 << 1)	/* should not show up after DROP */
#define THING_NO_ALTER		(1 << 2)	/* should not show up after ALTER */
#define THING_NO_SHOW		(THING_NO_CREATE | THING_NO_DROP | THING_NO_ALTER)

/* When we have DROP USER etc, also offer MAPPING FOR */
static const char *const Keywords_for_user_thing[] = {
	"MAPPING FOR",
	NULL
};

static const pgsql_thing_t words_after_create[] = {
	{"ACCESS METHOD", NULL, NULL, NULL, NULL, THING_NO_ALTER},
	{"AGGREGATE", NULL, NULL, Query_for_list_of_aggregates},
	{"CAST", NULL, NULL, NULL}, /* Casts have complex structures for names, so
								 * skip it */
	{"COLLATION", NULL, NULL, &Query_for_list_of_collations},

	/*
	 * CREATE CONSTRAINT TRIGGER is not supported here because it is designed
	 * to be used only by pg_dump.
	 */
	{"CONFIGURATION", NULL, NULL, &Query_for_list_of_ts_configurations, NULL, THING_NO_SHOW},
	{"CONVERSION", "SELECT conname FROM pg_catalog.pg_conversion WHERE conname LIKE '%s'"},
	{"DATABASE", Query_for_list_of_databases},
	{"DEFAULT PRIVILEGES", NULL, NULL, NULL, NULL, THING_NO_CREATE | THING_NO_DROP},
	{"DICTIONARY", NULL, NULL, &Query_for_list_of_ts_dictionaries, NULL, THING_NO_SHOW},
	{"DOMAIN", NULL, NULL, &Query_for_list_of_domains},
	{"EVENT TRIGGER", NULL, NULL, NULL},
	{"EXTENSION", Query_for_list_of_extensions},
	{"FOREIGN DATA WRAPPER", NULL, NULL, NULL},
	{"FOREIGN TABLE", NULL, NULL, NULL},
	{"FUNCTION", NULL, NULL, Query_for_list_of_functions},
	{"GROUP", Query_for_list_of_roles},
	{"INDEX", NULL, NULL, &Query_for_list_of_indexes},
	{"LANGUAGE", Query_for_list_of_languages},
	{"LARGE OBJECT", NULL, NULL, NULL, NULL, THING_NO_CREATE | THING_NO_DROP},
	{"MATERIALIZED VIEW", NULL, NULL, &Query_for_list_of_matviews},
	{"OPERATOR", NULL, NULL, NULL}, /* Querying for this is probably not such
									 * a good idea. */
	{"OR REPLACE", NULL, NULL, NULL, NULL, THING_NO_DROP | THING_NO_ALTER},
	{"OWNED", NULL, NULL, NULL, NULL, THING_NO_CREATE | THING_NO_ALTER},	/* for DROP OWNED BY ... */
	{"PARSER", NULL, NULL, &Query_for_list_of_ts_parsers, NULL, THING_NO_SHOW},
	{"POLICY", NULL, NULL, NULL},
	{"PROCEDURE", NULL, NULL, Query_for_list_of_procedures},
	{"PROPERTY GRAPH", NULL, NULL, &Query_for_list_of_propgraphs},
	{"PUBLICATION", Query_for_list_of_publications},
	{"ROLE", Query_for_list_of_roles},
	{"ROUTINE", NULL, NULL, &Query_for_list_of_routines, NULL, THING_NO_CREATE},
	{"RULE", "SELECT rulename FROM pg_catalog.pg_rules WHERE rulename LIKE '%s'"},
	{"SCHEMA", Query_for_list_of_schemas},
	{"SEQUENCE", NULL, NULL, &Query_for_list_of_sequences},
	{"SERVER", Query_for_list_of_servers},
	{"STATISTICS", NULL, NULL, &Query_for_list_of_statistics},
	{"SUBSCRIPTION", Query_for_list_of_subscriptions},
	{"SYSTEM", NULL, NULL, NULL, NULL, THING_NO_CREATE | THING_NO_DROP},
	{"TABLE", NULL, NULL, &Query_for_list_of_tables},
	{"TABLESPACE", Query_for_list_of_tablespaces},
	{"TEMP", NULL, NULL, NULL, NULL, THING_NO_DROP | THING_NO_ALTER},	/* for CREATE TEMP TABLE
																		 * ... */
	{"TEMPLATE", NULL, NULL, &Query_for_list_of_ts_templates, NULL, THING_NO_SHOW},
	{"TEMPORARY", NULL, NULL, NULL, NULL, THING_NO_DROP | THING_NO_ALTER},	/* for CREATE TEMPORARY
																			 * TABLE ... */
	{"TEXT SEARCH", NULL, NULL, NULL},
	{"TRANSFORM", NULL, NULL, NULL, NULL, THING_NO_ALTER},
	{"TRIGGER", "SELECT tgname FROM pg_catalog.pg_trigger WHERE tgname LIKE '%s' AND NOT tgisinternal"},
	{"TYPE", NULL, NULL, &Query_for_list_of_datatypes},
	{"UNIQUE", NULL, NULL, NULL, NULL, THING_NO_DROP | THING_NO_ALTER}, /* for CREATE UNIQUE
																		 * INDEX ... */
	{"UNLOGGED", NULL, NULL, NULL, NULL, THING_NO_DROP | THING_NO_ALTER},	/* for CREATE UNLOGGED
																			 * TABLE ... */
	{"USER", Query_for_list_of_roles, NULL, NULL, Keywords_for_user_thing},
	{"USER MAPPING FOR", NULL, NULL, NULL},
	{"VIEW", NULL, NULL, &Query_for_list_of_views},
	{NULL}						/* end of list */
};

/*
 * The tcpatterns[] table provides the initial pattern-match rule for each
 * switch case in match_previous_words().  The contents of the table
 * are constructed by gen_tabcomplete.pl.
 */

/* Basic match rules appearing in tcpatterns[].kind */
enum TCPatternKind
{
	Match,
	MatchCS,
	HeadMatch,
	HeadMatchCS,
	TailMatch,
	TailMatchCS,
};

/* Things besides string literals that can appear in tcpatterns[].words */
#define MatchAny  NULL
#define MatchAnyExcept(pattern)  ("!" pattern)
#define MatchAnyN ""

/* One entry in tcpatterns[] */
typedef struct
{
	int			id;				/* case label used in match_previous_words */
	enum TCPatternKind kind;	/* match kind, see above */
	int			nwords;			/* length of words[] array */
	const char *const *words;	/* array of match words */
} TCPattern;

/* Macro emitted by gen_tabcomplete.pl to fill a tcpatterns[] entry */
#define TCPAT(id, kind, ...) \
	{ (id), (kind), VA_ARGS_NARGS(__VA_ARGS__), \
	  (const char * const []) { __VA_ARGS__ } }

#ifdef SWITCH_CONVERSION_APPLIED

static const TCPattern tcpatterns[] =
{
	TCPAT(1, TailMatch, "CREATE"),
	TCPAT(2, TailMatch, "CREATE", "OR", "REPLACE"),
	TCPAT(3, Match, "DROP"),
	TCPAT(4, Match, "ALTER", "TABLE"),
	TCPAT(5, Match, "ALTER"),
	TCPAT(6, TailMatch, "ALL", "IN", "TABLESPACE", MatchAny),
	TCPAT(7, TailMatch, "ALL", "IN", "TABLESPACE", MatchAny, "OWNED", "BY"),
	TCPAT(8, TailMatch, "ALL", "IN", "TABLESPACE", MatchAny, "OWNED", "BY", MatchAny),
	TCPAT(9, Match, "ALTER", "AGGREGATE|FUNCTION|PROCEDURE|ROUTINE", MatchAny),
	TCPAT(10, Match, "ALTER", "AGGREGATE", MatchAny, MatchAny),
	TCPAT(11, Match, "ALTER", "FUNCTION", MatchAny, MatchAny),
	TCPAT(12, Match, "ALTER", "PROCEDURE", MatchAny, MatchAny),
	TCPAT(13, Match, "ALTER", "ROUTINE", MatchAny, MatchAny),
	TCPAT(14, Match, "ALTER", "FUNCTION|ROUTINE", MatchAny, MatchAny, "PARALLEL"),
	TCPAT(15, Match, "ALTER", "FUNCTION|PROCEDURE|ROUTINE", MatchAny, MatchAny, "SECURITY"),
	TCPAT(15, Match, "ALTER", "FUNCTION|PROCEDURE|ROUTINE", MatchAny, MatchAny, "EXTERNAL", "SECURITY"),
	TCPAT(16, Match, "ALTER", "FUNCTION|PROCEDURE|ROUTINE", MatchAny, MatchAny, "RESET"),
	TCPAT(17, Match, "ALTER", "FUNCTION|PROCEDURE|ROUTINE", MatchAny, MatchAny, "SET"),
	TCPAT(18, Match, "ALTER", "PUBLICATION", MatchAny),
	TCPAT(19, Match, "ALTER", "PUBLICATION", MatchAny, "ADD"),
	TCPAT(20, Match, "ALTER", "PUBLICATION", MatchAny, "ADD|SET", "TABLE"),
	TCPAT(21, HeadMatch, "ALTER", "PUBLICATION", MatchAny, "ADD|SET", "TABLE"),
	TCPAT(22, Match, "ALTER", "PUBLICATION", MatchAny, MatchAnyN, "WHERE"),
	TCPAT(23, Match, "ALTER", "PUBLICATION", MatchAny, MatchAnyN, "WHERE", "("),
	TCPAT(24, HeadMatch, "ALTER", "PUBLICATION", MatchAny, "ADD|SET", "TABLE"),
	TCPAT(25, HeadMatch, "ALTER", "PUBLICATION", MatchAny, "ADD|SET", "TABLE"),
	TCPAT(26, Match, "ALTER", "PUBLICATION", MatchAny, "DROP"),
	TCPAT(27, Match, "ALTER", "PUBLICATION", MatchAny, "SET"),
	TCPAT(28, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL"),
	TCPAT(29, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL", "TABLES"),
	TCPAT(30, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL", "TABLES", "EXCEPT"),
	TCPAT(31, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL", "TABLES", "EXCEPT", "("),
	TCPAT(32, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL", "TABLES", "EXCEPT", "(", "TABLE"),
	TCPAT(33, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL", "TABLES", "EXCEPT", "(", "TABLE", MatchAnyN),
	TCPAT(34, Match, "ALTER", "PUBLICATION", MatchAny, "SET", "ALL", "TABLES", "EXCEPT", "(", "TABLE", MatchAnyN),
	TCPAT(35, Match, "ALTER", "PUBLICATION", MatchAny, "ADD|DROP|SET", "TABLES", "IN", "SCHEMA"),
	TCPAT(36, Match, "ALTER", "PUBLICATION", MatchAny, MatchAnyN, "SET", "("),
	TCPAT(37, Match, "ALTER", "SUBSCRIPTION", MatchAny),
	TCPAT(38, Match, "ALTER", "SUBSCRIPTION", MatchAny, "SERVER"),
	TCPAT(39, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "REFRESH"),
	TCPAT(40, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "REFRESH", "PUBLICATION"),
	TCPAT(41, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "REFRESH", "PUBLICATION", "WITH", "("),
	TCPAT(42, Match, "ALTER", "SUBSCRIPTION", MatchAny, "SET"),
	TCPAT(43, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "SET", "("),
	TCPAT(44, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "SKIP", "("),
	TCPAT(45, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "SET", "PUBLICATION"),
	TCPAT(46, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "ADD|DROP|SET", "PUBLICATION", MatchAny),
	TCPAT(47, Match, "ALTER", "SUBSCRIPTION", MatchAny, MatchAnyN, "ADD|DROP|SET", "PUBLICATION", MatchAny, "WITH", "("),
	TCPAT(48, Match, "ALTER", "SCHEMA", MatchAny),
	TCPAT(49, Match, "ALTER", "COLLATION", MatchAny),
	TCPAT(50, Match, "ALTER", "CONVERSION", MatchAny),
	TCPAT(51, Match, "ALTER", "DATABASE", MatchAny),
	TCPAT(52, Match, "ALTER", "DATABASE", MatchAny, "RESET"),
	TCPAT(53, Match, "ALTER", "DATABASE", MatchAny, "SET", "TABLESPACE"),
	TCPAT(54, Match, "ALTER", "EVENT", "TRIGGER"),
	TCPAT(55, Match, "ALTER", "EVENT", "TRIGGER", MatchAny),
	TCPAT(56, Match, "ALTER", "EVENT", "TRIGGER", MatchAny, "ENABLE"),
	TCPAT(57, Match, "ALTER", "EXTENSION", MatchAny),
	TCPAT(58, Match, "ALTER", "EXTENSION", MatchAny, "ADD|DROP"),
	TCPAT(59, Match, "ALTER", "EXTENSION", MatchAny, "ADD|DROP", "FOREIGN"),
	TCPAT(60, Match, "ALTER", "EXTENSION", MatchAny, "ADD|DROP", "OPERATOR"),
	TCPAT(61, Match, "ALTER", "EXTENSION", MatchAny, "ADD|DROP", "TEXT", "SEARCH"),
	TCPAT(62, Match, "ALTER", "EXTENSION", MatchAny, "UPDATE"),
	TCPAT(63, Match, "ALTER", "EXTENSION", MatchAny, "UPDATE", "TO"),
	TCPAT(64, Match, "ALTER", "FOREIGN"),
	TCPAT(65, Match, "ALTER", "FOREIGN", "DATA", "WRAPPER", MatchAny),
	TCPAT(66, Match, "ALTER", "FOREIGN", "DATA", "WRAPPER", MatchAny, "NO"),
	TCPAT(67, Match, "ALTER", "FOREIGN", "TABLE", MatchAny),
	TCPAT(68, Match, "ALTER", "INDEX"),
	TCPAT(69, Match, "ALTER", "INDEX", MatchAny),
	TCPAT(70, Match, "ALTER", "INDEX", MatchAny, "ATTACH"),
	TCPAT(71, Match, "ALTER", "INDEX", MatchAny, "ATTACH", "PARTITION"),
	TCPAT(72, Match, "ALTER", "INDEX", MatchAny, "ALTER"),
	TCPAT(73, Match, "ALTER", "INDEX", MatchAny, "ALTER", "COLUMN"),
	TCPAT(74, Match, "ALTER", "INDEX", MatchAny, "ALTER", "COLUMN", MatchAny),
	TCPAT(75, Match, "ALTER", "INDEX", MatchAny, "ALTER", "COLUMN", MatchAny, "SET"),
	TCPAT(76, Match, "ALTER", "INDEX", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "STATISTICS"),
	TCPAT(77, Match, "ALTER", "INDEX", MatchAny, "SET"),
	TCPAT(78, Match, "ALTER", "INDEX", MatchAny, "RESET"),
	TCPAT(79, Match, "ALTER", "INDEX", MatchAny, "RESET", "("),
	TCPAT(80, Match, "ALTER", "INDEX", MatchAny, "SET", "("),
	TCPAT(81, Match, "ALTER", "INDEX", MatchAny, "NO", "DEPENDS"),
	TCPAT(82, Match, "ALTER", "INDEX", MatchAny, "DEPENDS"),
	TCPAT(83, Match, "ALTER", "LANGUAGE", MatchAny),
	TCPAT(84, Match, "ALTER", "LARGE", "OBJECT", MatchAny),
	TCPAT(85, Match, "ALTER", "MATERIALIZED", "VIEW"),
	TCPAT(86, Match, "ALTER", "USER|ROLE", MatchAny),
	TCPAT(87, Match, "ALTER", "USER|ROLE", MatchAny, "IN"),
	TCPAT(88, Match, "ALTER", "USER|ROLE", MatchAny, "IN", "DATABASE"),
	TCPAT(89, Match, "ALTER", "USER|ROLE", MatchAny, "IN", "DATABASE", MatchAny),
	TCPAT(90, Match, "ALTER", "USER|ROLE", MatchAny, "IN", "DATABASE", MatchAny, "SET"),
	TCPAT(91, Match, "ALTER", "USER|ROLE", MatchAny, "RESET"),
	TCPAT(92, Match, "ALTER", "USER|ROLE", MatchAny, "WITH"),
	TCPAT(93, Match, "ALTER", "DEFAULT", "PRIVILEGES"),
	TCPAT(94, Match, "ALTER", "DEFAULT", "PRIVILEGES", "FOR"),
	TCPAT(95, Match, "ALTER", "DEFAULT", "PRIVILEGES", "IN"),
	TCPAT(96, Match, "ALTER", "DEFAULT", "PRIVILEGES", "FOR", "ROLE|USER", MatchAny),
	TCPAT(97, Match, "ALTER", "DEFAULT", "PRIVILEGES", "IN", "SCHEMA", MatchAny),
	TCPAT(98, Match, "ALTER", "DEFAULT", "PRIVILEGES", "IN", "SCHEMA", MatchAny, "FOR"),
	TCPAT(99, Match, "ALTER", "DEFAULT", "PRIVILEGES", "FOR", "ROLE|USER", MatchAny, "IN", "SCHEMA", MatchAny),
	TCPAT(99, Match, "ALTER", "DEFAULT", "PRIVILEGES", "IN", "SCHEMA", MatchAny, "FOR", "ROLE|USER", MatchAny),
	TCPAT(100, Match, "ALTER", "DOMAIN", MatchAny),
	TCPAT(101, Match, "ALTER", "DOMAIN", MatchAny, "ADD"),
	TCPAT(102, Match, "ALTER", "DOMAIN", MatchAny, "ADD", "CONSTRAINT", MatchAny),
	TCPAT(103, Match, "ALTER", "DOMAIN", MatchAny, "DROP"),
	TCPAT(104, Match, "ALTER", "DOMAIN", MatchAny, "DROP|RENAME|VALIDATE", "CONSTRAINT"),
	TCPAT(105, Match, "ALTER", "DOMAIN", MatchAny, "RENAME"),
	TCPAT(106, Match, "ALTER", "DOMAIN", MatchAny, "RENAME", "CONSTRAINT", MatchAny),
	TCPAT(107, Match, "ALTER", "DOMAIN", MatchAny, "SET"),
	TCPAT(108, Match, "ALTER", "SEQUENCE", MatchAny),
	TCPAT(109, TailMatch, "ALTER", "SEQUENCE", MatchAny, "AS"),
	TCPAT(110, Match, "ALTER", "SEQUENCE", MatchAny, "NO"),
	TCPAT(111, Match, "ALTER", "SEQUENCE", MatchAny, "SET"),
	TCPAT(112, Match, "ALTER", "SERVER", MatchAny),
	TCPAT(113, Match, "ALTER", "SERVER", MatchAny, "VERSION", MatchAny),
	TCPAT(114, Match, "ALTER", "SYSTEM"),
	TCPAT(115, Match, "ALTER", "SYSTEM", "SET|RESET"),
	TCPAT(116, Match, "ALTER", "SYSTEM", "SET", MatchAny),
	TCPAT(117, Match, "ALTER", "VIEW", MatchAny),
	TCPAT(118, Match, "ALTER", "VIEW", MatchAny, "RENAME"),
	TCPAT(119, Match, "ALTER", "VIEW", MatchAny, "ALTER|RENAME", "COLUMN"),
	TCPAT(120, Match, "ALTER", "VIEW", MatchAny, "ALTER", MatchAny),
	TCPAT(120, Match, "ALTER", "VIEW", MatchAny, "ALTER", "COLUMN", MatchAny),
	TCPAT(121, Match, "ALTER", "VIEW", MatchAny, "RENAME", MatchAnyExcept("TO")),
	TCPAT(122, Match, "ALTER", "VIEW", MatchAny, "RENAME", "COLUMN", MatchAnyExcept("TO")),
	TCPAT(123, Match, "ALTER", "VIEW", MatchAny, "RESET"),
	TCPAT(124, Match, "ALTER", "VIEW", MatchAny, "SET"),
	TCPAT(125, Match, "ALTER", "VIEW", MatchAny, "SET|RESET", "("),
	TCPAT(126, Match, "ALTER", "VIEW", MatchAny, "SET", "(", MatchAny),
	TCPAT(127, Match, "ALTER", "VIEW", MatchAny, "SET", "(", "check_option", "="),
	TCPAT(128, Match, "ALTER", "VIEW", MatchAny, "SET", "(", "security_barrier|security_invoker", "="),
	TCPAT(129, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny),
	TCPAT(130, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny, "RENAME"),
	TCPAT(131, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny, "ALTER|RENAME", "COLUMN"),
	TCPAT(132, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny, "RENAME", MatchAnyExcept("TO")),
	TCPAT(133, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny, "RENAME", "COLUMN", MatchAnyExcept("TO")),
	TCPAT(134, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny, "SET"),
	TCPAT(135, Match, "ALTER", "MATERIALIZED", "VIEW", MatchAny, "SET", "ACCESS", "METHOD"),
	TCPAT(136, Match, "ALTER", "POLICY"),
	TCPAT(137, Match, "ALTER", "POLICY", MatchAny),
	TCPAT(138, Match, "ALTER", "POLICY", MatchAny, "ON"),
	TCPAT(139, Match, "ALTER", "POLICY", MatchAny, "ON", MatchAny),
	TCPAT(140, Match, "ALTER", "POLICY", MatchAny, "ON", MatchAny, "TO"),
	TCPAT(141, Match, "ALTER", "POLICY", MatchAny, "ON", MatchAny, "USING"),
	TCPAT(142, Match, "ALTER", "POLICY", MatchAny, "ON", MatchAny, "WITH", "CHECK"),
	TCPAT(143, Match, "ALTER", "PROPERTY", "GRAPH"),
	TCPAT(144, Match, "ALTER", "PROPERTY", "GRAPH", MatchAny),
	TCPAT(145, Match, "ALTER", "PROPERTY", "GRAPH", MatchAny, "ADD|ALTER|DROP"),
	TCPAT(146, Match, "ALTER", "PROPERTY", "GRAPH", MatchAny, "ADD|DROP", "VERTEX|EDGE"),
	TCPAT(147, HeadMatch, "ALTER", "PROPERTY", "GRAPH", MatchAny, "ADD"),
	TCPAT(148, Match, "ALTER", "PROPERTY", "GRAPH", MatchAny, "ALTER", "VERTEX|EDGE"),
	TCPAT(149, Match, "ALTER", "RULE", MatchAny),
	TCPAT(150, Match, "ALTER", "RULE", MatchAny, "ON"),
	TCPAT(151, Match, "ALTER", "RULE", MatchAny, "ON", MatchAny),
	TCPAT(152, Match, "ALTER", "STATISTICS", MatchAny),
	TCPAT(153, Match, "ALTER", "STATISTICS", MatchAny, "SET"),
	TCPAT(154, Match, "ALTER", "TRIGGER", MatchAny),
	TCPAT(155, Match, "ALTER", "TRIGGER", MatchAny, "ON"),
	TCPAT(156, Match, "ALTER", "TRIGGER", MatchAny, "ON", MatchAny),
	TCPAT(157, Match, "ALTER", "TABLE", MatchAny),
	TCPAT(158, Match, "ALTER", "TABLE", MatchAny, "ADD"),
	TCPAT(159, Match, "ALTER", "TABLE", MatchAny, "ADD", "COLUMN", MatchAny),
	TCPAT(159, Match, "ALTER", "TABLE", MatchAny, "ADD", MatchAnyExcept("COLUMN|CONSTRAINT|CHECK|UNIQUE|PRIMARY|NOT|EXCLUDE|FOREIGN")),
	TCPAT(160, Match, "ALTER", "TABLE", MatchAny, "ADD", "CONSTRAINT", MatchAny),
	TCPAT(161, Match, "ALTER", "TABLE", MatchAny, "ADD", "NOT", "NULL"),
	TCPAT(162, Match, "ALTER", "TABLE", MatchAny, "ADD", "CONSTRAINT", MatchAny, "NOT", "NULL"),
	TCPAT(163, Match, "ALTER", "TABLE", MatchAny, "ADD", "PRIMARY", "KEY"),
	TCPAT(163, Match, "ALTER", "TABLE", MatchAny, "ADD", "UNIQUE"),
	TCPAT(163, Match, "ALTER", "TABLE", MatchAny, "ADD", "CONSTRAINT", MatchAny, "PRIMARY", "KEY"),
	TCPAT(163, Match, "ALTER", "TABLE", MatchAny, "ADD", "CONSTRAINT", MatchAny, "UNIQUE"),
	TCPAT(164, Match, "ALTER", "TABLE", MatchAny, "ADD", "PRIMARY", "KEY", "USING", "INDEX"),
	TCPAT(165, Match, "ALTER", "TABLE", MatchAny, "ADD", "UNIQUE", "USING", "INDEX"),
	TCPAT(166, Match, "ALTER", "TABLE", MatchAny, "ADD", "CONSTRAINT", MatchAny, "PRIMARY", "KEY", "USING", "INDEX"),
	TCPAT(167, Match, "ALTER", "TABLE", MatchAny, "ADD", "CONSTRAINT", MatchAny, "UNIQUE", "USING", "INDEX"),
	TCPAT(168, Match, "ALTER", "TABLE", MatchAny, "ENABLE"),
	TCPAT(169, Match, "ALTER", "TABLE", MatchAny, "ENABLE", "REPLICA|ALWAYS"),
	TCPAT(170, Match, "ALTER", "TABLE", MatchAny, "ENABLE", "RULE"),
	TCPAT(171, Match, "ALTER", "TABLE", MatchAny, "ENABLE", MatchAny, "RULE"),
	TCPAT(172, Match, "ALTER", "TABLE", MatchAny, "ENABLE", "TRIGGER"),
	TCPAT(173, Match, "ALTER", "TABLE", MatchAny, "ENABLE", MatchAny, "TRIGGER"),
	TCPAT(174, Match, "ALTER", "TABLE", MatchAny, "INHERIT"),
	TCPAT(175, Match, "ALTER", "TABLE", MatchAny, "NO"),
	TCPAT(176, Match, "ALTER", "TABLE", MatchAny, "NO", "INHERIT"),
	TCPAT(177, Match, "ALTER", "TABLE", MatchAny, "DISABLE"),
	TCPAT(178, Match, "ALTER", "TABLE", MatchAny, "DISABLE", "RULE"),
	TCPAT(179, Match, "ALTER", "TABLE", MatchAny, "DISABLE", "TRIGGER"),
	TCPAT(180, Match, "ALTER", "TABLE", MatchAny, "ALTER"),
	TCPAT(181, Match, "ALTER", "TABLE", MatchAny, "RENAME"),
	TCPAT(182, Match, "ALTER", "TABLE", MatchAny, "ALTER|RENAME", "COLUMN"),
	TCPAT(183, Match, "ALTER", "TABLE", MatchAny, "RENAME", MatchAnyExcept("CONSTRAINT|TO")),
	TCPAT(184, Match, "ALTER", "TABLE", MatchAny, "RENAME", "COLUMN|CONSTRAINT", MatchAnyExcept("TO")),
	TCPAT(185, Match, "ALTER", "TABLE", MatchAny, "DROP"),
	TCPAT(186, Match, "ALTER", "TABLE", MatchAny, "DROP", "COLUMN"),
	TCPAT(187, Match, "ALTER", "TABLE", MatchAny, "ALTER|DROP|RENAME", "CONSTRAINT"),
	TCPAT(188, Match, "ALTER", "TABLE", MatchAny, "VALIDATE", "CONSTRAINT"),
	TCPAT(189, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny),
	TCPAT(189, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny),
	TCPAT(190, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "ADD"),
	TCPAT(190, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "ADD"),
	TCPAT(191, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "ADD", "GENERATED"),
	TCPAT(191, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "ADD", "GENERATED"),
	TCPAT(192, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "ADD", "GENERATED", "ALWAYS"),
	TCPAT(192, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "ADD", "GENERATED", "ALWAYS"),
	TCPAT(192, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "ADD", "GENERATED", "BY", "DEFAULT"),
	TCPAT(192, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "ADD", "GENERATED", "BY", "DEFAULT"),
	TCPAT(193, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET"),
	TCPAT(193, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET"),
	TCPAT(194, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "("),
	TCPAT(194, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "("),
	TCPAT(195, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "COMPRESSION"),
	TCPAT(195, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "COMPRESSION"),
	TCPAT(196, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "EXPRESSION"),
	TCPAT(196, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "EXPRESSION"),
	TCPAT(197, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "EXPRESSION", "AS"),
	TCPAT(197, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "EXPRESSION", "AS"),
	TCPAT(198, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "GENERATED"),
	TCPAT(198, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "GENERATED"),
	TCPAT(199, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "NO"),
	TCPAT(199, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "NO"),
	TCPAT(200, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "STORAGE"),
	TCPAT(200, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "STORAGE"),
	TCPAT(201, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "SET", "STATISTICS"),
	TCPAT(201, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "SET", "STATISTICS"),
	TCPAT(202, Match, "ALTER", "TABLE", MatchAny, "ALTER", "COLUMN", MatchAny, "DROP"),
	TCPAT(202, Match, "ALTER", "TABLE", MatchAny, "ALTER", MatchAny, "DROP"),
	TCPAT(203, Match, "ALTER", "TABLE", MatchAny, "CLUSTER"),
	TCPAT(204, Match, "ALTER", "TABLE", MatchAny, "CLUSTER", "ON"),
	TCPAT(205, Match, "ALTER", "TABLE", MatchAny, "SET"),
	TCPAT(206, Match, "ALTER", "TABLE", MatchAny, "SET", "ACCESS", "METHOD"),
	TCPAT(207, Match, "ALTER", "TABLE", MatchAny, "SET", "TABLESPACE"),
	TCPAT(208, Match, "ALTER", "TABLE", MatchAny, "SET", "WITHOUT"),
	TCPAT(209, Match, "ALTER", "TABLE", MatchAny, "RESET"),
	TCPAT(210, Match, "ALTER", "TABLE", MatchAny, "SET|RESET", "("),
	TCPAT(211, Match, "ALTER", "TABLE", MatchAny, "REPLICA", "IDENTITY", "USING", "INDEX"),
	TCPAT(212, Match, "ALTER", "TABLE", MatchAny, "REPLICA", "IDENTITY", "USING"),
	TCPAT(213, Match, "ALTER", "TABLE", MatchAny, "REPLICA", "IDENTITY"),
	TCPAT(214, Match, "ALTER", "TABLE", MatchAny, "REPLICA"),
	TCPAT(215, Match, "ALTER", "TABLE", MatchAny, "ATTACH", "PARTITION"),
	TCPAT(216, TailMatch, "ATTACH", "PARTITION", MatchAny),
	TCPAT(217, TailMatch, "FOR", "VALUES"),
	TCPAT(218, Match, "ALTER", "TABLE", MatchAny, "DETACH|SPLIT", "PARTITION"),
	TCPAT(219, Match, "ALTER", "TABLE", MatchAny, "DETACH", "PARTITION", MatchAny),
	TCPAT(220, Match, "ALTER", "TABLE", MatchAny, "SPLIT", "PARTITION", MatchAny),
	TCPAT(221, Match, "ALTER", "TABLE", MatchAny, "MERGE", "PARTITIONS", "("),
	TCPAT(222, Match, "ALTER", "TABLE", MatchAny, "MERGE", "PARTITIONS", "(*)"),
	TCPAT(223, Match, "ALTER", "TABLE", MatchAny, "OF"),
	TCPAT(224, Match, "ALTER", "TABLESPACE", MatchAny),
	TCPAT(225, Match, "ALTER", "TABLESPACE", MatchAny, "SET|RESET"),
	TCPAT(226, Match, "ALTER", "TABLESPACE", MatchAny, "SET|RESET", "("),
	TCPAT(227, Match, "ALTER", "TEXT", "SEARCH"),
	TCPAT(228, Match, "ALTER", "TEXT", "SEARCH", "TEMPLATE|PARSER", MatchAny),
	TCPAT(229, Match, "ALTER", "TEXT", "SEARCH", "DICTIONARY", MatchAny),
	TCPAT(230, Match, "ALTER", "TEXT", "SEARCH", "CONFIGURATION", MatchAny),
	TCPAT(231, Match, "ALTER", "TYPE", MatchAny),
	TCPAT(232, Match, "ALTER", "TYPE", MatchAny, "ADD"),
	TCPAT(233, Match, "ALTER", "TYPE", MatchAny, "RENAME"),
	TCPAT(234, Match, "ALTER", "TYPE", MatchAny, "RENAME", "ATTRIBUTE|VALUE", MatchAny),
	TCPAT(235, Match, "ALTER", "TYPE", MatchAny, "RENAME", "ATTRIBUTE", MatchAny, "TO", MatchAny),
	TCPAT(236, Match, "ALTER", "TYPE", MatchAny, "ALTER|DROP|RENAME", "ATTRIBUTE"),
	TCPAT(237, Match, "ALTER", "TYPE", MatchAny, "ADD", "ATTRIBUTE", MatchAny),
	TCPAT(238, Match, "ALTER", "TYPE", MatchAny, "ADD", "ATTRIBUTE", MatchAny, MatchAny),
	TCPAT(239, Match, "ALTER", "TYPE", MatchAny, "DROP", "ATTRIBUTE", MatchAny),
	TCPAT(240, Match, "ALTER", "TYPE", MatchAny, "ALTER", "ATTRIBUTE", MatchAny),
	TCPAT(241, Match, "ALTER", "TYPE", MatchAny, "ALTER", "ATTRIBUTE", MatchAny, "TYPE", MatchAny),
	TCPAT(242, Match, "ALTER", "TYPE", MatchAny, "RENAME", "VALUE"),
	TCPAT(243, Match, "ALTER", "TYPE", MatchAny, "SET"),
	TCPAT(244, Match, "ALTER", "TYPE", MatchAny, "SET", "("),
	TCPAT(245, Match, "ALTER", "GROUP", MatchAny),
	TCPAT(246, Match, "ALTER", "GROUP", MatchAny, "ADD|DROP"),
	TCPAT(247, Match, "ALTER", "GROUP", MatchAny, "ADD|DROP", "USER"),
	TCPAT(248, Match, "ANALYZE"),
	TCPAT(249, Match, "ANALYZE", "VERBOSE"),
	TCPAT(250, HeadMatch, "ANALYZE", "(*"),
	TCPAT(251, Match, "ANALYZE", "(*)"),
	TCPAT(252, Match, "ANALYZE", MatchAnyN, "("),
	TCPAT(253, HeadMatch, "ANALYZE"),
	TCPAT(254, Match, "BEGIN"),
	TCPAT(255, Match, "END|ABORT"),
	TCPAT(256, Match, "COMMIT"),
	TCPAT(257, Match, "RELEASE"),
	TCPAT(258, Match, "ROLLBACK"),
	TCPAT(259, Match, "ABORT|END|COMMIT|ROLLBACK", "AND"),
	TCPAT(260, Match, "CALL"),
	TCPAT(261, Match, "CALL", MatchAny),
	TCPAT(262, Match, "CHECKPOINT"),
	TCPAT(263, HeadMatch, "CHECKPOINT", "(*"),
	TCPAT(264, Match, "CLOSE"),
	TCPAT(265, Match, "CLUSTER"),
	TCPAT(266, Match, "CLUSTER", "VERBOSE"),
	TCPAT(266, Match, "CLUSTER", "(*)"),
	TCPAT(267, Match, "CLUSTER", MatchAnyExcept("VERBOSE|ON|(|(*)")),
	TCPAT(268, Match, "CLUSTER", "VERBOSE|(*)", MatchAny),
	TCPAT(269, Match, "CLUSTER", MatchAny, "USING"),
	TCPAT(269, Match, "CLUSTER", "VERBOSE|(*)", MatchAny, "USING"),
	TCPAT(270, HeadMatch, "CLUSTER", "(*"),
	TCPAT(271, Match, "COMMENT"),
	TCPAT(272, Match, "COMMENT", "ON"),
	TCPAT(273, Match, "COMMENT", "ON", "ACCESS", "METHOD"),
	TCPAT(274, Match, "COMMENT", "ON", "CONSTRAINT"),
	TCPAT(275, Match, "COMMENT", "ON", "CONSTRAINT", MatchAny),
	TCPAT(276, Match, "COMMENT", "ON", "CONSTRAINT", MatchAny, "ON"),
	TCPAT(277, Match, "COMMENT", "ON", "CONSTRAINT", MatchAny, "ON", "DOMAIN"),
	TCPAT(278, Match, "COMMENT", "ON", "EVENT", "TRIGGER"),
	TCPAT(279, Match, "COMMENT", "ON", "FOREIGN"),
	TCPAT(280, Match, "COMMENT", "ON", "FOREIGN", "TABLE"),
	TCPAT(281, Match, "COMMENT", "ON", "MATERIALIZED", "VIEW"),
	TCPAT(282, Match, "COMMENT", "ON", "POLICY"),
	TCPAT(283, Match, "COMMENT", "ON", "POLICY", MatchAny),
	TCPAT(284, Match, "COMMENT", "ON", "POLICY", MatchAny, "ON"),
	TCPAT(285, Match, "COMMENT", "ON", "PROCEDURAL", "LANGUAGE"),
	TCPAT(286, Match, "COMMENT", "ON", "PROPERTY", "GRAPH"),
	TCPAT(287, Match, "COMMENT", "ON", "RULE", MatchAny),
	TCPAT(288, Match, "COMMENT", "ON", "RULE", MatchAny, "ON"),
	TCPAT(289, Match, "COMMENT", "ON", "TEXT", "SEARCH"),
	TCPAT(290, Match, "COMMENT", "ON", "TEXT", "SEARCH", "CONFIGURATION"),
	TCPAT(291, Match, "COMMENT", "ON", "TEXT", "SEARCH", "DICTIONARY"),
	TCPAT(292, Match, "COMMENT", "ON", "TEXT", "SEARCH", "PARSER"),
	TCPAT(293, Match, "COMMENT", "ON", "TEXT", "SEARCH", "TEMPLATE"),
	TCPAT(294, Match, "COMMENT", "ON", "TRANSFORM", "FOR"),
	TCPAT(295, Match, "COMMENT", "ON", "TRANSFORM", "FOR", MatchAny),
	TCPAT(296, Match, "COMMENT", "ON", "TRANSFORM", "FOR", MatchAny, "LANGUAGE"),
	TCPAT(297, Match, "COMMENT", "ON", "TRIGGER", MatchAny),
	TCPAT(298, Match, "COMMENT", "ON", "TRIGGER", MatchAny, "ON"),
	TCPAT(299, Match, "COMMENT", "ON", MatchAny, MatchAnyExcept("IS")),
	TCPAT(299, Match, "COMMENT", "ON", MatchAny, MatchAny, MatchAnyExcept("IS")),
	TCPAT(299, Match, "COMMENT", "ON", MatchAny, MatchAny, MatchAny, MatchAnyExcept("IS")),
	TCPAT(299, Match, "COMMENT", "ON", MatchAny, MatchAny, MatchAny, MatchAny, MatchAnyExcept("IS")),
	TCPAT(300, Match, "COPY|\\copy"),
	TCPAT(301, Match, "COPY|\\copy", "("),
	TCPAT(302, Match, "COPY|\\copy", MatchAny),
	TCPAT(303, Match, "COPY|\\copy", MatchAny, "FROM|TO"),
	TCPAT(304, Match, "COPY|\\copy", MatchAny, "FROM|TO", "PROGRAM"),
	TCPAT(305, Match, "COPY|\\copy", MatchAny, "TO", MatchAnyExcept("PROGRAM")),
	TCPAT(305, Match, "COPY|\\copy", MatchAny, "TO", "PROGRAM", MatchAny),
	TCPAT(306, Match, "COPY|\\copy", MatchAny, "FROM", MatchAnyExcept("PROGRAM")),
	TCPAT(306, Match, "COPY|\\copy", MatchAny, "FROM", "PROGRAM", MatchAny),
	TCPAT(307, HeadMatch, "COPY|\\copy", MatchAny, "FROM|TO", MatchAnyExcept("PROGRAM"), "WITH", "(*"),
	TCPAT(307, HeadMatch, "COPY|\\copy", MatchAny, "FROM|TO", "PROGRAM", MatchAny, "WITH", "(*"),
	TCPAT(308, Match, "COPY|\\copy", MatchAny, "FROM", MatchAnyExcept("PROGRAM"), "WITH", MatchAny),
	TCPAT(308, Match, "COPY|\\copy", MatchAny, "FROM", "PROGRAM", MatchAny, "WITH", MatchAny),
	TCPAT(309, Match, "CREATE", "ACCESS", "METHOD", MatchAny),
	TCPAT(310, Match, "CREATE", "ACCESS", "METHOD", MatchAny, "TYPE"),
	TCPAT(311, Match, "CREATE", "ACCESS", "METHOD", MatchAny, "TYPE", MatchAny),
	TCPAT(312, Match, "CREATE", "COLLATION", MatchAny),
	TCPAT(313, Match, "CREATE", "COLLATION", MatchAny, "FROM"),
	TCPAT(314, HeadMatch, "CREATE", "COLLATION", MatchAny, "(*"),
	TCPAT(315, Match, "CREATE", "DATABASE", MatchAny),
	TCPAT(316, Match, "CREATE", "DATABASE", MatchAny, "TEMPLATE"),
	TCPAT(317, Match, "CREATE", "DATABASE", MatchAny, "STRATEGY"),
	TCPAT(318, TailMatch, "CREATE", "DOMAIN", MatchAny),
	TCPAT(319, TailMatch, "CREATE", "DOMAIN", MatchAny, "AS"),
	TCPAT(320, TailMatch, "CREATE", "DOMAIN", MatchAny, "AS", MatchAny),
	TCPAT(321, TailMatch, "CREATE", "DOMAIN", MatchAny, "COLLATE"),
	TCPAT(322, Match, "CREATE", "EXTENSION"),
	TCPAT(323, Match, "CREATE", "EXTENSION", MatchAny),
	TCPAT(324, Match, "CREATE", "EXTENSION", MatchAny, "VERSION"),
	TCPAT(325, Match, "CREATE", "FOREIGN"),
	TCPAT(326, Match, "CREATE", "FOREIGN", "DATA", "WRAPPER", MatchAny),
	TCPAT(327, Match, "CREATE", "FOREIGN", "TABLE", MatchAny),
	TCPAT(328, TailMatch, "CREATE", "UNIQUE"),
	TCPAT(329, TailMatch, "CREATE|UNIQUE", "INDEX"),
	TCPAT(330, TailMatch, "INDEX|CONCURRENTLY", MatchAny, "ON"),
	TCPAT(330, TailMatch, "INDEX|CONCURRENTLY", "ON"),
	TCPAT(331, TailMatch, "CREATE|UNIQUE", "INDEX", "CONCURRENTLY"),
	TCPAT(332, TailMatch, "CREATE|UNIQUE", "INDEX", MatchAny),
	TCPAT(332, TailMatch, "CREATE|UNIQUE", "INDEX", "CONCURRENTLY", MatchAny),
	TCPAT(333, TailMatch, "INDEX", MatchAny, "ON", MatchAny),
	TCPAT(333, TailMatch, "INDEX|CONCURRENTLY", "ON", MatchAny),
	TCPAT(334, TailMatch, "INDEX", MatchAny, "ON", MatchAny, "("),
	TCPAT(334, TailMatch, "INDEX|CONCURRENTLY", "ON", MatchAny, "("),
	TCPAT(335, TailMatch, "ON", MatchAny, "USING", MatchAny, "("),
	TCPAT(336, TailMatch, "INDEX", MatchAny, MatchAny, "ON", MatchAny, "USING"),
	TCPAT(336, TailMatch, "INDEX", MatchAny, "ON", MatchAny, "USING"),
	TCPAT(336, TailMatch, "INDEX", "ON", MatchAny, "USING"),
	TCPAT(337, TailMatch, "ON", MatchAny, "USING", MatchAny),
	TCPAT(338, Match, "CREATE", "OR"),
	TCPAT(339, Match, "CREATE", "POLICY", MatchAny),
	TCPAT(340, Match, "CREATE", "POLICY", MatchAny, "ON"),
	TCPAT(341, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny),
	TCPAT(342, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS"),
	TCPAT(343, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny),
	TCPAT(344, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "FOR"),
	TCPAT(345, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "FOR", "INSERT"),
	TCPAT(346, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "FOR", "SELECT|DELETE"),
	TCPAT(347, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "FOR", "ALL|UPDATE"),
	TCPAT(348, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "TO"),
	TCPAT(349, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "USING"),
	TCPAT(350, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny, "FOR"),
	TCPAT(351, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny, "FOR", "INSERT"),
	TCPAT(352, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny, "FOR", "SELECT|DELETE"),
	TCPAT(353, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny, "FOR", "ALL|UPDATE"),
	TCPAT(354, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny, "TO"),
	TCPAT(355, Match, "CREATE", "POLICY", MatchAny, "ON", MatchAny, "AS", MatchAny, "USING"),
	TCPAT(356, Match, "CREATE", "PROPERTY"),
	TCPAT(357, Match, "CREATE", "PROPERTY", "GRAPH", MatchAny),
	TCPAT(358, Match, "CREATE", "PROPERTY", "GRAPH", MatchAny, "VERTEX|NODE"),
	TCPAT(359, Match, "CREATE", "PROPERTY", "GRAPH", MatchAny, "VERTEX|NODE", "TABLES"),
	TCPAT(360, Match, "CREATE", "PROPERTY", "GRAPH", MatchAny, "VERTEX|NODE", "TABLES", "("),
	TCPAT(361, Match, "CREATE", "PROPERTY", "GRAPH", MatchAny, "VERTEX|NODE", "TABLES", "(*)"),
	TCPAT(362, HeadMatch, "CREATE", "PROPERTY", "GRAPH"),
	TCPAT(363, HeadMatch, "CREATE", "PROPERTY", "GRAPH"),
	TCPAT(364, HeadMatch, "CREATE", "PROPERTY", "GRAPH"),
	TCPAT(365, Match, "CREATE", "PUBLICATION", MatchAny),
	TCPAT(366, Match, "CREATE", "PUBLICATION", MatchAny, "FOR"),
	TCPAT(367, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL"),
	TCPAT(368, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL", "TABLES"),
	TCPAT(369, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL", "TABLES", "EXCEPT"),
	TCPAT(370, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL", "TABLES", "EXCEPT", "("),
	TCPAT(371, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL", "TABLES", "EXCEPT", "(", "TABLE"),
	TCPAT(372, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL", "TABLES", "EXCEPT", "(", "TABLE", MatchAnyN),
	TCPAT(373, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "ALL", "TABLES", "EXCEPT", "(", "TABLE", MatchAnyN),
	TCPAT(374, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "TABLES"),
	TCPAT(375, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "TABLE", MatchAny),
	TCPAT(376, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "TABLE"),
	TCPAT(377, Match, "CREATE", "PUBLICATION", MatchAny, MatchAnyN, "WHERE"),
	TCPAT(378, Match, "CREATE", "PUBLICATION", MatchAny, MatchAnyN, "WHERE", "("),
	TCPAT(379, Match, "CREATE", "PUBLICATION", MatchAny, MatchAnyN, "WHERE", "(*)"),
	TCPAT(380, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "TABLES", "IN", "SCHEMA"),
	TCPAT(381, Match, "CREATE", "PUBLICATION", MatchAny, "FOR", "TABLES", "IN", "SCHEMA", MatchAny),
	TCPAT(382, Match, "CREATE", "PUBLICATION", MatchAnyN, "WITH", "("),
	TCPAT(383, Match, "CREATE", "RULE", MatchAny),
	TCPAT(383, Match, "CREATE", "OR", "REPLACE", "RULE", MatchAny),
	TCPAT(384, Match, "CREATE", "RULE", MatchAny, "AS"),
	TCPAT(384, Match, "CREATE", "OR", "REPLACE", "RULE", MatchAny, "AS"),
	TCPAT(385, Match, "CREATE", "RULE", MatchAny, "AS", "ON"),
	TCPAT(385, Match, "CREATE", "OR", "REPLACE", "RULE", MatchAny, "AS", "ON"),
	TCPAT(386, TailMatch, "AS", "ON", "SELECT|UPDATE|INSERT|DELETE"),
	TCPAT(387, TailMatch, "AS", "ON", "SELECT|UPDATE|INSERT|DELETE", "TO"),
	TCPAT(388, Match, "CREATE", "SCHEMA"),
	TCPAT(389, Match, "CREATE", "SCHEMA", "AUTHORIZATION"),
	TCPAT(389, Match, "CREATE", "SCHEMA", MatchAny, "AUTHORIZATION"),
	TCPAT(390, Match, "CREATE", "SCHEMA", "AUTHORIZATION", MatchAny),
	TCPAT(390, Match, "CREATE", "SCHEMA", MatchAny, "AUTHORIZATION", MatchAny),
	TCPAT(391, Match, "CREATE", "SCHEMA", MatchAny),
	TCPAT(392, TailMatch, "CREATE", "SEQUENCE", MatchAny),
	TCPAT(392, TailMatch, "CREATE", "TEMP|TEMPORARY", "SEQUENCE", MatchAny),
	TCPAT(393, TailMatch, "CREATE", "SEQUENCE", MatchAny, "AS"),
	TCPAT(393, TailMatch, "CREATE", "TEMP|TEMPORARY", "SEQUENCE", MatchAny, "AS"),
	TCPAT(394, TailMatch, "CREATE", "SEQUENCE", MatchAny, "NO"),
	TCPAT(394, TailMatch, "CREATE", "TEMP|TEMPORARY", "SEQUENCE", MatchAny, "NO"),
	TCPAT(395, Match, "CREATE", "SERVER", MatchAny),
	TCPAT(396, Match, "CREATE", "STATISTICS", MatchAny),
	TCPAT(397, Match, "CREATE", "STATISTICS", MatchAny, "("),
	TCPAT(398, Match, "CREATE", "STATISTICS", MatchAny, "(*)"),
	TCPAT(399, Match, "CREATE", "STATISTICS", MatchAny, MatchAnyN, "FROM"),
	TCPAT(400, TailMatch, "CREATE", "TEMP|TEMPORARY"),
	TCPAT(401, TailMatch, "CREATE", "UNLOGGED"),
	TCPAT(402, TailMatch, "PARTITION", "BY"),
	TCPAT(403, TailMatch, "PARTITION", "OF"),
	TCPAT(404, TailMatch, "PARTITION", "OF", MatchAny),
	TCPAT(405, TailMatch, "CREATE", "TABLE", MatchAny),
	TCPAT(405, TailMatch, "CREATE", "TEMP|TEMPORARY|UNLOGGED", "TABLE", MatchAny),
	TCPAT(406, TailMatch, "CREATE", "TABLE", MatchAny, "OF"),
	TCPAT(406, TailMatch, "CREATE", "TEMP|TEMPORARY|UNLOGGED", "TABLE", MatchAny, "OF"),
	TCPAT(407, TailMatch, "CREATE", "TABLE", MatchAny, "AS"),
	TCPAT(407, TailMatch, "CREATE", "TABLE", MatchAny, "(*)", "AS"),
	TCPAT(407, TailMatch, "CREATE", "TEMP|TEMPORARY|UNLOGGED", "TABLE", MatchAny, "AS"),
	TCPAT(407, TailMatch, "CREATE", "TEMP|TEMPORARY|UNLOGGED", "TABLE", MatchAny, "(*)", "AS"),
	TCPAT(408, TailMatch, "CREATE", "TABLE", MatchAny, "(*)"),
	TCPAT(409, TailMatch, "CREATE", "UNLOGGED", "TABLE", MatchAny, "(*)"),
	TCPAT(410, TailMatch, "CREATE", "TEMP|TEMPORARY", "TABLE", MatchAny, "(*)"),
	TCPAT(411, TailMatch, "CREATE", "TABLE", MatchAny, "(*)", "USING"),
	TCPAT(411, TailMatch, "CREATE", "TEMP|TEMPORARY|UNLOGGED", "TABLE", MatchAny, "(*)", "USING"),
	TCPAT(412, TailMatch, "CREATE", "TABLE", MatchAny, "(*)", "WITH", "("),
	TCPAT(412, TailMatch, "CREATE", "TEMP|TEMPORARY|UNLOGGED", "TABLE", MatchAny, "(*)", "WITH", "("),
	TCPAT(413, TailMatch, "CREATE", "TEMP|TEMPORARY", "TABLE", MatchAny, "(*)", "ON", "COMMIT"),
	TCPAT(414, Match, "CREATE", "TABLESPACE", MatchAny),
	TCPAT(415, Match, "CREATE", "TABLESPACE", MatchAny, "OWNER", MatchAny),
	TCPAT(416, TailMatch, "CREATE", "TEXT", "SEARCH"),
	TCPAT(417, TailMatch, "CREATE", "TEXT", "SEARCH", "CONFIGURATION|DICTIONARY|PARSER|TEMPLATE", MatchAny),
	TCPAT(418, Match, "CREATE", "TRANSFORM"),
	TCPAT(418, Match, "CREATE", "OR", "REPLACE", "TRANSFORM"),
	TCPAT(419, Match, "CREATE", "TRANSFORM", "FOR"),
	TCPAT(419, Match, "CREATE", "OR", "REPLACE", "TRANSFORM", "FOR"),
	TCPAT(420, Match, "CREATE", "TRANSFORM", "FOR", MatchAny),
	TCPAT(420, Match, "CREATE", "OR", "REPLACE", "TRANSFORM", "FOR", MatchAny),
	TCPAT(421, Match, "CREATE", "TRANSFORM", "FOR", MatchAny, "LANGUAGE"),
	TCPAT(421, Match, "CREATE", "OR", "REPLACE", "TRANSFORM", "FOR", MatchAny, "LANGUAGE"),
	TCPAT(422, Match, "CREATE", "SUBSCRIPTION", MatchAny),
	TCPAT(423, Match, "CREATE", "SUBSCRIPTION", MatchAny, "SERVER"),
	TCPAT(424, Match, "CREATE", "SUBSCRIPTION", MatchAny, "SERVER", MatchAny),
	TCPAT(425, Match, "CREATE", "SUBSCRIPTION", MatchAny, "CONNECTION", MatchAny),
	TCPAT(426, Match, "CREATE", "SUBSCRIPTION", MatchAny, "SERVER", MatchAny, "PUBLICATION"),
	TCPAT(427, Match, "CREATE", "SUBSCRIPTION", MatchAny, "CONNECTION", MatchAny, "PUBLICATION"),
	TCPAT(428, Match, "CREATE", "SUBSCRIPTION", MatchAnyN, "PUBLICATION", MatchAny),
	TCPAT(429, Match, "CREATE", "SUBSCRIPTION", MatchAnyN, "WITH", "("),
	TCPAT(430, TailMatch, "CREATE", "TRIGGER", MatchAny),
	TCPAT(430, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny),
	TCPAT(431, TailMatch, "CREATE", "TRIGGER", MatchAny, "BEFORE|AFTER"),
	TCPAT(431, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny, "BEFORE|AFTER"),
	TCPAT(432, TailMatch, "CREATE", "TRIGGER", MatchAny, "INSTEAD", "OF"),
	TCPAT(432, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny, "INSTEAD", "OF"),
	TCPAT(433, TailMatch, "CREATE", "TRIGGER", MatchAny, "BEFORE|AFTER", MatchAny),
	TCPAT(433, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny, "BEFORE|AFTER", MatchAny),
	TCPAT(433, TailMatch, "CREATE", "TRIGGER", MatchAny, "INSTEAD", "OF", MatchAny),
	TCPAT(433, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny, "INSTEAD", "OF", MatchAny),
	TCPAT(434, TailMatch, "CREATE", "TRIGGER", MatchAny, "BEFORE|AFTER", MatchAny, "ON"),
	TCPAT(434, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny, "BEFORE|AFTER", MatchAny, "ON"),
	TCPAT(435, TailMatch, "CREATE", "TRIGGER", MatchAny, "INSTEAD", "OF", MatchAny, "ON"),
	TCPAT(435, TailMatch, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAny, "INSTEAD", "OF", MatchAny, "ON"),
	TCPAT(436, Match, "CREATE", "TRIGGER", MatchAnyN, "ON", MatchAny),
	TCPAT(436, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "ON", MatchAny),
	TCPAT(437, Match, "CREATE", "TRIGGER", MatchAnyN, "DEFERRABLE"),
	TCPAT(437, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "DEFERRABLE"),
	TCPAT(437, Match, "CREATE", "TRIGGER", MatchAnyN, "INITIALLY", "IMMEDIATE|DEFERRED"),
	TCPAT(437, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "INITIALLY", "IMMEDIATE|DEFERRED"),
	TCPAT(438, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING"),
	TCPAT(438, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING"),
	TCPAT(439, Match, "CREATE", "TRIGGER", MatchAnyN, "OLD|NEW", "TABLE"),
	TCPAT(439, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "OLD|NEW", "TABLE"),
	TCPAT(440, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD", "TABLE", "AS", MatchAny),
	TCPAT(440, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD", "TABLE", "AS", MatchAny),
	TCPAT(440, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD", "TABLE", MatchAny),
	TCPAT(440, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD", "TABLE", MatchAny),
	TCPAT(441, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "NEW", "TABLE", "AS", MatchAny),
	TCPAT(441, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "NEW", "TABLE", "AS", MatchAny),
	TCPAT(441, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "NEW", "TABLE", MatchAny),
	TCPAT(441, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "NEW", "TABLE", MatchAny),
	TCPAT(442, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", "AS", MatchAny, "OLD|NEW", "TABLE", "AS", MatchAny),
	TCPAT(442, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", "AS", MatchAny, "OLD|NEW", "TABLE", "AS", MatchAny),
	TCPAT(442, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", MatchAny, "OLD|NEW", "TABLE", "AS", MatchAny),
	TCPAT(442, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", MatchAny, "OLD|NEW", "TABLE", "AS", MatchAny),
	TCPAT(442, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", "AS", MatchAny, "OLD|NEW", "TABLE", MatchAny),
	TCPAT(442, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", "AS", MatchAny, "OLD|NEW", "TABLE", MatchAny),
	TCPAT(442, Match, "CREATE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", MatchAny, "OLD|NEW", "TABLE", MatchAny),
	TCPAT(442, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "REFERENCING", "OLD|NEW", "TABLE", MatchAny, "OLD|NEW", "TABLE", MatchAny),
	TCPAT(443, Match, "CREATE", "TRIGGER", MatchAnyN, "FOR"),
	TCPAT(443, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "FOR"),
	TCPAT(444, Match, "CREATE", "TRIGGER", MatchAnyN, "FOR", "EACH"),
	TCPAT(444, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "FOR", "EACH"),
	TCPAT(445, Match, "CREATE", "TRIGGER", MatchAnyN, "FOR", "EACH", "ROW|STATEMENT"),
	TCPAT(445, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "FOR", "EACH", "ROW|STATEMENT"),
	TCPAT(445, Match, "CREATE", "TRIGGER", MatchAnyN, "FOR", "ROW|STATEMENT"),
	TCPAT(445, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "FOR", "ROW|STATEMENT"),
	TCPAT(446, Match, "CREATE", "TRIGGER", MatchAnyN, "WHEN", "(*)"),
	TCPAT(446, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "WHEN", "(*)"),
	TCPAT(447, Match, "CREATE", "TRIGGER", MatchAnyN, "EXECUTE"),
	TCPAT(447, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "EXECUTE"),
	TCPAT(448, Match, "CREATE", "TRIGGER", MatchAnyN, "EXECUTE", "FUNCTION|PROCEDURE"),
	TCPAT(448, Match, "CREATE", "OR", "REPLACE", "TRIGGER", MatchAnyN, "EXECUTE", "FUNCTION|PROCEDURE"),
	TCPAT(449, Match, "CREATE", "ROLE|GROUP|USER", MatchAny),
	TCPAT(450, Match, "CREATE", "ROLE|GROUP|USER", MatchAny, "WITH"),
	TCPAT(451, Match, "CREATE", "ROLE|USER|GROUP", MatchAny, "IN"),
	TCPAT(452, Match, "CREATE", "TYPE", MatchAny),
	TCPAT(453, Match, "CREATE", "TYPE", MatchAny, "AS"),
	TCPAT(454, HeadMatch, "CREATE", "TYPE", MatchAny, "AS", "("),
	TCPAT(455, Match, "CREATE", "TYPE", MatchAny, "AS", "ENUM|RANGE"),
	TCPAT(456, HeadMatch, "CREATE", "TYPE", MatchAny, "("),
	TCPAT(457, HeadMatch, "CREATE", "TYPE", MatchAny, "AS", "RANGE", "("),
	TCPAT(458, TailMatch, "CREATE", "VIEW", MatchAny),
	TCPAT(458, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny),
	TCPAT(459, TailMatch, "CREATE", "VIEW", MatchAny, "AS"),
	TCPAT(459, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "AS"),
	TCPAT(460, TailMatch, "CREATE", "VIEW", MatchAny, "WITH"),
	TCPAT(460, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "WITH"),
	TCPAT(461, TailMatch, "CREATE", "VIEW", MatchAny, "WITH", "("),
	TCPAT(461, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "WITH", "("),
	TCPAT(462, TailMatch, "CREATE", "VIEW", MatchAny, "WITH", "(", "check_option"),
	TCPAT(462, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "WITH", "(", "check_option"),
	TCPAT(463, TailMatch, "CREATE", "VIEW", MatchAny, "WITH", "(", "check_option", "="),
	TCPAT(463, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "WITH", "(", "check_option", "="),
	TCPAT(464, TailMatch, "CREATE", "VIEW", MatchAny, "WITH", "(*)"),
	TCPAT(464, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "WITH", "(*)"),
	TCPAT(465, TailMatch, "CREATE", "VIEW", MatchAny, "WITH", "(*)", "AS"),
	TCPAT(465, TailMatch, "CREATE", "OR", "REPLACE", "VIEW", MatchAny, "WITH", "(*)", "AS"),
	TCPAT(466, Match, "CREATE", "MATERIALIZED"),
	TCPAT(467, Match, "CREATE", "MATERIALIZED", "VIEW", MatchAny),
	TCPAT(468, Match, "CREATE", "MATERIALIZED", "VIEW", MatchAny, "USING"),
	TCPAT(469, Match, "CREATE", "MATERIALIZED", "VIEW", MatchAny, "USING", MatchAny),
	TCPAT(470, Match, "CREATE", "MATERIALIZED", "VIEW", MatchAny, "AS"),
	TCPAT(470, Match, "CREATE", "MATERIALIZED", "VIEW", MatchAny, "USING", MatchAny, "AS"),
	TCPAT(471, Match, "CREATE", "EVENT"),
	TCPAT(472, Match, "CREATE", "EVENT", "TRIGGER", MatchAny),
	TCPAT(473, Match, "CREATE", "EVENT", "TRIGGER", MatchAny, "ON"),
	TCPAT(474, Match, "CREATE", "EVENT", "TRIGGER", MatchAny, "ON", MatchAny),
	TCPAT(475, Match, "CREATE", "EVENT", "TRIGGER", MatchAnyN, "WHEN|AND", MatchAny, "IN", "(*)"),
	TCPAT(476, Match, "CREATE", "EVENT", "TRIGGER", MatchAnyN, "EXECUTE", "FUNCTION|PROCEDURE"),
	TCPAT(477, Match, "DEALLOCATE"),
	TCPAT(478, Match, "DECLARE", MatchAny),
	TCPAT(479, Match, "DECLARE", MatchAnyN, "BINARY"),
	TCPAT(480, Match, "DECLARE", MatchAnyN, "ASENSITIVE|INSENSITIVE"),
	TCPAT(481, Match, "DECLARE", MatchAnyN, "SCROLL"),
	TCPAT(482, Match, "DECLARE", MatchAnyN, "NO"),
	TCPAT(483, Match, "DECLARE", MatchAnyN, "CURSOR"),
	TCPAT(484, Match, "DECLARE", MatchAnyN, "CURSOR", "WITH|WITHOUT"),
	TCPAT(485, Match, "DECLARE", MatchAnyN, "CURSOR", "WITH|WITHOUT", "HOLD"),
	TCPAT(486, Match, "DELETE"),
	TCPAT(487, TailMatch, "DELETE", "FROM"),
	TCPAT(488, TailMatch, "DELETE", "FROM", MatchAny),
	TCPAT(489, TailMatch, "DELETE", "FROM", MatchAny, "FOR"),
	TCPAT(490, TailMatch, "DELETE", "FROM", MatchAny, "FOR", "PORTION"),
	TCPAT(491, TailMatch, "DELETE", "FROM", MatchAny, "FOR", "PORTION", "OF"),
	TCPAT(492, TailMatch, "DELETE", "FROM", MatchAny, "FOR", "PORTION", "OF", MatchAny),
	TCPAT(493, TailMatch, "DELETE", "FROM", MatchAny, "USING"),
	TCPAT(494, Match, "DISCARD"),
	TCPAT(495, Match, "DO"),
	TCPAT(496, Match, "DROP", "COLLATION|CONVERSION|DOMAIN|EXTENSION|LANGUAGE|PUBLICATION|SCHEMA|SEQUENCE|SERVER|SUBSCRIPTION|STATISTICS|TABLE|TYPE|VIEW", MatchAny),
	TCPAT(496, Match, "DROP", "ACCESS", "METHOD", MatchAny),
	TCPAT(496, Match, "DROP", "EVENT", "TRIGGER", MatchAny),
	TCPAT(496, Match, "DROP", "FOREIGN", "DATA", "WRAPPER", MatchAny),
	TCPAT(496, Match, "DROP", "FOREIGN", "TABLE", MatchAny),
	TCPAT(496, Match, "DROP", "TEXT", "SEARCH", "CONFIGURATION|DICTIONARY|PARSER|TEMPLATE", MatchAny),
	TCPAT(497, Match, "DROP", "AGGREGATE|FUNCTION|PROCEDURE|ROUTINE", MatchAny, MatchAny),
	TCPAT(498, Match, "DROP", "AGGREGATE|FUNCTION|PROCEDURE|ROUTINE", MatchAny),
	TCPAT(499, Match, "DROP", "AGGREGATE|FUNCTION|PROCEDURE|ROUTINE", MatchAny, "("),
	TCPAT(500, Match, "DROP", "FOREIGN"),
	TCPAT(501, Match, "DROP", "DATABASE", MatchAny),
	TCPAT(502, HeadMatch, "DROP", "DATABASE"),
	TCPAT(503, Match, "DROP", "INDEX"),
	TCPAT(504, Match, "DROP", "INDEX", "CONCURRENTLY"),
	TCPAT(505, Match, "DROP", "INDEX", MatchAny),
	TCPAT(506, Match, "DROP", "INDEX", "CONCURRENTLY", MatchAny),
	TCPAT(507, Match, "DROP", "MATERIALIZED"),
	TCPAT(508, Match, "DROP", "MATERIALIZED", "VIEW"),
	TCPAT(509, Match, "DROP", "MATERIALIZED", "VIEW", MatchAny),
	TCPAT(510, Match, "DROP", "OWNED"),
	TCPAT(511, Match, "DROP", "OWNED", "BY"),
	TCPAT(512, Match, "DROP", "OWNED", "BY", MatchAny),
	TCPAT(513, Match, "DROP", "TEXT", "SEARCH"),
	TCPAT(514, Match, "DROP", "TRIGGER", MatchAny),
	TCPAT(515, Match, "DROP", "TRIGGER", MatchAny, "ON"),
	TCPAT(516, Match, "DROP", "TRIGGER", MatchAny, "ON", MatchAny),
	TCPAT(517, Match, "DROP", "ACCESS"),
	TCPAT(518, Match, "DROP", "ACCESS", "METHOD"),
	TCPAT(519, Match, "DROP", "EVENT"),
	TCPAT(520, Match, "DROP", "EVENT", "TRIGGER"),
	TCPAT(521, Match, "DROP", "POLICY"),
	TCPAT(522, Match, "DROP", "POLICY", MatchAny),
	TCPAT(523, Match, "DROP", "POLICY", MatchAny, "ON"),
	TCPAT(524, Match, "DROP", "POLICY", MatchAny, "ON", MatchAny),
	TCPAT(525, Match, "DROP", "PROPERTY"),
	TCPAT(526, Match, "DROP", "PROPERTY", "GRAPH"),
	TCPAT(527, Match, "DROP", "PROPERTY", "GRAPH", MatchAny),
	TCPAT(528, Match, "DROP", "RULE", MatchAny),
	TCPAT(529, Match, "DROP", "RULE", MatchAny, "ON"),
	TCPAT(530, Match, "DROP", "RULE", MatchAny, "ON", MatchAny),
	TCPAT(531, Match, "DROP", "TRANSFORM"),
	TCPAT(532, Match, "DROP", "TRANSFORM", "FOR"),
	TCPAT(533, Match, "DROP", "TRANSFORM", "FOR", MatchAny),
	TCPAT(534, Match, "DROP", "TRANSFORM", "FOR", MatchAny, "LANGUAGE"),
	TCPAT(535, Match, "DROP", "TRANSFORM", "FOR", MatchAny, "LANGUAGE", MatchAny),
	TCPAT(536, Match, "EXECUTE"),
	TCPAT(537, Match, "EXPLAIN"),
	TCPAT(538, HeadMatch, "EXPLAIN", "(*"),
	TCPAT(539, Match, "EXPLAIN", "ANALYZE"),
	TCPAT(540, Match, "EXPLAIN", "(*)"),
	TCPAT(540, Match, "EXPLAIN", "VERBOSE"),
	TCPAT(540, Match, "EXPLAIN", "ANALYZE", "VERBOSE"),
	TCPAT(541, Match, "FETCH|MOVE"),
	TCPAT(542, Match, "FETCH|MOVE", "BACKWARD|FORWARD"),
	TCPAT(543, Match, "FETCH|MOVE", "ABSOLUTE|BACKWARD|FORWARD|RELATIVE", MatchAnyExcept("FROM|IN")),
	TCPAT(543, Match, "FETCH|MOVE", "ALL|NEXT|PRIOR|FIRST|LAST"),
	TCPAT(544, Match, "FETCH|MOVE", MatchAnyN, "FROM|IN"),
	TCPAT(545, TailMatch, "FOREIGN", "DATA", "WRAPPER"),
	TCPAT(546, Match, "CREATE", "SERVER", MatchAnyN, "FOREIGN", "DATA", "WRAPPER", MatchAny),
	TCPAT(547, TailMatch, "FOREIGN", "TABLE"),
	TCPAT(548, TailMatch, "FOREIGN", "SERVER"),
	TCPAT(549, TailMatch, "GRANT|REVOKE"),
	TCPAT(549, TailMatch, "REVOKE", "ADMIN|GRANT|INHERIT|SET", "OPTION", "FOR"),
	TCPAT(550, TailMatch, "GRANT|REVOKE", "ALTER"),
	TCPAT(550, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", "ALTER"),
	TCPAT(551, TailMatch, "REVOKE", "SET"),
	TCPAT(552, TailMatch, "GRANT", "SET"),
	TCPAT(552, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", "SET"),
	TCPAT(552, TailMatch, "GRANT|REVOKE", "ALTER", "SYSTEM"),
	TCPAT(552, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", "ALTER", "SYSTEM"),
	TCPAT(553, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "PARAMETER"),
	TCPAT(553, TailMatch, "GRANT|REVOKE", MatchAny, MatchAny, "ON", "PARAMETER"),
	TCPAT(553, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "PARAMETER"),
	TCPAT(553, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, MatchAny, "ON", "PARAMETER"),
	TCPAT(554, TailMatch, "GRANT", MatchAny, "ON", "PARAMETER", MatchAny),
	TCPAT(554, TailMatch, "GRANT", MatchAny, MatchAny, "ON", "PARAMETER", MatchAny),
	TCPAT(555, TailMatch, "REVOKE", MatchAny, "ON", "PARAMETER", MatchAny),
	TCPAT(555, TailMatch, "REVOKE", MatchAny, MatchAny, "ON", "PARAMETER", MatchAny),
	TCPAT(555, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "PARAMETER", MatchAny),
	TCPAT(555, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, MatchAny, "ON", "PARAMETER", MatchAny),
	TCPAT(556, TailMatch, "GRANT|REVOKE", MatchAny),
	TCPAT(556, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny),
	TCPAT(557, TailMatch, "GRANT|REVOKE", MatchAny, "ON"),
	TCPAT(557, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON"),
	TCPAT(558, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "ALL"),
	TCPAT(558, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "ALL"),
	TCPAT(559, TailMatch, "GRANT|REVOKE", MatchAny, "ON", MatchAny),
	TCPAT(559, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", MatchAny),
	TCPAT(560, Match, "GRANT", MatchAnyN, "TO"),
	TCPAT(560, Match, "REVOKE", MatchAnyN, "FROM"),
	TCPAT(561, Match, "GRANT", MatchAnyN, "TO", MatchAny),
	TCPAT(562, Match, "GRANT", MatchAnyN, "TO", MatchAny, "WITH"),
	TCPAT(563, Match, "GRANT", MatchAnyN, "TO", MatchAny, "WITH", "ADMIN|INHERIT|SET"),
	TCPAT(564, Match, "GRANT", MatchAnyN, "TO", MatchAny, "WITH", MatchAny, "OPTION"),
	TCPAT(565, Match, "GRANT", MatchAnyN, "TO", MatchAny, "WITH", MatchAny, "OPTION", "GRANTED", "BY"),
	TCPAT(566, Match, "ALTER", "DEFAULT", "PRIVILEGES", MatchAnyN, "TO|FROM"),
	TCPAT(567, Match, "ALTER", "DEFAULT", "PRIVILEGES", MatchAnyN, "TO", MatchAny),
	TCPAT(568, Match, "GRANT|REVOKE", MatchAnyN, "ON", MatchAny, MatchAny),
	TCPAT(569, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "ALL", MatchAny, "IN", "SCHEMA", MatchAny),
	TCPAT(569, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "ALL", MatchAny, "IN", "SCHEMA", MatchAny),
	TCPAT(570, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "FOREIGN", "DATA", "WRAPPER", MatchAny),
	TCPAT(570, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "FOREIGN", "DATA", "WRAPPER", MatchAny),
	TCPAT(571, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "FOREIGN", "SERVER", MatchAny),
	TCPAT(571, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "FOREIGN", "SERVER", MatchAny),
	TCPAT(572, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "LARGE", "OBJECT", MatchAny),
	TCPAT(572, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "LARGE", "OBJECT", MatchAny),
	TCPAT(573, TailMatch, "GRANT|REVOKE", MatchAny, "ON", "LARGE", "OBJECTS"),
	TCPAT(573, TailMatch, "REVOKE", "GRANT", "OPTION", "FOR", MatchAny, "ON", "LARGE", "OBJECTS"),
	TCPAT(574, TailMatch, "GRAPH_TABLE"),
	TCPAT(575, TailMatch, "GRAPH_TABLE", "("),
	TCPAT(576, TailMatch, "GRAPH_TABLE", "(", MatchAny),
	TCPAT(577, TailMatch, "FROM", MatchAny, "GROUP"),
	TCPAT(578, Match, "IMPORT"),
	TCPAT(579, Match, "IMPORT", "FOREIGN"),
	TCPAT(580, Match, "IMPORT", "FOREIGN", "SCHEMA", MatchAny),
	TCPAT(581, TailMatch, "LIMIT", "TO", "(*)"),
	TCPAT(581, Match, "IMPORT", "FOREIGN", "SCHEMA", MatchAny, "EXCEPT", "(*)"),
	TCPAT(582, TailMatch, "FROM", "SERVER", MatchAny),
	TCPAT(583, TailMatch, "FROM", "SERVER", MatchAny, "INTO"),
	TCPAT(584, TailMatch, "FROM", "SERVER", MatchAny, "INTO", MatchAny),
	TCPAT(585, TailMatch, "NOT", "MATCHED", "THEN", "INSERT"),
	TCPAT(586, TailMatch, "INSERT"),
	TCPAT(587, TailMatch, "INSERT", "INTO"),
	TCPAT(588, TailMatch, "INSERT", "INTO", MatchAny, "("),
	TCPAT(589, TailMatch, "INSERT", "INTO", MatchAny),
	TCPAT(590, TailMatch, "INSERT", "INTO", MatchAny, MatchAny),
	TCPAT(591, TailMatch, "OVERRIDING"),
	TCPAT(592, TailMatch, "OVERRIDING", MatchAny, "VALUE"),
	TCPAT(593, TailMatch, "VALUES"),
	TCPAT(594, Match, "LOCK"),
	TCPAT(595, Match, "LOCK", "TABLE"),
	TCPAT(596, Match, "LOCK", "TABLE", "ONLY"),
	TCPAT(596, Match, "LOCK", "ONLY"),
	TCPAT(597, Match, "LOCK", MatchAnyExcept("TABLE|ONLY")),
	TCPAT(597, Match, "LOCK", "TABLE", MatchAnyExcept("ONLY")),
	TCPAT(597, Match, "LOCK", "ONLY", MatchAny),
	TCPAT(597, Match, "LOCK", "TABLE", "ONLY", MatchAny),
	TCPAT(598, Match, "LOCK", MatchAnyN, "IN"),
	TCPAT(599, Match, "LOCK", MatchAnyN, "IN", "ACCESS|ROW"),
	TCPAT(600, Match, "LOCK", MatchAnyN, "IN", "SHARE"),
	TCPAT(601, Match, "LOCK", MatchAnyN, "MODE"),
	TCPAT(602, TailMatch, "MERGE"),
	TCPAT(603, TailMatch, "MERGE", "INTO"),
	TCPAT(604, TailMatch, "MERGE", "INTO", MatchAny),
	TCPAT(605, TailMatch, "MERGE", "INTO", MatchAny, "AS", MatchAny),
	TCPAT(605, TailMatch, "MERGE", "INTO", MatchAny, MatchAnyExcept("USING|AS")),
	TCPAT(606, TailMatch, "MERGE", "INTO", MatchAny, "USING"),
	TCPAT(606, TailMatch, "MERGE", "INTO", MatchAny, "AS", MatchAny, "USING"),
	TCPAT(606, TailMatch, "MERGE", "INTO", MatchAny, MatchAny, "USING"),
	TCPAT(607, TailMatch, "MERGE", "INTO", MatchAny, "USING", MatchAny),
	TCPAT(607, TailMatch, "MERGE", "INTO", MatchAny, "AS", MatchAny, "USING", MatchAny),
	TCPAT(607, TailMatch, "MERGE", "INTO", MatchAny, MatchAny, "USING", MatchAny),
	TCPAT(608, TailMatch, "MERGE", "INTO", MatchAny, "USING", MatchAny, "AS", MatchAny),
	TCPAT(608, TailMatch, "MERGE", "INTO", MatchAny, "AS", MatchAny, "USING", MatchAny, "AS", MatchAny),
	TCPAT(608, TailMatch, "MERGE", "INTO", MatchAny, MatchAny, "USING", MatchAny, "AS", MatchAny),
	TCPAT(608, TailMatch, "MERGE", "INTO", MatchAny, "USING", MatchAny, MatchAnyExcept("ON|AS")),
	TCPAT(608, TailMatch, "MERGE", "INTO", MatchAny, "AS", MatchAny, "USING", MatchAny, MatchAnyExcept("ON|AS")),
	TCPAT(608, TailMatch, "MERGE", "INTO", MatchAny, MatchAny, "USING", MatchAny, MatchAnyExcept("ON|AS")),
	TCPAT(609, TailMatch, "INTO", MatchAny, "USING", MatchAny, "ON"),
	TCPAT(610, TailMatch, "INTO", MatchAny, "AS", MatchAny, "USING", MatchAny, "AS", MatchAny, "ON"),
	TCPAT(611, TailMatch, "INTO", MatchAny, MatchAny, "USING", MatchAny, MatchAny, "ON"),
	TCPAT(612, TailMatch, "USING", MatchAny, "ON", MatchAny),
	TCPAT(612, TailMatch, "USING", MatchAny, "AS", MatchAny, "ON", MatchAny),
	TCPAT(612, TailMatch, "USING", MatchAny, MatchAny, "ON", MatchAny),
	TCPAT(612, TailMatch, "USING", MatchAny, "ON", MatchAny, MatchAnyExcept("WHEN"), MatchAnyExcept("WHEN")),
	TCPAT(612, TailMatch, "USING", MatchAny, "AS", MatchAny, "ON", MatchAny, MatchAnyExcept("WHEN"), MatchAnyExcept("WHEN")),
	TCPAT(612, TailMatch, "USING", MatchAny, MatchAny, "ON", MatchAny, MatchAnyExcept("WHEN"), MatchAnyExcept("WHEN")),
	TCPAT(613, TailMatch, "USING", MatchAny, "ON", MatchAny, "WHEN"),
	TCPAT(613, TailMatch, "USING", MatchAny, "AS", MatchAny, "ON", MatchAny, "WHEN"),
	TCPAT(613, TailMatch, "USING", MatchAny, MatchAny, "ON", MatchAny, "WHEN"),
	TCPAT(613, TailMatch, "USING", MatchAny, "ON", MatchAny, MatchAny, MatchAny, "WHEN"),
	TCPAT(613, TailMatch, "USING", MatchAny, "AS", MatchAny, "ON", MatchAny, MatchAny, MatchAny, "WHEN"),
	TCPAT(613, TailMatch, "USING", MatchAny, MatchAny, "ON", MatchAny, MatchAny, MatchAny, "WHEN"),
	TCPAT(614, TailMatch, "WHEN", "MATCHED"),
	TCPAT(614, TailMatch, "WHEN", "NOT", "MATCHED", "BY", "SOURCE|TARGET"),
	TCPAT(615, TailMatch, "WHEN", "NOT", "MATCHED"),
	TCPAT(616, TailMatch, "WHEN", "NOT", "MATCHED", "BY"),
	TCPAT(617, TailMatch, "WHEN", "MATCHED", "THEN"),
	TCPAT(617, TailMatch, "WHEN", "NOT", "MATCHED", "BY", "SOURCE", "THEN"),
	TCPAT(618, TailMatch, "WHEN", "NOT", "MATCHED", "THEN"),
	TCPAT(618, TailMatch, "WHEN", "NOT", "MATCHED", "BY", "TARGET", "THEN"),
	TCPAT(619, TailMatch, "NOTIFY"),
	TCPAT(620, TailMatch, "OPTIONS"),
	TCPAT(621, TailMatch, "OWNER", "TO"),
	TCPAT(622, TailMatch, "FROM", MatchAny, "ORDER"),
	TCPAT(623, TailMatch, "FROM", MatchAny, "ORDER", "BY"),
	TCPAT(624, Match, "PREPARE", MatchAny, "AS"),
	TCPAT(625, Match, "REASSIGN"),
	TCPAT(626, Match, "REASSIGN", "OWNED"),
	TCPAT(627, Match, "REASSIGN", "OWNED", "BY"),
	TCPAT(628, Match, "REASSIGN", "OWNED", "BY", MatchAny),
	TCPAT(629, Match, "REASSIGN", "OWNED", "BY", MatchAny, "TO"),
	TCPAT(630, Match, "REFRESH"),
	TCPAT(631, Match, "REFRESH", "MATERIALIZED"),
	TCPAT(632, Match, "REFRESH", "MATERIALIZED", "VIEW"),
	TCPAT(633, Match, "REFRESH", "MATERIALIZED", "VIEW", "CONCURRENTLY"),
	TCPAT(634, Match, "REFRESH", "MATERIALIZED", "VIEW", MatchAny),
	TCPAT(635, Match, "REFRESH", "MATERIALIZED", "VIEW", "CONCURRENTLY", MatchAny),
	TCPAT(636, Match, "REFRESH", "MATERIALIZED", "VIEW", MatchAny, "WITH"),
	TCPAT(637, Match, "REFRESH", "MATERIALIZED", "VIEW", "CONCURRENTLY", MatchAny, "WITH"),
	TCPAT(638, Match, "REFRESH", "MATERIALIZED", "VIEW", MatchAny, "WITH", "NO"),
	TCPAT(639, Match, "REFRESH", "MATERIALIZED", "VIEW", "CONCURRENTLY", MatchAny, "WITH", "NO"),
	TCPAT(640, Match, "REINDEX"),
	TCPAT(640, Match, "REINDEX", "(*)"),
	TCPAT(641, Match, "REINDEX", "TABLE"),
	TCPAT(641, Match, "REINDEX", "(*)", "TABLE"),
	TCPAT(642, Match, "REINDEX", "INDEX"),
	TCPAT(642, Match, "REINDEX", "(*)", "INDEX"),
	TCPAT(643, Match, "REINDEX", "SCHEMA"),
	TCPAT(643, Match, "REINDEX", "(*)", "SCHEMA"),
	TCPAT(644, Match, "REINDEX", "SYSTEM|DATABASE"),
	TCPAT(644, Match, "REINDEX", "(*)", "SYSTEM|DATABASE"),
	TCPAT(645, Match, "REINDEX", "TABLE", "CONCURRENTLY"),
	TCPAT(645, Match, "REINDEX", "(*)", "TABLE", "CONCURRENTLY"),
	TCPAT(646, Match, "REINDEX", "INDEX", "CONCURRENTLY"),
	TCPAT(646, Match, "REINDEX", "(*)", "INDEX", "CONCURRENTLY"),
	TCPAT(647, Match, "REINDEX", "SCHEMA", "CONCURRENTLY"),
	TCPAT(647, Match, "REINDEX", "(*)", "SCHEMA", "CONCURRENTLY"),
	TCPAT(648, Match, "REINDEX", "SYSTEM|DATABASE", "CONCURRENTLY"),
	TCPAT(648, Match, "REINDEX", "(*)", "SYSTEM|DATABASE", "CONCURRENTLY"),
	TCPAT(649, HeadMatch, "REINDEX", "(*"),
	TCPAT(650, Match, "REPACK"),
	TCPAT(651, Match, "REPACK", "(*)"),
	TCPAT(652, Match, "REPACK", MatchAnyExcept("(")),
	TCPAT(653, Match, "REPACK", "(*)", MatchAnyExcept("(")),
	TCPAT(654, Match, "REPACK", MatchAny, "USING", "INDEX"),
	TCPAT(654, Match, "REPACK", "(*)", MatchAny, "USING", "INDEX"),
	TCPAT(655, TailMatch, MatchAny, "USING", "INDEX"),
	TCPAT(656, HeadMatch, "REPACK", "(*"),
	TCPAT(657, Match, "SECURITY"),
	TCPAT(658, Match, "SECURITY", "LABEL"),
	TCPAT(659, Match, "SECURITY", "LABEL", "FOR", MatchAny),
	TCPAT(660, Match, "SECURITY", "LABEL", "ON"),
	TCPAT(660, Match, "SECURITY", "LABEL", "FOR", MatchAny, "ON"),
	TCPAT(661, Match, "SECURITY", "LABEL", "ON", MatchAny, MatchAny),
	TCPAT(662, TailMatch, "SET|RESET"),
	TCPAT(663, Match, "SHOW"),
	TCPAT(664, Match, "SHOW", "SESSION"),
	TCPAT(665, Match, "SET", "TRANSACTION"),
	TCPAT(666, Match, "BEGIN|START", "TRANSACTION"),
	TCPAT(666, Match, "BEGIN", "WORK"),
	TCPAT(666, Match, "BEGIN"),
	TCPAT(666, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION"),
	TCPAT(667, Match, "SET|BEGIN|START", "TRANSACTION|WORK", "NOT"),
	TCPAT(667, Match, "BEGIN", "NOT"),
	TCPAT(667, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION", "NOT"),
	TCPAT(668, Match, "SET|BEGIN|START", "TRANSACTION|WORK", "ISOLATION"),
	TCPAT(668, Match, "BEGIN", "ISOLATION"),
	TCPAT(668, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION", "ISOLATION"),
	TCPAT(669, Match, "SET|BEGIN|START", "TRANSACTION|WORK", "ISOLATION", "LEVEL"),
	TCPAT(669, Match, "BEGIN", "ISOLATION", "LEVEL"),
	TCPAT(669, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION", "ISOLATION", "LEVEL"),
	TCPAT(670, Match, "SET|BEGIN|START", "TRANSACTION|WORK", "ISOLATION", "LEVEL", "READ"),
	TCPAT(670, Match, "BEGIN", "ISOLATION", "LEVEL", "READ"),
	TCPAT(670, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION", "ISOLATION", "LEVEL", "READ"),
	TCPAT(671, Match, "SET|BEGIN|START", "TRANSACTION|WORK", "ISOLATION", "LEVEL", "REPEATABLE"),
	TCPAT(671, Match, "BEGIN", "ISOLATION", "LEVEL", "REPEATABLE"),
	TCPAT(671, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION", "ISOLATION", "LEVEL", "REPEATABLE"),
	TCPAT(672, Match, "SET|BEGIN|START", "TRANSACTION|WORK", "READ"),
	TCPAT(672, Match, "BEGIN", "READ"),
	TCPAT(672, Match, "SET", "SESSION", "CHARACTERISTICS", "AS", "TRANSACTION", "READ"),
	TCPAT(673, Match, "SET", "CONSTRAINTS"),
	TCPAT(674, Match, "SET", "CONSTRAINTS", MatchAny),
	TCPAT(675, Match, "SET", "ROLE"),
	TCPAT(676, Match, "SET", "SESSION"),
	TCPAT(677, Match, "SET", "SESSION", "AUTHORIZATION"),
	TCPAT(678, Match, "RESET", "SESSION"),
	TCPAT(679, Match, "SET", MatchAny),
	TCPAT(680, Match, "ALTER", "DATABASE|FUNCTION|PROCEDURE|ROLE|ROUTINE|USER", MatchAnyN, "SET", MatchAnyExcept("SCHEMA")),
	TCPAT(681, TailMatch, "SET", MatchAny, "TO|="),
	TCPAT(682, Match, "START"),
	TCPAT(683, Match, "TABLE"),
	TCPAT(684, TailMatch, "TABLESAMPLE"),
	TCPAT(685, TailMatch, "TABLESAMPLE", MatchAny),
	TCPAT(686, Match, "TRUNCATE"),
	TCPAT(687, Match, "TRUNCATE", "TABLE"),
	TCPAT(688, Match, "TRUNCATE", MatchAnyN, "ONLY"),
	TCPAT(689, Match, "TRUNCATE", MatchAny),
	TCPAT(689, Match, "TRUNCATE", "TABLE|ONLY", MatchAny),
	TCPAT(689, Match, "TRUNCATE", "TABLE", "ONLY", MatchAny),
	TCPAT(690, Match, "TRUNCATE", MatchAnyN, "IDENTITY"),
	TCPAT(691, Match, "UNLISTEN"),
	TCPAT(692, TailMatch, "UPDATE"),
	TCPAT(693, TailMatch, "UPDATE", MatchAny),
	TCPAT(694, TailMatch, "UPDATE", MatchAny, "FOR"),
	TCPAT(695, TailMatch, "UPDATE", MatchAny, "FOR", "PORTION"),
	TCPAT(696, TailMatch, "UPDATE", MatchAny, "FOR", "PORTION", "OF"),
	TCPAT(697, TailMatch, "UPDATE", MatchAny, "FOR", "PORTION", "OF", MatchAny),
	TCPAT(698, TailMatch, "UPDATE", MatchAny, "SET"),
	TCPAT(699, TailMatch, "UPDATE", MatchAny, "SET", MatchAnyExcept("*=")),
	TCPAT(700, Match, "ALTER|CREATE|DROP", "USER", "MAPPING"),
	TCPAT(701, Match, "CREATE", "USER", "MAPPING", "FOR"),
	TCPAT(702, Match, "ALTER|DROP", "USER", "MAPPING", "FOR"),
	TCPAT(703, Match, "CREATE|ALTER|DROP", "USER", "MAPPING", "FOR", MatchAny),
	TCPAT(704, Match, "CREATE|ALTER", "USER", "MAPPING", "FOR", MatchAny, "SERVER", MatchAny),
	TCPAT(705, Match, "VACUUM"),
	TCPAT(706, HeadMatch, "VACUUM", "(*"),
	TCPAT(707, Match, "VACUUM", "(*)"),
	TCPAT(708, Match, "VACUUM", "FULL"),
	TCPAT(709, Match, "VACUUM", MatchAnyN, "FREEZE"),
	TCPAT(710, Match, "VACUUM", MatchAnyN, "VERBOSE"),
	TCPAT(711, Match, "VACUUM", MatchAnyN, "ANALYZE"),
	TCPAT(712, Match, "VACUUM", MatchAnyN, "("),
	TCPAT(713, HeadMatch, "VACUUM"),
	TCPAT(714, Match, "WAIT"),
	TCPAT(715, Match, "WAIT", "FOR"),
	TCPAT(716, Match, "WAIT", "FOR", "LSN"),
	TCPAT(717, Match, "WAIT", "FOR", "LSN", MatchAny),
	TCPAT(718, Match, "WAIT", "FOR", "LSN", MatchAny, "WITH"),
	TCPAT(719, HeadMatch, "WAIT", "FOR", "LSN", MatchAny, "WITH", "(*"),
	TCPAT(720, Match, "WITH"),
	TCPAT(721, TailMatch, MatchAny, "WHERE"),
	TCPAT(722, TailMatch, "FROM"),
	TCPAT(723, TailMatch, "JOIN"),
	TCPAT(724, TailMatch, "JOIN", MatchAny),
	TCPAT(725, TailMatch, "JOIN", MatchAny, MatchAny),
	TCPAT(726, TailMatch, "JOIN", "LATERAL", MatchAny, MatchAny),
	TCPAT(727, TailMatch, "JOIN", MatchAny, "USING"),
	TCPAT(727, TailMatch, "JOIN", MatchAny, MatchAny, "USING"),
	TCPAT(727, TailMatch, "JOIN", "LATERAL", MatchAny, MatchAny, "USING"),
	TCPAT(728, TailMatch, "JOIN", MatchAny, "USING", "("),
	TCPAT(729, TailMatch, "JOIN", MatchAny, MatchAny, "USING", "("),
	TCPAT(730, TailMatch, "AT"),
	TCPAT(731, TailMatch, "AT", "TIME", "ZONE"),
	TCPAT(732, TailMatchCS, "\\?"),
	TCPAT(733, TailMatchCS, "\\connect|\\c"),
	TCPAT(734, TailMatchCS, "\\connect|\\c", MatchAny),
	TCPAT(735, TailMatchCS, "\\da*"),
	TCPAT(736, TailMatchCS, "\\dAc*", MatchAny),
	TCPAT(736, TailMatchCS, "\\dAf*", MatchAny),
	TCPAT(737, TailMatchCS, "\\dAo*", MatchAny),
	TCPAT(737, TailMatchCS, "\\dAp*", MatchAny),
	TCPAT(738, TailMatchCS, "\\dA*"),
	TCPAT(739, TailMatchCS, "\\db*"),
	TCPAT(740, TailMatchCS, "\\dconfig*"),
	TCPAT(741, TailMatchCS, "\\dD*"),
	TCPAT(742, TailMatchCS, "\\des*"),
	TCPAT(743, TailMatchCS, "\\deu*"),
	TCPAT(744, TailMatchCS, "\\dew*"),
	TCPAT(745, TailMatchCS, "\\df*"),
	TCPAT(746, HeadMatchCS, "\\df*"),
	TCPAT(747, TailMatchCS, "\\dFd*"),
	TCPAT(748, TailMatchCS, "\\dFp*"),
	TCPAT(749, TailMatchCS, "\\dFt*"),
	TCPAT(750, TailMatchCS, "\\dF*"),
	TCPAT(751, TailMatchCS, "\\di*"),
	TCPAT(752, TailMatchCS, "\\dL*"),
	TCPAT(753, TailMatchCS, "\\dn*"),
	TCPAT(754, HeadMatchCS, "\\do*", MatchAny),
	TCPAT(755, TailMatchCS, "\\dp"),
	TCPAT(755, TailMatchCS, "\\z"),
	TCPAT(756, TailMatchCS, "\\dPi*"),
	TCPAT(757, TailMatchCS, "\\dPt*"),
	TCPAT(758, TailMatchCS, "\\dP*"),
	TCPAT(759, TailMatchCS, "\\dRp*"),
	TCPAT(760, TailMatchCS, "\\dRs*"),
	TCPAT(761, TailMatchCS, "\\ds*"),
	TCPAT(762, TailMatchCS, "\\dt*"),
	TCPAT(763, TailMatchCS, "\\dT*"),
	TCPAT(764, TailMatchCS, "\\du*"),
	TCPAT(764, TailMatchCS, "\\dg*"),
	TCPAT(764, TailMatchCS, "\\drg*"),
	TCPAT(765, TailMatchCS, "\\dv*"),
	TCPAT(766, TailMatchCS, "\\dx*"),
	TCPAT(767, TailMatchCS, "\\dX*"),
	TCPAT(768, TailMatchCS, "\\dm*"),
	TCPAT(769, TailMatchCS, "\\dE*"),
	TCPAT(770, TailMatchCS, "\\dy*"),
	TCPAT(771, TailMatchCS, "\\d*"),
	TCPAT(772, TailMatchCS, "\\ef"),
	TCPAT(773, TailMatchCS, "\\ev"),
	TCPAT(774, TailMatchCS, "\\encoding"),
	TCPAT(775, TailMatchCS, "\\h|\\help"),
	TCPAT(776, TailMatchCS, "\\h|\\help", MatchAny),
	TCPAT(777, TailMatchCS, "\\h|\\help", MatchAny, MatchAny),
	TCPAT(778, TailMatchCS, "\\h|\\help", MatchAny, MatchAny, MatchAny),
	TCPAT(779, TailMatchCS, "\\l*"),
	TCPAT(780, TailMatchCS, "\\password"),
	TCPAT(781, TailMatchCS, "\\pset"),
	TCPAT(782, TailMatchCS, "\\pset", MatchAny),
	TCPAT(783, TailMatchCS, "\\unset"),
	TCPAT(784, TailMatchCS, "\\set"),
	TCPAT(785, TailMatchCS, "\\set", MatchAny),
	TCPAT(786, TailMatchCS, "\\sf*"),
	TCPAT(787, TailMatchCS, "\\sv*"),
	TCPAT(788, TailMatchCS, "\\cd|\\e|\\edit|\\g|\\gx|\\i|\\include|" "\\ir|\\include_relative|\\o|\\out|" "\\s|\\w|\\write|\\lo_import"),
	TCPAT(788, TailMatchCS, "\\lo_export", MatchAny),
#line 1407 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
};

#endif							/* SWITCH_CONVERSION_APPLIED */

/* Storage parameters for CREATE TABLE and ALTER TABLE */
static const char *const table_storage_parameters[] = {
	"autovacuum_analyze_scale_factor",
	"autovacuum_analyze_threshold",
	"autovacuum_enabled",
	"autovacuum_freeze_max_age",
	"autovacuum_freeze_min_age",
	"autovacuum_freeze_table_age",
	"autovacuum_multixact_freeze_max_age",
	"autovacuum_multixact_freeze_min_age",
	"autovacuum_multixact_freeze_table_age",
	"autovacuum_parallel_workers",
	"autovacuum_vacuum_cost_delay",
	"autovacuum_vacuum_cost_limit",
	"autovacuum_vacuum_insert_scale_factor",
	"autovacuum_vacuum_insert_threshold",
	"autovacuum_vacuum_max_threshold",
	"autovacuum_vacuum_scale_factor",
	"autovacuum_vacuum_threshold",
	"fillfactor",
	"log_autovacuum_min_duration",
	"log_autoanalyze_min_duration",
	"parallel_workers",
	"toast.autovacuum_enabled",
	"toast.autovacuum_freeze_max_age",
	"toast.autovacuum_freeze_min_age",
	"toast.autovacuum_freeze_table_age",
	"toast.autovacuum_multixact_freeze_max_age",
	"toast.autovacuum_multixact_freeze_min_age",
	"toast.autovacuum_multixact_freeze_table_age",
	"toast.autovacuum_vacuum_cost_delay",
	"toast.autovacuum_vacuum_cost_limit",
	"toast.autovacuum_vacuum_insert_scale_factor",
	"toast.autovacuum_vacuum_insert_threshold",
	"toast.autovacuum_vacuum_max_threshold",
	"toast.autovacuum_vacuum_scale_factor",
	"toast.autovacuum_vacuum_threshold",
	"toast.log_autovacuum_min_duration",
	"toast.vacuum_index_cleanup",
	"toast.vacuum_max_eager_freeze_failure_rate",
	"toast.vacuum_truncate",
	"toast_tuple_target",
	"user_catalog_table",
	"vacuum_index_cleanup",
	"vacuum_max_eager_freeze_failure_rate",
	"vacuum_truncate",
	NULL
};

/* Optional parameters for CREATE VIEW and ALTER VIEW */
static const char *const view_optional_parameters[] = {
	"check_option",
	"security_barrier",
	"security_invoker",
	NULL
};

/* Forward declaration of functions */
static char **psql_completion(const char *text, int start, int end);
static char **match_previous_words(int pattern_id,
								   const char *text, int start, int end,
								   char **previous_words,
								   int previous_words_count);
static char *create_command_generator(const char *text, int state);
static char *drop_command_generator(const char *text, int state);
static char *alter_command_generator(const char *text, int state);
static char *complete_from_query(const char *text, int state);
static char *complete_from_versioned_query(const char *text, int state);
static char *complete_from_schema_query(const char *text, int state);
static char *complete_from_versioned_schema_query(const char *text, int state);
static char *_complete_from_query(const char *simple_query,
								  const SchemaQuery *schema_query,
								  const char *const *keywords,
								  bool verbatim,
								  const char *text, int state);
static void set_completion_reference(const char *word);
static void set_completion_reference_verbatim(const char *word);
static char *complete_from_list(const char *text, int state);
static char *complete_from_const(const char *text, int state);
static void append_variable_names(char ***varnames, int *nvars,
								  int *maxvars, const char *varname,
								  const char *prefix, const char *suffix);
static char **complete_from_variables(const char *text,
									  const char *prefix, const char *suffix, bool need_value);
static char *complete_from_files(const char *text, int state);
static char *_complete_from_files(const char *text, int state);

static char *pg_strdup_keyword_case(const char *s, const char *ref);
static char *escape_string(const char *text);
static char *make_like_pattern(const char *word);
static void parse_identifier(const char *ident,
							 char **schemaname, char **objectname,
							 bool *schemaquoted, bool *objectquoted);
static char *requote_identifier(const char *schemaname, const char *objectname,
								bool quote_schema, bool quote_object);
static bool identifier_needs_quotes(const char *ident);
static PGresult *exec_query(const char *query);

static char **get_previous_words(int point, char **buffer, int *nwords);

static char *get_guctype(const char *varname);

#ifdef USE_FILENAME_QUOTING_FUNCTIONS
static char *quote_file_name(char *fname, int match_type, char *quote_pointer);
static char *dequote_file_name(char *fname, int quote_char);
#endif


/*
 * Initialize the readline library for our purposes.
 */
void
initialize_readline(void)
{
	rl_readline_name = (char *) pset.progname;
	rl_attempted_completion_function = psql_completion;

#ifdef USE_FILENAME_QUOTING_FUNCTIONS
	rl_filename_quoting_function = quote_file_name;
	rl_filename_dequoting_function = dequote_file_name;
#endif

	rl_basic_word_break_characters = WORD_BREAKS;

	/*
	 * Ideally we'd include '"' in rl_completer_quote_characters too, which
	 * should allow us to complete quoted identifiers that include spaces.
	 * However, the library support for rl_completer_quote_characters is
	 * presently too inconsistent to want to mess with that.  (Note in
	 * particular that libedit has this variable but completely ignores it.)
	 */
	rl_completer_quote_characters = "'";

	/*
	 * Set rl_filename_quote_characters to "all possible characters",
	 * otherwise Readline will skip filename quoting if it thinks a filename
	 * doesn't need quoting.  Readline actually interprets this as bytes, so
	 * there are no encoding considerations here.
	 */
#ifdef HAVE_RL_FILENAME_QUOTE_CHARACTERS
	{
		unsigned char *fqc = (unsigned char *) pg_malloc(256);

		for (int i = 0; i < 255; i++)
			fqc[i] = (unsigned char) (i + 1);
		fqc[255] = '\0';
		rl_filename_quote_characters = (const char *) fqc;
	}
#endif

	completion_max_records = 1000;

	/*
	 * There is a variable rl_completion_query_items for this but apparently
	 * it's not defined everywhere.
	 */
}

/*
 * Check if 'word' matches any of the '|'-separated strings in 'pattern',
 * using case-insensitive or case-sensitive comparisons.
 *
 * If pattern is NULL, it's a wild card that matches any word.
 * If pattern begins with '!', the result is negated, ie we check that 'word'
 * does *not* match any alternative appearing in the rest of 'pattern'.
 * Any alternative can contain '*' which is a wild card, i.e., it can match
 * any substring; however, we allow at most one '*' per alternative.
 *
 * For readability, callers should use the macros MatchAny and MatchAnyExcept
 * to invoke those two special cases for 'pattern'.  (But '|' and '*' must
 * just be written directly in patterns.)  There is also MatchAnyN, but that
 * is supported only in Matches/MatchesCS and is not handled here.
 */
static bool
word_matches(const char *pattern,
			 const char *word,
			 bool case_sensitive)
{
	size_t		wordlen;

#define cimatch(s1, s2, n) \
	(case_sensitive ? strncmp(s1, s2, n) == 0 : pg_strncasecmp(s1, s2, n) == 0)

	/* NULL pattern matches anything. */
	if (pattern == NULL)
		return true;

	/* Handle negated patterns from the MatchAnyExcept macro. */
	if (*pattern == '!')
		return !word_matches(pattern + 1, word, case_sensitive);

	/* Else consider each alternative in the pattern. */
	wordlen = strlen(word);
	for (;;)
	{
		const char *star = NULL;
		const char *c;

		/* Find end of current alternative, and locate any wild card. */
		c = pattern;
		while (*c != '\0' && *c != '|')
		{
			if (*c == '*')
				star = c;
			c++;
		}
		/* Was there a wild card? */
		if (star)
		{
			/* Yes, wildcard match? */
			size_t		beforelen = star - pattern,
						afterlen = c - star - 1;

			if (wordlen >= (beforelen + afterlen) &&
				cimatch(word, pattern, beforelen) &&
				cimatch(word + wordlen - afterlen, star + 1, afterlen))
				return true;
		}
		else
		{
			/* No, plain match? */
			if (wordlen == (c - pattern) &&
				cimatch(word, pattern, wordlen))
				return true;
		}
		/* Out of alternatives? */
		if (*c == '\0')
			break;
		/* Nope, try next alternative. */
		pattern = c + 1;
	}

	return false;
}

/*
 * Implementation of TailMatches and TailMatchesCS tests: do the last N words
 * in previous_words match the pattern arguments?
 *
 * The array indexing might look backwards, but remember that
 * previous_words[0] contains the *last* word on the line, not the first.
 */
static bool
TailMatchesArray(bool case_sensitive,
				 int previous_words_count, char **previous_words,
				 int narg, const char *const *args)
{
	if (previous_words_count < narg)
		return false;

	for (int argno = 0; argno < narg; argno++)
	{
		const char *arg = args[argno];

		if (!word_matches(arg, previous_words[narg - argno - 1],
						  case_sensitive))
			return false;
	}

	return true;
}

/*
 * As above, but the pattern is passed as a variadic argument list.
 */
static bool
TailMatchesImpl(bool case_sensitive,
				int previous_words_count, char **previous_words,
				int narg, ...)
{
	const char *argarray[64];
	va_list		args;

	Assert(narg <= lengthof(argarray));

	if (previous_words_count < narg)
		return false;

	va_start(args, narg);
	for (int argno = 0; argno < narg; argno++)
		argarray[argno] = va_arg(args, const char *);
	va_end(args);

	return TailMatchesArray(case_sensitive,
							previous_words_count, previous_words,
							narg, argarray);
}

/*
 * Implementation of HeadMatches and HeadMatchesCS tests: do the first N
 * words in previous_words match the pattern arguments?
 */
static bool
HeadMatchesArray(bool case_sensitive,
				 int previous_words_count, char **previous_words,
				 int narg, const char *const *args)
{
	if (previous_words_count < narg)
		return false;

	for (int argno = 0; argno < narg; argno++)
	{
		const char *arg = args[argno];

		if (!word_matches(arg, previous_words[previous_words_count - argno - 1],
						  case_sensitive))
			return false;
	}

	return true;
}

/*
 * As above, but the pattern is passed as a variadic argument list.
 */
static bool
HeadMatchesImpl(bool case_sensitive,
				int previous_words_count, char **previous_words,
				int narg, ...)
{
	const char *argarray[64];
	va_list		args;

	Assert(narg <= lengthof(argarray));

	if (previous_words_count < narg)
		return false;

	va_start(args, narg);
	for (int argno = 0; argno < narg; argno++)
		argarray[argno] = va_arg(args, const char *);
	va_end(args);

	return HeadMatchesArray(case_sensitive,
							previous_words_count, previous_words,
							narg, argarray);
}

/*
 * Implementation of Matches and MatchesCS tests: do all of the words
 * in previous_words match the pattern arguments?
 *
 * This supports an additional kind of wildcard: MatchAnyN (represented as "")
 * can match any number of words, including zero, in the middle of the list.
 */
static bool
MatchesArray(bool case_sensitive,
			 int previous_words_count, char **previous_words,
			 int narg, const char *const *args)
{
	int			match_any_pos = -1;

	/* Even with MatchAnyN, there must be at least N-1 words */
	if (previous_words_count < narg - 1)
		return false;

	/* Check for MatchAnyN */
	for (int argno = 0; argno < narg; argno++)
	{
		const char *arg = args[argno];

		if (arg != NULL && arg[0] == '\0')
		{
			match_any_pos = argno;
			break;
		}
	}

	if (match_any_pos < 0)
	{
		/* Standard case without MatchAnyN */
		if (previous_words_count != narg)
			return false;

		/* Either Head or Tail match will do for the rest */
		if (!HeadMatchesArray(case_sensitive,
							  previous_words_count, previous_words,
							  narg, args))
			return false;
	}
	else
	{
		/* Match against head */
		if (!HeadMatchesArray(case_sensitive,
							  previous_words_count, previous_words,
							  match_any_pos, args))
			return false;

		/* Match against tail */
		if (!TailMatchesArray(case_sensitive,
							  previous_words_count, previous_words,
							  narg - match_any_pos - 1,
							  args + match_any_pos + 1))
			return false;
	}

	return true;
}

/*
 * As above, but the pattern is passed as a variadic argument list.
 */
static bool
MatchesImpl(bool case_sensitive,
			int previous_words_count, char **previous_words,
			int narg, ...)
{
	const char *argarray[64];
	va_list		args;

	Assert(narg <= lengthof(argarray));

	/* Even with MatchAnyN, there must be at least N-1 words */
	if (previous_words_count < narg - 1)
		return false;

	va_start(args, narg);
	for (int argno = 0; argno < narg; argno++)
		argarray[argno] = va_arg(args, const char *);
	va_end(args);

	return MatchesArray(case_sensitive,
						previous_words_count, previous_words,
						narg, argarray);
}

/*
 * Check if the final character of 's' is 'c'.
 */
static bool
ends_with(const char *s, char c)
{
	size_t		length = strlen(s);

	return (length > 0 && s[length - 1] == c);
}

/*
 * The completion function.
 *
 * According to readline spec this gets passed the text entered so far and its
 * start and end positions in the readline buffer. The return value is some
 * partially obscure list format that can be generated by readline's
 * rl_completion_matches() function, so we don't have to worry about it.
 */
static char **
psql_completion(const char *text, int start, int end)
{
	/* This is the variable we'll return. */
	char	  **matches = NULL;

	/* Workspace for parsed words. */
	char	   *words_buffer;

	/* This array will contain pointers to parsed words. */
	char	  **previous_words;

	/* The number of words found on the input line. */
	int			previous_words_count;

	/*
	 * For compactness, we use these macros to reference previous_words[].
	 * Caution: do not access a previous_words[] entry without having checked
	 * previous_words_count to be sure it's valid.  In most cases below, that
	 * check is implicit in a TailMatches() or similar macro, but in some
	 * places we have to check it explicitly.
	 */
#define prev_wd   (previous_words[0])
#define prev2_wd  (previous_words[1])
#define prev3_wd  (previous_words[2])
#define prev4_wd  (previous_words[3])
#define prev5_wd  (previous_words[4])
#define prev6_wd  (previous_words[5])
#define prev7_wd  (previous_words[6])
#define prev8_wd  (previous_words[7])
#define prev9_wd  (previous_words[8])

	/* Match the last N words before point, case-insensitively. */
#define TailMatches(...) \
	TailMatchesImpl(false, previous_words_count, previous_words, \
					VA_ARGS_NARGS(__VA_ARGS__), __VA_ARGS__)

	/* Match the last N words before point, case-sensitively. */
#define TailMatchesCS(...) \
	TailMatchesImpl(true, previous_words_count, previous_words, \
					VA_ARGS_NARGS(__VA_ARGS__), __VA_ARGS__)

	/* Match N words representing all of the line, case-insensitively. */
#define Matches(...) \
	MatchesImpl(false, previous_words_count, previous_words, \
				VA_ARGS_NARGS(__VA_ARGS__), __VA_ARGS__)

	/* Match N words representing all of the line, case-sensitively. */
#define MatchesCS(...) \
	MatchesImpl(true, previous_words_count, previous_words, \
				VA_ARGS_NARGS(__VA_ARGS__), __VA_ARGS__)

	/* Match the first N words on the line, case-insensitively. */
#define HeadMatches(...) \
	HeadMatchesImpl(false, previous_words_count, previous_words, \
					VA_ARGS_NARGS(__VA_ARGS__), __VA_ARGS__)

	/* Match the first N words on the line, case-sensitively. */
#define HeadMatchesCS(...) \
	HeadMatchesImpl(true, previous_words_count, previous_words, \
					VA_ARGS_NARGS(__VA_ARGS__), __VA_ARGS__)

	/* psql's backslash commands. */
	static const char *const backslash_commands[] = {
		"\\a",
		"\\bind", "\\bind_named",
		"\\connect", "\\conninfo", "\\C", "\\cd", "\\close_prepared", "\\copy",
		"\\copyright", "\\crosstabview",
		"\\d", "\\da", "\\dA", "\\dAc", "\\dAf", "\\dAo", "\\dAp",
		"\\db", "\\dc", "\\dconfig", "\\dC", "\\dd", "\\ddp", "\\dD",
		"\\des", "\\det", "\\deu", "\\dew", "\\dE", "\\df",
		"\\dF", "\\dFd", "\\dFp", "\\dFt", "\\dg", "\\di", "\\dl", "\\dL",
		"\\dm", "\\dn", "\\do", "\\dO", "\\dp", "\\dP", "\\dPi", "\\dPt",
		"\\drds", "\\drg", "\\dRs", "\\dRp", "\\ds",
		"\\dt", "\\dT", "\\dv", "\\du", "\\dx", "\\dX", "\\dy",
		"\\echo", "\\edit", "\\ef", "\\elif", "\\else", "\\encoding",
		"\\endif", "\\endpipeline", "\\errverbose", "\\ev",
		"\\f", "\\flush", "\\flushrequest",
		"\\g", "\\gdesc", "\\getenv", "\\getresults", "\\gexec", "\\gset", "\\gx",
		"\\help", "\\html",
		"\\if", "\\include", "\\include_relative", "\\ir",
		"\\list", "\\lo_import", "\\lo_export", "\\lo_list", "\\lo_unlink",
		"\\out",
		"\\parse", "\\password", "\\print", "\\prompt", "\\pset",
		"\\qecho", "\\quit",
		"\\reset", "\\restrict",
		"\\s", "\\sendpipeline", "\\set", "\\setenv", "\\sf",
		"\\startpipeline", "\\sv", "\\syncpipeline",
		"\\t", "\\T", "\\timing",
		"\\unrestrict", "\\unset",
		"\\x",
		"\\warn", "\\watch", "\\write",
		"\\z",
		"\\!", "\\?",
		NULL
	};

	/*
	 * Temporary workaround for a bug in recent (2019) libedit: it incorrectly
	 * de-escapes the input "text", causing us to fail to recognize backslash
	 * commands.  So get the string to look at from rl_line_buffer instead.
	 */
	char	   *text_copy = pnstrdup(rl_line_buffer + start, end - start);
	text = text_copy;

	/* Remember last char of the given input word. */
	completion_last_char = (end > start) ? text[end - start - 1] : '\0';

	/* We usually want the append character to be a space. */
	rl_completion_append_character = ' ';

	/* Clear a few things. */
	completion_charp = NULL;
	completion_charpp = NULL;
	completion_vquery = NULL;
	completion_squery = NULL;
	completion_ref_object = NULL;
	completion_ref_schema = NULL;

	/*
	 * Scan the input line to extract the words before our current position.
	 * According to those we'll make some smart decisions on what the user is
	 * probably intending to type.
	 */
	previous_words = get_previous_words(start,
										&words_buffer,
										&previous_words_count);

	/* If current word is a backslash command, offer completions for that */
	if (text[0] == '\\')
		COMPLETE_WITH_LIST_CS(backslash_commands);

	/* If current word is a variable interpolation, handle that case */
	else if (text[0] == ':' && text[1] != ':')
	{
		if (text[1] == '\'')
			matches = complete_from_variables(text, ":'", "'", true);
		else if (text[1] == '"')
			matches = complete_from_variables(text, ":\"", "\"", true);
		else if (text[1] == '{' && text[2] == '?')
			matches = complete_from_variables(text, ":{?", "}", true);
		else
			matches = complete_from_variables(text, ":", "", true);
	}

	/* If no previous word, suggest one of the basic sql commands */
	else if (previous_words_count == 0)
		COMPLETE_WITH_LIST(sql_commands);

	/* Else try completions based on matching patterns of previous words */
	else
	{
#ifdef SWITCH_CONVERSION_APPLIED
		/*
		 * If we have transformed match_previous_words into a switch, iterate
		 * through tcpatterns[] to see which pattern ids match.
		 *
		 * For now, we have to try the patterns in the order they are stored
		 * (matching the order of switch cases in match_previous_words),
		 * because some of the logic in match_previous_words assumes that
		 * previous matches have been eliminated.  This is fairly
		 * unprincipled, and it is likely that there are undesirable as well
		 * as desirable interactions hidden in the order of the pattern
		 * checks.  TODO: think about a better way to manage that.
		 */
		for (size_t tindx = 0; tindx < lengthof(tcpatterns); tindx++)
		{
			const TCPattern *tcpat = tcpatterns + tindx;
			bool		match = false;

			switch (tcpat->kind)
			{
				case Match:
					match = MatchesArray(false,
										 previous_words_count,
										 previous_words,
										 tcpat->nwords, tcpat->words);
					break;
				case MatchCS:
					match = MatchesArray(true,
										 previous_words_count,
										 previous_words,
										 tcpat->nwords, tcpat->words);
					break;
				case HeadMatch:
					match = HeadMatchesArray(false,
											 previous_words_count,
											 previous_words,
											 tcpat->nwords, tcpat->words);
					break;
				case HeadMatchCS:
					match = HeadMatchesArray(true,
											 previous_words_count,
											 previous_words,
											 tcpat->nwords, tcpat->words);
					break;
				case TailMatch:
					match = TailMatchesArray(false,
											 previous_words_count,
											 previous_words,
											 tcpat->nwords, tcpat->words);
					break;
				case TailMatchCS:
					match = TailMatchesArray(true,
											 previous_words_count,
											 previous_words,
											 tcpat->nwords, tcpat->words);
					break;
			}
			if (match)
			{
				matches = match_previous_words(tcpat->id, text, start, end,
											   previous_words,
											   previous_words_count);
				if (matches != NULL)
					break;
			}
		}
#else							/* !SWITCH_CONVERSION_APPLIED */
		/*
		 * If gen_tabcomplete.pl hasn't been applied to this code, just let
		 * match_previous_words scan through all its patterns.
		 */
		matches = match_previous_words(0, text, start, end,
									   previous_words,
									   previous_words_count);
#endif							/* SWITCH_CONVERSION_APPLIED */
	}

	/*
	 * Finally, we look through the list of "things", such as TABLE, INDEX and
	 * check if that was the previous word. If so, execute the query to get a
	 * list of them.
	 */
	if (matches == NULL && previous_words_count > 0)
	{
		const pgsql_thing_t *wac;

		for (wac = words_after_create; wac->name != NULL; wac++)
		{
			if (pg_strcasecmp(prev_wd, wac->name) == 0)
			{
				if (wac->query)
					COMPLETE_WITH_QUERY_LIST(wac->query,
											 wac->keywords);
				else if (wac->vquery)
					COMPLETE_WITH_VERSIONED_QUERY_LIST(wac->vquery,
													   wac->keywords);
				else if (wac->squery)
					COMPLETE_WITH_VERSIONED_SCHEMA_QUERY_LIST(wac->squery,
															  wac->keywords);
				break;
			}
		}
	}

	/*
	 * If we still don't have anything to match we have to fabricate some sort
	 * of default list. If we were to just return NULL, readline automatically
	 * attempts filename completion, and that's usually no good.
	 */
	if (matches == NULL)
	{
		COMPLETE_WITH_CONST(true, "");
		/* Also, prevent Readline from appending stuff to the non-match */
		rl_completion_append_character = '\0';
#ifdef HAVE_RL_COMPLETION_SUPPRESS_QUOTE
		rl_completion_suppress_quote = 1;
#endif
	}

	/* free storage */
	pg_free(previous_words);
	pg_free(words_buffer);
	pfree(text_copy);
	pg_free(completion_ref_object);
	completion_ref_object = NULL;
	pg_free(completion_ref_schema);
	completion_ref_schema = NULL;

	/* Return our Grand List O' Matches */
	return matches;
}

/*
 * Subroutine to try matches based on previous_words.
 *
 * This can operate in one of two modes.  As presented, the body of the
 * function is a long if-else-if chain that sequentially tries each known
 * match rule.  That works, but some C compilers have trouble with such a long
 * else-if chain, either taking extra time to compile or failing altogether.
 * Therefore, we prefer to transform the else-if chain into a switch, and then
 * each call of this function considers just one match rule (under control of
 * a loop in psql_completion()).  Compilers tend to be more ready to deal
 * with many-arm switches than many-arm else-if chains.
 *
 * Each if-condition in this function must begin with a call of one of the
 * functions Matches, HeadMatches, TailMatches, MatchesCS, HeadMatchesCS, or
 * TailMatchesCS.  The preprocessor gen_tabcomplete.pl strips out those
 * calls and converts them into entries in tcpatterns[], which are evaluated
 * by the calling loop in psql_completion().  Successful matches result in
 * calls to this function with the appropriate pattern_id, causing just the
 * corresponding switch case to be executed.
 *
 * If-conditions in this function can be more complex than a single *Matches
 * function call in one of two ways (but not both!).  They can be OR's
 * of *Matches calls, such as
 *  else if (Matches("ALTER", "VIEW", MatchAny, "ALTER", MatchAny) ||
 *           Matches("ALTER", "VIEW", MatchAny, "ALTER", "COLUMN", MatchAny))
 * or they can be a *Matches call AND'ed with some other condition, e.g.
 *  else if (Matches("CREATE", "PUBLICATION", MatchAny, "FOR", "TABLE", MatchAny) &&
 *           !ends_with(prev_wd, ','))
 * The former case is transformed into multiple tcpatterns[] entries and
 * multiple case labels for the same bit of code.  The latter case is
 * transformed into a case label and a contained if-statement.
 *
 * This is split out of psql_completion() primarily to separate code that
 * gen_tabcomplete.pl should process from code that it should not, although
 * doing so also helps to avoid extra indentation of this code.
 *
 * Returns a matches list, or NULL if no match.
 */
static char **
match_previous_words(int pattern_id,
					 const char *text, int start, int end,
					 char **previous_words, int previous_words_count)
{
	/* This is the variable we'll return. */
	char	  **matches = NULL;

	/* Dummy statement, allowing all the match rules to look like "else if" */
	if (0)
	{
		/* skip */
	}

	/* gen_tabcomplete.pl begins special processing here */
	switch (pattern_id)
	{
#line 2194 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"

/* CREATE */
	/* complete with something you can create */
	case 1:
	{
		/* only some object types can be created as part of CREATE SCHEMA */
		if (HeadMatches("CREATE", "SCHEMA"))
			COMPLETE_WITH("AGGREGATE", "COLLATION", "DOMAIN", "FUNCTION",
						  "INDEX", "OPERATOR", "PROCEDURE", "SEQUENCE", "TABLE",
						  "TEXT SEARCH CONFIGURATION", "TEXT SEARCH DICTIONARY",
						  "TEXT SEARCH PARSER", "TEXT SEARCH TEMPLATE",
						  "TRIGGER", "TYPE", "VIEW",
			/* for INDEX and TABLE/SEQUENCE, respectively */
						  "UNIQUE", "UNLOGGED");
		else
			COMPLETE_WITH_GENERATOR(create_command_generator);
	}
	/* complete with something you can create or replace */
	break; case 2:
		COMPLETE_WITH("FUNCTION", "PROCEDURE", "LANGUAGE", "RULE", "VIEW",
					  "AGGREGATE", "TRANSFORM", "TRIGGER");

/* DROP, but not DROP embedded in other commands */
	/* complete with something you can drop */
	break; case 3:
		COMPLETE_WITH_GENERATOR(drop_command_generator);

/* ALTER */

	/* ALTER TABLE */
	break; case 4:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_tables,
										"ALL IN TABLESPACE");

	/* ALTER something */
	break; case 5:
		COMPLETE_WITH_GENERATOR(alter_command_generator);
	/* ALTER TABLE,INDEX,MATERIALIZED VIEW ALL IN TABLESPACE xxx */
	break; case 6:
		COMPLETE_WITH("SET TABLESPACE", "OWNED BY");
	/* ALTER TABLE,INDEX,MATERIALIZED VIEW ALL IN TABLESPACE xxx OWNED BY */
	break; case 7:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	/* ALTER TABLE,INDEX,MATERIALIZED VIEW ALL IN TABLESPACE xxx OWNED BY xxx */
	break; case 8:
		COMPLETE_WITH("SET TABLESPACE");
	/* ALTER AGGREGATE,FUNCTION,PROCEDURE,ROUTINE <name> */
	break; case 9:
		COMPLETE_WITH("(");
	/* ALTER AGGREGATE <name> (...) */
	break; case 10:
	{
		if (ends_with(prev_wd, ')'))
			COMPLETE_WITH("OWNER TO", "RENAME TO", "SET SCHEMA");
		else
			COMPLETE_WITH_FUNCTION_ARG(prev2_wd);
	}
	/* ALTER FUNCTION <name> (...) */
	break; case 11:
	{
		if (ends_with(prev_wd, ')'))
			COMPLETE_WITH(Alter_function_options);
		else
			COMPLETE_WITH_FUNCTION_ARG(prev2_wd);
	}
	/* ALTER PROCEDURE <name> (...) */
	break; case 12:
	{
		if (ends_with(prev_wd, ')'))
			COMPLETE_WITH(Alter_procedure_options);
		else
			COMPLETE_WITH_FUNCTION_ARG(prev2_wd);
	}
	/* ALTER ROUTINE <name> (...) */
	break; case 13:
	{
		if (ends_with(prev_wd, ')'))
			COMPLETE_WITH(Alter_routine_options);
		else
			COMPLETE_WITH_FUNCTION_ARG(prev2_wd);
	}
	/* ALTER FUNCTION|ROUTINE <name> (...) PARALLEL */
	break; case 14:
		COMPLETE_WITH("RESTRICTED", "SAFE", "UNSAFE");
	/* ALTER FUNCTION|PROCEDURE|ROUTINE <name> (...) [EXTERNAL] SECURITY */
	break; case 15:
#line 2281 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("DEFINER", "INVOKER");
	/* ALTER FUNCTION|PROCEDURE|ROUTINE <name> (...) RESET */
	break; case 16:
		COMPLETE_WITH_QUERY_VERBATIM_PLUS(Query_for_list_of_set_vars,
										  "ALL");
	/* ALTER FUNCTION|PROCEDURE|ROUTINE <name> (...) SET */
	break; case 17:
		COMPLETE_WITH_QUERY_VERBATIM_PLUS(Query_for_list_of_set_vars,
										  "SCHEMA");

	/* ALTER PUBLICATION <name> */
	break; case 18:
		COMPLETE_WITH("ADD", "DROP", "OWNER TO", "RENAME TO", "SET");
	/* ALTER PUBLICATION <name> ADD */
	break; case 19:
		COMPLETE_WITH("TABLES IN SCHEMA", "TABLE");
	break; case 20:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 21:
#line 2300 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (ends_with(prev_wd, ','))
#line 2301 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);

	/*
	 * "ALTER PUBLICATION <name> SET TABLE <name> WHERE (" - complete with
	 * table attributes
	 *
	 * "ALTER PUBLICATION <name> ADD TABLE <name> WHERE (" - complete with
	 * table attributes
	 */
	break; case 22:
		COMPLETE_WITH("(");
	break; case 23:
		COMPLETE_WITH_ATTR(prev3_wd);
	break; case 24:
#line 2315 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("WHERE", "(*)"))
#line 2316 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH(",", "WHERE (");
	break; case 25:
		COMPLETE_WITH(",");
	/* ALTER PUBLICATION <name> DROP */
	break; case 26:
		COMPLETE_WITH("TABLES IN SCHEMA", "TABLE");
	/* ALTER PUBLICATION <name> SET */
	break; case 27:
		COMPLETE_WITH("(", "ALL SEQUENCES", "ALL TABLES", "TABLES IN SCHEMA", "TABLE");
	break; case 28:
		COMPLETE_WITH("SEQUENCES", "TABLES");
	break; case 29:
		COMPLETE_WITH("EXCEPT ( TABLE");
	break; case 30:
		COMPLETE_WITH("( TABLE");
	break; case 31:
		COMPLETE_WITH("TABLE");
	/* Complete "ALTER PUBLICATION <name> FOR TABLE" with "<table>, ..." */
	break; case 32:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 33:
#line 2336 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (ends_with(prev_wd, ','))
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 34:
#line 2338 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!ends_with(prev_wd, ','))
		COMPLETE_WITH(")");
	break; case 35:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_schemas
								 " AND nspname NOT LIKE E'pg\\\\_%%'",
								 "CURRENT_SCHEMA");
	/* ALTER PUBLICATION <name> SET ( */
	break; case 36:
		COMPLETE_WITH("publish", "publish_generated_columns", "publish_via_partition_root");
	/* ALTER SUBSCRIPTION <name> */
	break; case 37:
		COMPLETE_WITH("CONNECTION", "ENABLE", "DISABLE", "OWNER TO",
					  "RENAME TO", "REFRESH PUBLICATION", "REFRESH SEQUENCES",
					  "SERVER", "SET", "SKIP (", "ADD PUBLICATION", "DROP PUBLICATION");
	break; case 38:
		COMPLETE_WITH_QUERY(Query_for_list_of_servers);
	/* ALTER SUBSCRIPTION <name> REFRESH */
	break; case 39:
		COMPLETE_WITH("PUBLICATION", "SEQUENCES");
	/* ALTER SUBSCRIPTION <name> REFRESH PUBLICATION */
	break; case 40:
		COMPLETE_WITH("WITH (");
	/* ALTER SUBSCRIPTION <name> REFRESH PUBLICATION WITH ( */
	break; case 41:
		COMPLETE_WITH("copy_data");
	/* ALTER SUBSCRIPTION <name> SET */
	break; case 42:
		COMPLETE_WITH("(", "PUBLICATION");
	/* ALTER SUBSCRIPTION <name> SET ( */
	break; case 43:
		COMPLETE_WITH("binary", "conflict_log_destination", "disable_on_error",
					  "failover", "max_retention_duration", "origin",
					  "password_required", "retain_dead_tuples",
					  "run_as_owner", "slot_name", "streaming",
					  "synchronous_commit", "two_phase",
					  "wal_receiver_timeout");
	/* ALTER SUBSCRIPTION <name> SKIP ( */
	break; case 44:
		COMPLETE_WITH("lsn");
	/* ALTER SUBSCRIPTION <name> SET PUBLICATION */
	break; case 45:
	{
		/* complete with nothing here as this refers to remote publications */
	}
	/* ALTER SUBSCRIPTION <name> ADD|DROP|SET PUBLICATION <name> */
	break; case 46:
#line 2385 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("WITH (");
	/* ALTER SUBSCRIPTION <name> ADD|DROP|SET PUBLICATION <name> WITH ( */
	break; case 47:
#line 2389 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("copy_data", "refresh");

	/* ALTER SCHEMA <name> */
	break; case 48:
		COMPLETE_WITH("OWNER TO", "RENAME TO");

	/* ALTER COLLATION <name> */
	break; case 49:
		COMPLETE_WITH("OWNER TO", "REFRESH VERSION", "RENAME TO", "SET SCHEMA");

	/* ALTER CONVERSION <name> */
	break; case 50:
		COMPLETE_WITH("OWNER TO", "RENAME TO", "SET SCHEMA");

	/* ALTER DATABASE <name> */
	break; case 51:
		COMPLETE_WITH("RESET", "SET", "OWNER TO", "REFRESH COLLATION VERSION", "RENAME TO",
					  "IS_TEMPLATE", "ALLOW_CONNECTIONS",
					  "CONNECTION LIMIT");

	/* ALTER DATABASE <name> RESET */
	break; case 52:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_database_vars, "ALL");
	}

	/* ALTER DATABASE <name> SET TABLESPACE */
	break; case 53:
		COMPLETE_WITH_QUERY(Query_for_list_of_tablespaces);

	/* ALTER EVENT TRIGGER */
	break; case 54:
		COMPLETE_WITH_QUERY(Query_for_list_of_event_triggers);

	/* ALTER EVENT TRIGGER <name> */
	break; case 55:
		COMPLETE_WITH("DISABLE", "ENABLE", "OWNER TO", "RENAME TO");

	/* ALTER EVENT TRIGGER <name> ENABLE */
	break; case 56:
		COMPLETE_WITH("REPLICA", "ALWAYS");

	/* ALTER EXTENSION <name> */
	break; case 57:
		COMPLETE_WITH("ADD", "DROP", "UPDATE", "SET SCHEMA");

	/* ALTER EXTENSION <name> ADD|DROP */
	break; case 58:
		COMPLETE_WITH("ACCESS METHOD", "AGGREGATE", "CAST", "COLLATION",
					  "CONVERSION", "DOMAIN", "EVENT TRIGGER", "FOREIGN",
					  "FUNCTION", "MATERIALIZED VIEW", "OPERATOR",
					  "LANGUAGE", "PROCEDURE", "ROUTINE", "SCHEMA",
					  "SEQUENCE", "SERVER", "TABLE", "TEXT SEARCH",
					  "TRANSFORM FOR", "TYPE", "VIEW");

	/* ALTER EXTENSION <name> ADD|DROP FOREIGN */
	break; case 59:
		COMPLETE_WITH("DATA WRAPPER", "TABLE");

	/* ALTER EXTENSION <name> ADD|DROP OPERATOR */
	break; case 60:
		COMPLETE_WITH("CLASS", "FAMILY");

	/* ALTER EXTENSION <name> ADD|DROP TEXT SEARCH */
	break; case 61:
		COMPLETE_WITH("CONFIGURATION", "DICTIONARY", "PARSER", "TEMPLATE");

	/* ALTER EXTENSION <name> UPDATE */
	break; case 62:
		COMPLETE_WITH("TO");

	/* ALTER EXTENSION <name> UPDATE TO */
	break; case 63:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_QUERY(Query_for_list_of_available_extension_versions);
	}

	/* ALTER FOREIGN */
	break; case 64:
		COMPLETE_WITH("DATA WRAPPER", "TABLE");

	/* ALTER FOREIGN DATA WRAPPER <name> */
	break; case 65:
		COMPLETE_WITH("CONNECTION", "HANDLER", "NO",
					  "OPTIONS", "OWNER TO", "RENAME TO", "VALIDATOR");
	break; case 66:
		COMPLETE_WITH("CONNECTION", "HANDLER", "VALIDATOR");

	/* ALTER FOREIGN TABLE <name> */
	break; case 67:
		COMPLETE_WITH("ADD", "ALTER", "DISABLE TRIGGER", "DROP", "ENABLE",
					  "INHERIT", "NO INHERIT", "OPTIONS", "OWNER TO",
					  "RENAME", "SET", "VALIDATE CONSTRAINT");

	/* ALTER INDEX */
	break; case 68:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_indexes,
										"ALL IN TABLESPACE");
	/* ALTER INDEX <name> */
	break; case 69:
		COMPLETE_WITH("ALTER COLUMN", "OWNER TO", "RENAME TO", "SET",
					  "RESET", "ATTACH PARTITION",
					  "DEPENDS ON EXTENSION", "NO DEPENDS ON EXTENSION");
	break; case 70:
		COMPLETE_WITH("PARTITION");
	break; case 71:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_indexes);
	/* ALTER INDEX <name> ALTER */
	break; case 72:
		COMPLETE_WITH("COLUMN");
	/* ALTER INDEX <name> ALTER COLUMN */
	break; case 73:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY_VERBATIM(Query_for_list_of_attribute_numbers);
	}
	/* ALTER INDEX <name> ALTER COLUMN <colnum> */
	break; case 74:
		COMPLETE_WITH("SET STATISTICS");
	/* ALTER INDEX <name> ALTER COLUMN <colnum> SET */
	break; case 75:
		COMPLETE_WITH("STATISTICS");
	/* ALTER INDEX <name> ALTER COLUMN <colnum> SET STATISTICS */
	break; case 76:
	{
		/* Enforce no completion here, as an integer has to be specified */
	}
	/* ALTER INDEX <name> SET */
	break; case 77:
		COMPLETE_WITH("(", "TABLESPACE");
	/* ALTER INDEX <name> RESET */
	break; case 78:
		COMPLETE_WITH("(");
	/* ALTER INDEX <foo> SET|RESET ( */
	break; case 79:
		COMPLETE_WITH("fillfactor",
					  "deduplicate_items",	/* BTREE */
					  "fastupdate", "gin_pending_list_limit",	/* GIN */
					  "buffering",	/* GiST */
					  "pages_per_range", "autosummarize"	/* BRIN */
			);
	break; case 80:
		COMPLETE_WITH("fillfactor =",
					  "deduplicate_items =",	/* BTREE */
					  "fastupdate =", "gin_pending_list_limit =",	/* GIN */
					  "buffering =",	/* GiST */
					  "pages_per_range =", "autosummarize ="	/* BRIN */
			);
	break; case 81:
		COMPLETE_WITH("ON EXTENSION");
	break; case 82:
		COMPLETE_WITH("ON EXTENSION");

	/* ALTER LANGUAGE <name> */
	break; case 83:
		COMPLETE_WITH("OWNER TO", "RENAME TO");

	/* ALTER LARGE OBJECT <oid> */
	break; case 84:
		COMPLETE_WITH("OWNER TO");

	/* ALTER MATERIALIZED VIEW */
	break; case 85:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_matviews,
										"ALL IN TABLESPACE");

	/* ALTER USER,ROLE <name> */
	break; case 86:
#line 2559 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("USER", "MAPPING"))
#line 2560 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("BYPASSRLS", "CONNECTION LIMIT", "CREATEDB", "CREATEROLE",
					  "ENCRYPTED PASSWORD", "IN", "INHERIT", "LOGIN", "NOBYPASSRLS",
					  "NOCREATEDB", "NOCREATEROLE", "NOINHERIT",
					  "NOLOGIN", "NOREPLICATION", "NOSUPERUSER", "PASSWORD",
					  "RENAME TO", "REPLICATION", "RESET", "SET", "SUPERUSER",
					  "VALID UNTIL", "WITH");
	/* ALTER USER,ROLE <name> IN */
	break; case 87:
		COMPLETE_WITH("DATABASE");
	/* ALTER USER,ROLE <name> IN DATABASE */
	break; case 88:
		COMPLETE_WITH_QUERY(Query_for_list_of_databases);
	/* ALTER USER,ROLE <name> IN DATABASE <dbname> */
	break; case 89:
		COMPLETE_WITH("SET", "RESET");
	/* ALTER USER,ROLE <name> IN DATABASE <dbname> SET */
	break; case 90:
		COMPLETE_WITH_QUERY(Query_for_list_of_set_vars);
	/* XXX missing support for ALTER ROLE <name> IN DATABASE <dbname> RESET */
	/* ALTER USER,ROLE <name> RESET */
	break; case 91:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_user_vars, "ALL");
	}

	/* ALTER USER,ROLE <name> WITH */
	break; case 92:
		/* Similar to the above, but don't complete "WITH" again. */
		COMPLETE_WITH("BYPASSRLS", "CONNECTION LIMIT", "CREATEDB", "CREATEROLE",
					  "ENCRYPTED PASSWORD", "INHERIT", "LOGIN", "NOBYPASSRLS",
					  "NOCREATEDB", "NOCREATEROLE", "NOINHERIT",
					  "NOLOGIN", "NOREPLICATION", "NOSUPERUSER", "PASSWORD",
					  "RENAME TO", "REPLICATION", "RESET", "SET", "SUPERUSER",
					  "VALID UNTIL");

	/* ALTER DEFAULT PRIVILEGES */
	break; case 93:
		COMPLETE_WITH("FOR", "GRANT", "IN SCHEMA", "REVOKE");
	/* ALTER DEFAULT PRIVILEGES FOR */
	break; case 94:
		COMPLETE_WITH("ROLE");
	/* ALTER DEFAULT PRIVILEGES IN */
	break; case 95:
		COMPLETE_WITH("SCHEMA");
	/* ALTER DEFAULT PRIVILEGES FOR ROLE|USER ... */
	break; case 96:
#line 2608 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("GRANT", "REVOKE", "IN SCHEMA");
	/* ALTER DEFAULT PRIVILEGES IN SCHEMA ... */
	break; case 97:
#line 2612 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("GRANT", "REVOKE", "FOR ROLE");
	/* ALTER DEFAULT PRIVILEGES IN SCHEMA ... FOR */
	break; case 98:
#line 2616 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ROLE");
	/* ALTER DEFAULT PRIVILEGES FOR ROLE|USER ... IN SCHEMA ... */
	/* ALTER DEFAULT PRIVILEGES IN SCHEMA ... FOR ROLE|USER ... */
	break; case 99:
#line 2623 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("GRANT", "REVOKE");
	/* ALTER DOMAIN <name> */
	break; case 100:
		COMPLETE_WITH("ADD", "DROP", "OWNER TO", "RENAME", "SET",
					  "VALIDATE CONSTRAINT");
	/* ALTER DOMAIN <sth> ADD */
	break; case 101:
		COMPLETE_WITH("CONSTRAINT", "NOT NULL", "CHECK (");
	/* ALTER DOMAIN <sth> ADD CONSTRAINT <sth> */
	break; case 102:
		COMPLETE_WITH("NOT NULL", "CHECK (");
	/* ALTER DOMAIN <sth> DROP */
	break; case 103:
		COMPLETE_WITH("CONSTRAINT", "DEFAULT", "NOT NULL");
	/* ALTER DOMAIN <sth> DROP|RENAME|VALIDATE CONSTRAINT */
	break; case 104:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_constraint_of_type);
	}
	/* ALTER DOMAIN <sth> RENAME */
	break; case 105:
		COMPLETE_WITH("CONSTRAINT", "TO");
	/* ALTER DOMAIN <sth> RENAME CONSTRAINT <sth> */
	break; case 106:
		COMPLETE_WITH("TO");

	/* ALTER DOMAIN <sth> SET */
	break; case 107:
		COMPLETE_WITH("DEFAULT", "NOT NULL", "SCHEMA");
	/* ALTER SEQUENCE <name> */
	break; case 108:
		COMPLETE_WITH("AS", "INCREMENT", "MINVALUE", "MAXVALUE", "RESTART",
					  "START", "NO", "CACHE", "CYCLE", "SET", "OWNED BY",
					  "OWNER TO", "RENAME TO");
	/* ALTER SEQUENCE <name> AS */
	break; case 109:
		COMPLETE_WITH_CS("smallint", "integer", "bigint");
	/* ALTER SEQUENCE <name> NO */
	break; case 110:
		COMPLETE_WITH("MINVALUE", "MAXVALUE", "CYCLE");
	/* ALTER SEQUENCE <name> SET */
	break; case 111:
		COMPLETE_WITH("SCHEMA", "LOGGED", "UNLOGGED");
	/* ALTER SERVER <name> */
	break; case 112:
		COMPLETE_WITH("VERSION", "OPTIONS", "OWNER TO", "RENAME TO");
	/* ALTER SERVER <name> VERSION <version> */
	break; case 113:
		COMPLETE_WITH("OPTIONS");
	/* ALTER SYSTEM SET, RESET, RESET ALL */
	break; case 114:
		COMPLETE_WITH("SET", "RESET");
	break; case 115:
		COMPLETE_WITH_QUERY_VERBATIM_PLUS(Query_for_list_of_alter_system_set_vars,
										  "ALL");
	break; case 116:
		COMPLETE_WITH("TO");
	/* ALTER VIEW <name> */
	break; case 117:
		COMPLETE_WITH("ALTER COLUMN", "OWNER TO", "RENAME", "RESET", "SET");
	/* ALTER VIEW xxx RENAME */
	break; case 118:
		COMPLETE_WITH_ATTR_PLUS(prev2_wd, "COLUMN", "TO");
	break; case 119:
		COMPLETE_WITH_ATTR(prev3_wd);
	/* ALTER VIEW xxx ALTER [ COLUMN ] yyy */
	break; case 120:
#line 2692 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SET DEFAULT", "DROP DEFAULT");
	/* ALTER VIEW xxx RENAME yyy */
	break; case 121:
		COMPLETE_WITH("TO");
	/* ALTER VIEW xxx RENAME COLUMN yyy */
	break; case 122:
		COMPLETE_WITH("TO");
	/* ALTER VIEW xxx RESET ( */
	break; case 123:
		COMPLETE_WITH("(");
	/* Complete ALTER VIEW xxx SET with "(" or "SCHEMA" */
	break; case 124:
		COMPLETE_WITH("(", "SCHEMA");
	/* ALTER VIEW xxx SET|RESET ( yyy [= zzz] ) */
	break; case 125:
		COMPLETE_WITH_LIST(view_optional_parameters);
	break; case 126:
		COMPLETE_WITH("=");
	break; case 127:
		COMPLETE_WITH("local", "cascaded");
	break; case 128:
		COMPLETE_WITH("true", "false");

	/* ALTER MATERIALIZED VIEW <name> */
	break; case 129:
		COMPLETE_WITH("ALTER COLUMN", "CLUSTER ON", "DEPENDS ON EXTENSION",
					  "NO DEPENDS ON EXTENSION", "OWNER TO", "RENAME",
					  "RESET (", "SET");
	/* ALTER MATERIALIZED VIEW xxx RENAME */
	break; case 130:
		COMPLETE_WITH_ATTR_PLUS(prev2_wd, "COLUMN", "TO");
	break; case 131:
		COMPLETE_WITH_ATTR(prev3_wd);
	/* ALTER MATERIALIZED VIEW xxx RENAME yyy */
	break; case 132:
		COMPLETE_WITH("TO");
	/* ALTER MATERIALIZED VIEW xxx RENAME COLUMN yyy */
	break; case 133:
		COMPLETE_WITH("TO");
	/* ALTER MATERIALIZED VIEW xxx SET */
	break; case 134:
		COMPLETE_WITH("(", "ACCESS METHOD", "SCHEMA", "TABLESPACE", "WITHOUT CLUSTER");
	/* ALTER MATERIALIZED VIEW xxx SET ACCESS METHOD */
	break; case 135:
		COMPLETE_WITH_QUERY(Query_for_list_of_table_access_methods);

	/* ALTER POLICY <name> */
	break; case 136:
		COMPLETE_WITH_QUERY(Query_for_list_of_policies);
	/* ALTER POLICY <name> ON */
	break; case 137:
		COMPLETE_WITH("ON");
	/* ALTER POLICY <name> ON <table> */
	break; case 138:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_policy);
	}
	/* ALTER POLICY <name> ON <table> - show options */
	break; case 139:
		COMPLETE_WITH("RENAME TO", "TO", "USING (", "WITH CHECK (");
	/* ALTER POLICY <name> ON <table> TO <role> */
	break; case 140:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_grant_roles);
	/* ALTER POLICY <name> ON <table> USING ( */
	break; case 141:
		COMPLETE_WITH("(");
	/* ALTER POLICY <name> ON <table> WITH CHECK ( */
	break; case 142:
		COMPLETE_WITH("(");

	/* ALTER PROPERTY GRAPH */
	break; case 143:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_propgraphs);
	break; case 144:
		COMPLETE_WITH("ADD", "ALTER", "DROP", "OWNER TO", "RENAME TO", "SET SCHEMA");
	break; case 145:
		COMPLETE_WITH("VERTEX", "EDGE");
	break; case 146:
		COMPLETE_WITH("TABLES");
	break; case 147:
#line 2773 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (TailMatches("EDGE"))
		COMPLETE_WITH("TABLES");
	break; case 148:
		COMPLETE_WITH("TABLE");

	/* ALTER RULE <name>, add ON */
	break; case 149:
		COMPLETE_WITH("ON");

	/* If we have ALTER RULE <name> ON, then add the correct tablename */
	break; case 150:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_rule);
	}

	/* ALTER RULE <name> ON <name> */
	break; case 151:
		COMPLETE_WITH("RENAME TO");

	/* ALTER STATISTICS <name> */
	break; case 152:
		COMPLETE_WITH("OWNER TO", "RENAME TO", "SET SCHEMA", "SET STATISTICS");
	/* ALTER STATISTICS <name> SET */
	break; case 153:
		COMPLETE_WITH("SCHEMA", "STATISTICS");

	/* ALTER TRIGGER <name>, add ON */
	break; case 154:
		COMPLETE_WITH("ON");

	break; case 155:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_trigger);
	}

	/* ALTER TRIGGER <name> ON <name> */
	break; case 156:
		COMPLETE_WITH("RENAME TO", "DEPENDS ON EXTENSION",
					  "NO DEPENDS ON EXTENSION");

	/*
	 * If we detect ALTER TABLE <name>, suggest sub commands
	 */
	break; case 157:
		COMPLETE_WITH("ADD", "ALTER", "CLUSTER ON", "DISABLE", "DROP",
					  "ENABLE", "INHERIT", "NO", "RENAME", "RESET",
					  "OWNER TO", "SET", "VALIDATE CONSTRAINT",
					  "REPLICA IDENTITY", "ATTACH PARTITION",
					  "DETACH PARTITION", "FORCE ROW LEVEL SECURITY",
					  "SPLIT PARTITION", "MERGE PARTITIONS (",
					  "OF", "NOT OF");
	/* ALTER TABLE xxx ADD */
	break; case 158:
	{
		/*
		 * make sure to keep this list and the MatchAnyExcept() below in sync
		 */
		COMPLETE_WITH("COLUMN", "CONSTRAINT", "CHECK (", "NOT NULL", "UNIQUE",
					  "PRIMARY KEY", "EXCLUDE", "FOREIGN KEY");
	}
	/* ALTER TABLE xxx ADD [COLUMN] yyy */
	break; case 159:
#line 2838 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	/* ALTER TABLE xxx ADD CONSTRAINT yyy */
	break; case 160:
		COMPLETE_WITH("CHECK (", "NOT NULL", "UNIQUE", "PRIMARY KEY", "EXCLUDE", "FOREIGN KEY");
	/* ALTER TABLE xxx ADD NOT NULL */
	break; case 161:
		COMPLETE_WITH_ATTR(prev4_wd);
	break; case 162:
		COMPLETE_WITH_ATTR(prev6_wd);
	/* ALTER TABLE xxx ADD [CONSTRAINT yyy] (PRIMARY KEY|UNIQUE) */
	break; case 163:
#line 2852 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(", "USING INDEX");
	/* ALTER TABLE xxx ADD PRIMARY KEY USING INDEX */
	break; case 164:
	{
		set_completion_reference(prev6_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_unique_index_of_table);
	}
	/* ALTER TABLE xxx ADD UNIQUE USING INDEX */
	break; case 165:
	{
		set_completion_reference(prev5_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_unique_index_of_table);
	}
	/* ALTER TABLE xxx ADD CONSTRAINT yyy PRIMARY KEY USING INDEX */
	break; case 166:
#line 2868 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		set_completion_reference(prev8_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_unique_index_of_table);
	}
	/* ALTER TABLE xxx ADD CONSTRAINT yyy UNIQUE USING INDEX */
	break; case 167:
#line 2875 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		set_completion_reference(prev7_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_unique_index_of_table);
	}
	/* ALTER TABLE xxx ENABLE */
	break; case 168:
		COMPLETE_WITH("ALWAYS", "REPLICA", "ROW LEVEL SECURITY", "RULE",
					  "TRIGGER");
	break; case 169:
		COMPLETE_WITH("RULE", "TRIGGER");
	break; case 170:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_rule_of_table);
	}
	break; case 171:
	{
		set_completion_reference(prev4_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_rule_of_table);
	}
	break; case 172:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_trigger_of_table);
	}
	break; case 173:
	{
		set_completion_reference(prev4_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_trigger_of_table);
	}
	/* ALTER TABLE xxx INHERIT */
	break; case 174:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	/* ALTER TABLE xxx NO */
	break; case 175:
		COMPLETE_WITH("FORCE ROW LEVEL SECURITY", "INHERIT");
	/* ALTER TABLE xxx NO INHERIT */
	break; case 176:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	/* ALTER TABLE xxx DISABLE */
	break; case 177:
		COMPLETE_WITH("ROW LEVEL SECURITY", "RULE", "TRIGGER");
	break; case 178:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_rule_of_table);
	}
	break; case 179:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_trigger_of_table);
	}

	/* ALTER TABLE xxx ALTER */
	break; case 180:
		COMPLETE_WITH_ATTR_PLUS(prev2_wd, "COLUMN", "CONSTRAINT");

	/* ALTER TABLE xxx RENAME */
	break; case 181:
		COMPLETE_WITH_ATTR_PLUS(prev2_wd, "COLUMN", "CONSTRAINT", "TO");
	break; case 182:
		COMPLETE_WITH_ATTR(prev3_wd);

	/* ALTER TABLE xxx RENAME yyy */
	break; case 183:
		COMPLETE_WITH("TO");

	/* ALTER TABLE xxx RENAME COLUMN/CONSTRAINT yyy */
	break; case 184:
		COMPLETE_WITH("TO");

	/* If we have ALTER TABLE <sth> DROP, provide COLUMN or CONSTRAINT */
	break; case 185:
		COMPLETE_WITH("COLUMN", "CONSTRAINT");
	/* If we have ALTER TABLE <sth> DROP COLUMN, provide list of columns */
	break; case 186:
		COMPLETE_WITH_ATTR(prev3_wd);
	/* ALTER TABLE <sth> ALTER|DROP|RENAME CONSTRAINT <constraint> */
	break; case 187:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_constraint_of_table);
	}
	/* ALTER TABLE <sth> VALIDATE CONSTRAINT <non-validated constraint> */
	break; case 188:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_constraint_of_table_not_validated);
	}
	/* ALTER TABLE ALTER [COLUMN] <foo> */
	break; case 189:
#line 2967 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("TYPE", "SET", "RESET", "RESTART", "ADD", "DROP");
	/* ALTER TABLE ALTER [COLUMN] <foo> ADD */
	break; case 190:
#line 2971 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("GENERATED");
	/* ALTER TABLE ALTER [COLUMN] <foo> ADD GENERATED */
	break; case 191:
#line 2975 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ALWAYS", "BY DEFAULT");
	/* ALTER TABLE ALTER [COLUMN] <foo> ADD GENERATED */
	break; case 192:
#line 2981 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS IDENTITY");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET */
	break; case 193:
#line 2985 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(", "COMPRESSION", "DATA TYPE", "DEFAULT", "EXPRESSION", "GENERATED", "NOT NULL",
					  "STATISTICS", "STORAGE",
		/* a subset of ALTER SEQUENCE options */
					  "INCREMENT", "MINVALUE", "MAXVALUE", "START", "NO", "CACHE", "CYCLE");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET ( */
	break; case 194:
#line 2992 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("n_distinct", "n_distinct_inherited");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET COMPRESSION */
	break; case 195:
#line 2996 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("DEFAULT", "PGLZ", "LZ4");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET EXPRESSION */
	break; case 196:
#line 3000 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET EXPRESSION AS */
	break; case 197:
#line 3004 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET GENERATED */
	break; case 198:
#line 3008 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ALWAYS", "BY DEFAULT");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET NO */
	break; case 199:
#line 3012 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("MINVALUE", "MAXVALUE", "CYCLE");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET STORAGE */
	break; case 200:
#line 3016 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("DEFAULT", "PLAIN", "EXTERNAL", "EXTENDED", "MAIN");
	/* ALTER TABLE ALTER [COLUMN] <foo> SET STATISTICS */
	break; case 201:
#line 3020 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/* Enforce no completion here, as an integer has to be specified */
	}
	/* ALTER TABLE ALTER [COLUMN] <foo> DROP */
	break; case 202:
#line 3026 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("DEFAULT", "EXPRESSION", "IDENTITY", "NOT NULL");
	break; case 203:
		COMPLETE_WITH("ON");
	break; case 204:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_index_of_table);
	}
	/* If we have ALTER TABLE <sth> SET, provide list of attributes and '(' */
	break; case 205:
		COMPLETE_WITH("(", "ACCESS METHOD", "LOGGED", "SCHEMA",
					  "TABLESPACE", "UNLOGGED", "WITH", "WITHOUT");

	/*
	 * If we have ALTER TABLE <sth> SET ACCESS METHOD provide a list of table
	 * AMs.
	 */
	break; case 206:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_table_access_methods,
								 "DEFAULT");

	/*
	 * If we have ALTER TABLE <sth> SET TABLESPACE provide a list of
	 * tablespaces
	 */
	break; case 207:
		COMPLETE_WITH_QUERY(Query_for_list_of_tablespaces);
	/* If we have ALTER TABLE <sth> SET WITHOUT provide CLUSTER or OIDS */
	break; case 208:
		COMPLETE_WITH("CLUSTER", "OIDS");
	/* ALTER TABLE <foo> RESET */
	break; case 209:
		COMPLETE_WITH("(");
	/* ALTER TABLE <foo> SET|RESET ( */
	break; case 210:
		COMPLETE_WITH_LIST(table_storage_parameters);
	break; case 211:
	{
		set_completion_reference(prev5_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_index_of_table);
	}
	break; case 212:
		COMPLETE_WITH("INDEX");
	break; case 213:
		COMPLETE_WITH("FULL", "NOTHING", "DEFAULT", "USING");
	break; case 214:
		COMPLETE_WITH("IDENTITY");

	/*
	 * If we have ALTER TABLE <foo> ATTACH PARTITION, provide a list of
	 * tables.
	 */
	break; case 215:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	/* Limited completion support for partition bound specification */
	break; case 216:
		COMPLETE_WITH("FOR VALUES", "DEFAULT");
	break; case 217:
		COMPLETE_WITH("FROM (", "IN (", "WITH (");

	/*
	 * If we have ALTER TABLE <foo> DETACH|SPLIT PARTITION, provide a list of
	 * partitions of <foo>.
	 */
	break; case 218:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_partition_of_table);
	}
	break; case 219:
		COMPLETE_WITH("CONCURRENTLY", "FINALIZE");

	/* ALTER TABLE <name> SPLIT PARTITION <name> */
	break; case 220:
		COMPLETE_WITH("INTO ( PARTITION");

	/* ALTER TABLE <name> MERGE PARTITIONS ( */
	break; case 221:
	{
		set_completion_reference(prev4_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_partition_of_table);
	}
	break; case 222:
		COMPLETE_WITH("INTO");

	/* ALTER TABLE <name> OF */
	break; case 223:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_composite_datatypes);

	/* ALTER TABLESPACE <foo> with RENAME TO, OWNER TO, SET, RESET */
	break; case 224:
		COMPLETE_WITH("RENAME TO", "OWNER TO", "SET", "RESET");
	/* ALTER TABLESPACE <foo> SET|RESET */
	break; case 225:
		COMPLETE_WITH("(");
	/* ALTER TABLESPACE <foo> SET|RESET ( */
	break; case 226:
		COMPLETE_WITH("seq_page_cost", "random_page_cost",
					  "effective_io_concurrency", "maintenance_io_concurrency");

	/* ALTER TEXT SEARCH */
	break; case 227:
		COMPLETE_WITH("CONFIGURATION", "DICTIONARY", "PARSER", "TEMPLATE");
	break; case 228:
		COMPLETE_WITH("RENAME TO", "SET SCHEMA");
	break; case 229:
		COMPLETE_WITH("(", "OWNER TO", "RENAME TO", "SET SCHEMA");
	break; case 230:
		COMPLETE_WITH("ADD MAPPING FOR", "ALTER MAPPING",
					  "DROP MAPPING FOR",
					  "OWNER TO", "RENAME TO", "SET SCHEMA");

	/* complete ALTER TYPE <foo> with actions */
	break; case 231:
		COMPLETE_WITH("ADD ATTRIBUTE", "ADD VALUE", "ALTER ATTRIBUTE",
					  "DROP ATTRIBUTE",
					  "OWNER TO", "RENAME", "SET SCHEMA", "SET (");
	/* complete ALTER TYPE <foo> ADD with actions */
	break; case 232:
		COMPLETE_WITH("ATTRIBUTE", "VALUE");
	/* ALTER TYPE <foo> RENAME	*/
	break; case 233:
		COMPLETE_WITH("ATTRIBUTE", "TO", "VALUE");
	/* ALTER TYPE xxx RENAME (ATTRIBUTE|VALUE) yyy */
	break; case 234:
		COMPLETE_WITH("TO");
	/* ALTER TYPE xxx RENAME ATTRIBUTE yyy TO zzz */
	break; case 235:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/*
	 * If we have ALTER TYPE <sth> ALTER/DROP/RENAME ATTRIBUTE, provide list
	 * of attributes
	 */
	break; case 236:
		COMPLETE_WITH_ATTR(prev3_wd);
	/* complete ALTER TYPE ADD ATTRIBUTE <foo> with list of types */
	break; case 237:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	/* complete ALTER TYPE ADD ATTRIBUTE <foo> <footype> with CASCADE/RESTRICT */
	break; case 238:
		COMPLETE_WITH("CASCADE", "RESTRICT");
	/* complete ALTER TYPE DROP ATTRIBUTE <foo> with CASCADE/RESTRICT */
	break; case 239:
		COMPLETE_WITH("CASCADE", "RESTRICT");
	/* ALTER TYPE ALTER ATTRIBUTE <foo> */
	break; case 240:
		COMPLETE_WITH("TYPE");
	/* ALTER TYPE ALTER ATTRIBUTE <foo> TYPE <footype> */
	break; case 241:
		COMPLETE_WITH("CASCADE", "RESTRICT");
	/* complete ALTER TYPE <sth> RENAME VALUE with list of enum values */
	break; case 242:
		COMPLETE_WITH_ENUM_VALUE(prev3_wd);
	/* ALTER TYPE <foo> SET */
	break; case 243:
		COMPLETE_WITH("(", "SCHEMA");
	/* complete ALTER TYPE <foo> SET ( with settable properties */
	break; case 244:
		COMPLETE_WITH("ANALYZE", "RECEIVE", "SEND", "STORAGE", "SUBSCRIPT",
					  "TYPMOD_IN", "TYPMOD_OUT");

	/* complete ALTER GROUP <foo> */
	break; case 245:
		COMPLETE_WITH("ADD USER", "DROP USER", "RENAME TO");
	/* complete ALTER GROUP <foo> ADD|DROP with USER */
	break; case 246:
		COMPLETE_WITH("USER");
	/* complete ALTER GROUP <foo> ADD|DROP USER with a user name */
	break; case 247:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);

/*
 * ANALYZE [ ( option [, ...] ) ] [ [ ONLY ] table_and_columns [, ...] ]
 * ANALYZE [ VERBOSE ] [ [ ONLY ] table_and_columns [, ...] ]
 */
	break; case 248:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_analyzables,
										"(", "VERBOSE", "ONLY");
	break; case 249:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_analyzables,
										"ONLY");
	break; case 250:
#line 3209 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("ANALYZE", "(*)"))
#line 3210 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("VERBOSE", "SKIP_LOCKED", "BUFFER_USAGE_LIMIT");
		else if (TailMatches("VERBOSE|SKIP_LOCKED"))
			COMPLETE_WITH("ON", "OFF");
	}
	break; case 251:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_analyzables,
										"ONLY");
	break; case 252:
		/* "ANALYZE (" should be caught above, so assume we want columns */
		COMPLETE_WITH_ATTR(prev2_wd);
	break; case 253:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_analyzables);

/* BEGIN */
	break; case 254:
		COMPLETE_WITH("WORK", "TRANSACTION", "ISOLATION LEVEL", "READ", "DEFERRABLE", "NOT DEFERRABLE");
/* END, ABORT */
	break; case 255:
		COMPLETE_WITH("AND", "WORK", "TRANSACTION");
/* COMMIT */
	break; case 256:
		COMPLETE_WITH("AND", "WORK", "TRANSACTION", "PREPARED");
/* RELEASE SAVEPOINT */
	break; case 257:
		COMPLETE_WITH("SAVEPOINT");
/* ROLLBACK */
	break; case 258:
		COMPLETE_WITH("AND", "WORK", "TRANSACTION", "TO SAVEPOINT", "PREPARED");
	break; case 259:
		COMPLETE_WITH("CHAIN");
/* CALL */
	break; case 260:
		COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(Query_for_list_of_procedures);
	break; case 261:
		COMPLETE_WITH("(");
/* CHECKPOINT */
	break; case 262:
		COMPLETE_WITH("(");
	break; case 263:
#line 3256 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("CHECKPOINT", "(*)"))
#line 3257 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("MODE", "FLUSH_UNLOGGED");
		else if (TailMatches("MODE"))
			COMPLETE_WITH("FAST", "SPREAD");
		else if (TailMatches("FLUSH_UNLOGGED"))
			COMPLETE_WITH("ON", "OFF");
	}
/* CLOSE */
	break; case 264:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_cursors,
								 "ALL");
/* CLUSTER */
	break; case 265:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_clusterables,
										"VERBOSE");
	break; case 266:
#line 3280 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_clusterables);
	/* If we have CLUSTER <sth>, then add "USING" */
	break; case 267:
		COMPLETE_WITH("USING");
	/* If we have CLUSTER VERBOSE <sth>, then add "USING" */
	break; case 268:
		COMPLETE_WITH("USING");
	/* If we have CLUSTER <sth> USING, then add the index as well */
	break; case 269:
#line 3290 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_index_of_table);
	}
	break; case 270:
#line 3295 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("CLUSTER", "(*)"))
#line 3296 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("VERBOSE");
	}

/* COMMENT */
	break; case 271:
		COMPLETE_WITH("ON");
	break; case 272:
		COMPLETE_WITH("ACCESS METHOD", "AGGREGATE", "CAST", "COLLATION",
					  "COLUMN", "CONSTRAINT", "CONVERSION", "DATABASE",
					  "DOMAIN", "EXTENSION", "EVENT TRIGGER",
					  "FOREIGN DATA WRAPPER", "FOREIGN TABLE",
					  "FUNCTION", "INDEX", "LANGUAGE", "LARGE OBJECT",
					  "MATERIALIZED VIEW", "OPERATOR", "POLICY",
					  "PROCEDURE", "PROCEDURAL LANGUAGE", "PROPERTY GRAPH", "PUBLICATION", "ROLE",
					  "ROUTINE", "RULE", "SCHEMA", "SEQUENCE", "SERVER",
					  "STATISTICS", "SUBSCRIPTION", "TABLE",
					  "TABLESPACE", "TEXT SEARCH", "TRANSFORM FOR",
					  "TRIGGER", "TYPE", "VIEW");
	break; case 273:
		COMPLETE_WITH_QUERY(Query_for_list_of_access_methods);
	break; case 274:
		COMPLETE_WITH_QUERY(Query_for_all_table_constraints);
	break; case 275:
		COMPLETE_WITH("ON");
	break; case 276:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_tables_for_constraint,
										"DOMAIN");
	}
	break; case 277:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_domains);
	break; case 278:
		COMPLETE_WITH_QUERY(Query_for_list_of_event_triggers);
	break; case 279:
		COMPLETE_WITH("DATA WRAPPER", "TABLE");
	break; case 280:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_foreign_tables);
	break; case 281:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_matviews);
	break; case 282:
		COMPLETE_WITH_QUERY(Query_for_list_of_policies);
	break; case 283:
		COMPLETE_WITH("ON");
	break; case 284:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_policy);
	}
	break; case 285:
		COMPLETE_WITH_QUERY(Query_for_list_of_languages);
	break; case 286:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_propgraphs);
	break; case 287:
		COMPLETE_WITH("ON");
	break; case 288:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_rule);
	}
	break; case 289:
		COMPLETE_WITH("CONFIGURATION", "DICTIONARY", "PARSER", "TEMPLATE");
	break; case 290:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_configurations);
	break; case 291:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_dictionaries);
	break; case 292:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_parsers);
	break; case 293:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_templates);
	break; case 294:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 295:
		COMPLETE_WITH("LANGUAGE");
	break; case 296:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_QUERY(Query_for_list_of_languages);
	}
	break; case 297:
		COMPLETE_WITH("ON");
	break; case 298:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_trigger);
	}
	break; case 299:
#line 3393 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("IS");

/* COPY */

	/*
	 * If we have COPY, offer list of tables or "(" (Also cover the analogous
	 * backslash command).
	 */
	break; case 300:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_tables_for_copy, "(");
	/* Complete COPY ( with legal query commands */
	break; case 301:
		COMPLETE_WITH("SELECT", "TABLE", "VALUES", "INSERT INTO", "UPDATE", "DELETE FROM", "MERGE INTO", "WITH");
	/* Complete COPY <sth> */
	break; case 302:
		COMPLETE_WITH("FROM", "TO");
	/* Complete COPY|\copy <sth> FROM|TO with filename or STDIN/STDOUT/PROGRAM */
	break; case 303:
	{
		if (HeadMatches("COPY"))
		{
			/* COPY requires quoted filename */
			if (TailMatches("FROM"))
				COMPLETE_WITH_FILES_PLUS("", true, "STDIN", "PROGRAM");
			else
				COMPLETE_WITH_FILES_PLUS("", true, "STDOUT", "PROGRAM");
		}
		else
		{
			/* \copy supports pstdin and pstdout */
			if (TailMatches("FROM"))
				COMPLETE_WITH_FILES_PLUS("", false, "STDIN", "PSTDIN", "PROGRAM");
			else
				COMPLETE_WITH_FILES_PLUS("", false, "STDOUT", "PSTDOUT", "PROGRAM");
		}
	}

	/* Complete COPY|\copy <sth> FROM|TO PROGRAM */
	break; case 304:
		COMPLETE_WITH_FILES("", HeadMatches("COPY"));	/* COPY requires quoted
														 * filename */

	/* Complete COPY <sth> TO [PROGRAM] <sth> */
	break; case 305:
#line 3438 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("WITH (");

	/* Complete COPY <sth> FROM [PROGRAM] <sth> */
	break; case 306:
#line 3443 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("WITH (", "WHERE");

	/* Complete COPY <sth> FROM|TO [PROGRAM] filename WITH ( */
	break; case 307:
#line 3448 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (!HeadMatches("COPY|\\copy", MatchAny, "FROM|TO", MatchAnyExcept("PROGRAM"), "WITH", "(*)") &&
			!HeadMatches("COPY|\\copy", MatchAny, "FROM|TO", "PROGRAM", MatchAny, "WITH", "(*)"))
		{
			/*
			 * This fires if we're in an unfinished parenthesized option list.
			 * get_previous_words treats a completed parenthesized option list
			 * as one word, so the above tests are correct.
			 */

			if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			{
				if (HeadMatches("COPY|\\copy", MatchAny, "FROM"))
					COMPLETE_WITH(Copy_from_options);
				else
					COMPLETE_WITH(Copy_to_options);
			}

			/* Complete COPY <sth> FROM|TO filename WITH (FORMAT */
			else if (TailMatches("FORMAT"))
				COMPLETE_WITH("binary", "csv", "text", "json");

			/* Complete COPY <sth> FROM|TO filename WITH (FREEZE */
			else if (TailMatches("FREEZE"))
				COMPLETE_WITH("true", "false");

			/* Complete COPY <sth> FROM|TO filename WITH (HEADER */
			else if (TailMatches("HEADER"))
				COMPLETE_WITH("true", "false", "MATCH");

			/* Complete COPY <sth> FROM filename WITH (ON_ERROR */
			else if (TailMatches("ON_ERROR"))
				COMPLETE_WITH("stop", "ignore", "set_null");

			/* Complete COPY <sth> FROM filename WITH (LOG_VERBOSITY */
			else if (TailMatches("LOG_VERBOSITY"))
				COMPLETE_WITH("silent", "default", "verbose");
		}

		/* A completed parenthesized option list should be caught below */
	}

	/* Complete COPY <sth> FROM [PROGRAM] <sth> WITH (<options>) */
	break; case 308:
#line 3493 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("WHERE");

	/* CREATE ACCESS METHOD */
	/* Complete "CREATE ACCESS METHOD <name>" */
	break; case 309:
		COMPLETE_WITH("TYPE");
	/* Complete "CREATE ACCESS METHOD <name> TYPE" */
	break; case 310:
		COMPLETE_WITH("INDEX", "TABLE");
	/* Complete "CREATE ACCESS METHOD <name> TYPE <type>" */
	break; case 311:
		COMPLETE_WITH("HANDLER");

	/* CREATE COLLATION */
	break; case 312:
		COMPLETE_WITH("(", "FROM");
	break; case 313:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_collations);
	break; case 314:
	{
		if (TailMatches("(|*,"))
			COMPLETE_WITH("LOCALE =", "LC_COLLATE =", "LC_CTYPE =",
						  "PROVIDER =", "DETERMINISTIC =");
		else if (TailMatches("PROVIDER", "="))
			COMPLETE_WITH("libc", "icu");
		else if (TailMatches("DETERMINISTIC", "="))
			COMPLETE_WITH("true", "false");
	}

	/* CREATE DATABASE */
	break; case 315:
		COMPLETE_WITH("OWNER", "TEMPLATE", "ENCODING", "TABLESPACE",
					  "IS_TEMPLATE", "STRATEGY",
					  "ALLOW_CONNECTIONS", "CONNECTION LIMIT",
					  "LC_COLLATE", "LC_CTYPE", "LOCALE", "OID",
					  "LOCALE_PROVIDER", "ICU_LOCALE");

	break; case 316:
		COMPLETE_WITH_QUERY(Query_for_list_of_template_databases);
	break; case 317:
		COMPLETE_WITH("WAL_LOG", "FILE_COPY");

	/* CREATE DOMAIN --- is allowed inside CREATE SCHEMA, so use TailMatches */
	break; case 318:
		COMPLETE_WITH("AS");
	break; case 319:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 320:
		COMPLETE_WITH("COLLATE", "DEFAULT", "CONSTRAINT",
					  "NOT NULL", "NULL", "CHECK (");
	break; case 321:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_collations);

	/* CREATE EXTENSION */
	/* Complete with available extensions rather than installed ones. */
	break; case 322:
		COMPLETE_WITH_QUERY(Query_for_list_of_available_extensions);
	/* CREATE EXTENSION <name> */
	break; case 323:
		COMPLETE_WITH("WITH SCHEMA", "CASCADE", "VERSION");
	/* CREATE EXTENSION <name> VERSION */
	break; case 324:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_QUERY(Query_for_list_of_available_extension_versions);
	}

	/* CREATE FOREIGN */
	break; case 325:
		COMPLETE_WITH("DATA WRAPPER", "TABLE");

	/* CREATE FOREIGN DATA WRAPPER */
	break; case 326:
		COMPLETE_WITH("CONNECTION", "HANDLER", "OPTIONS", "VALIDATOR");

	/* CREATE FOREIGN TABLE */
	break; case 327:
		COMPLETE_WITH("(", "PARTITION OF");

	/* CREATE INDEX --- is allowed inside CREATE SCHEMA, so use TailMatches */
	/* First off we complete CREATE UNIQUE with "INDEX" */
	break; case 328:
		COMPLETE_WITH("INDEX");

	/*
	 * If we have CREATE|UNIQUE INDEX, then add "ON", "CONCURRENTLY", and
	 * existing indexes
	 */
	break; case 329:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_indexes,
										"ON", "CONCURRENTLY");

	/*
	 * Complete ... INDEX|CONCURRENTLY [<name>] ON with a list of relations
	 * that indexes can be created on
	 */
	break; case 330:
#line 3591 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_indexables);

	/*
	 * Complete CREATE|UNIQUE INDEX CONCURRENTLY with "ON" and existing
	 * indexes
	 */
	break; case 331:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_indexes,
										"ON");
	/* Complete CREATE|UNIQUE INDEX [CONCURRENTLY] <sth> with "ON" */
	break; case 332:
#line 3603 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON");

	/*
	 * Complete INDEX <name> ON <table> with a list of table columns (which
	 * should really be in parens)
	 */
	break; case 333:
#line 3611 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(", "USING");
	break; case 334:
#line 3614 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_ATTR(prev2_wd);
	/* same if you put in USING */
	break; case 335:
		COMPLETE_WITH_ATTR(prev4_wd);
	/* Complete USING with an index method */
	break; case 336:
#line 3622 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY(Query_for_list_of_index_access_methods);
	break; case 337:
#line 3625 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("POLICY", MatchAny, MatchAny, MatchAny, MatchAny, MatchAny) && !TailMatches("FOR", MatchAny, MatchAny, MatchAny))
#line 3626 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(");

	/* CREATE OR REPLACE */
	break; case 338:
		COMPLETE_WITH("REPLACE");

	/* CREATE POLICY */
	/* Complete "CREATE POLICY <name> ON" */
	break; case 339:
		COMPLETE_WITH("ON");
	/* Complete "CREATE POLICY <name> ON <table>" */
	break; case 340:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	/* Complete "CREATE POLICY <name> ON <table> AS|FOR|TO|USING|WITH CHECK" */
	break; case 341:
		COMPLETE_WITH("AS", "FOR", "TO", "USING (", "WITH CHECK (");
	/* CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE */
	break; case 342:
		COMPLETE_WITH("PERMISSIVE", "RESTRICTIVE");

	/*
	 * CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE
	 * FOR|TO|USING|WITH CHECK
	 */
	break; case 343:
		COMPLETE_WITH("FOR", "TO", "USING", "WITH CHECK");
	/* CREATE POLICY <name> ON <table> FOR ALL|SELECT|INSERT|UPDATE|DELETE */
	break; case 344:
		COMPLETE_WITH("ALL", "SELECT", "INSERT", "UPDATE", "DELETE");
	/* Complete "CREATE POLICY <name> ON <table> FOR INSERT TO|WITH CHECK" */
	break; case 345:
		COMPLETE_WITH("TO", "WITH CHECK (");
	/* Complete "CREATE POLICY <name> ON <table> FOR SELECT|DELETE TO|USING" */
	break; case 346:
		COMPLETE_WITH("TO", "USING (");
	/* CREATE POLICY <name> ON <table> FOR ALL|UPDATE TO|USING|WITH CHECK */
	break; case 347:
		COMPLETE_WITH("TO", "USING (", "WITH CHECK (");
	/* Complete "CREATE POLICY <name> ON <table> TO <role>" */
	break; case 348:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_grant_roles);
	/* Complete "CREATE POLICY <name> ON <table> USING (" */
	break; case 349:
		COMPLETE_WITH("(");

	/*
	 * CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE FOR
	 * ALL|SELECT|INSERT|UPDATE|DELETE
	 */
	break; case 350:
		COMPLETE_WITH("ALL", "SELECT", "INSERT", "UPDATE", "DELETE");

	/*
	 * Complete "CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE FOR
	 * INSERT TO|WITH CHECK"
	 */
	break; case 351:
		COMPLETE_WITH("TO", "WITH CHECK (");

	/*
	 * Complete "CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE FOR
	 * SELECT|DELETE TO|USING"
	 */
	break; case 352:
		COMPLETE_WITH("TO", "USING (");

	/*
	 * CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE FOR
	 * ALL|UPDATE TO|USING|WITH CHECK
	 */
	break; case 353:
		COMPLETE_WITH("TO", "USING (", "WITH CHECK (");

	/*
	 * Complete "CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE TO
	 * <role>"
	 */
	break; case 354:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_grant_roles);

	/*
	 * Complete "CREATE POLICY <name> ON <table> AS PERMISSIVE|RESTRICTIVE
	 * USING ("
	 */
	break; case 355:
		COMPLETE_WITH("(");

/* CREATE PROPERTY GRAPH */
	break; case 356:
		COMPLETE_WITH("GRAPH");
	break; case 357:
		COMPLETE_WITH("VERTEX");
	break; case 358:
		COMPLETE_WITH("TABLES");
	break; case 359:
		COMPLETE_WITH("(");
	break; case 360:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 361:
		COMPLETE_WITH("EDGE");
	break; case 362:
#line 3728 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (TailMatches("EDGE|RELATIONSHIP"))
		COMPLETE_WITH("TABLES");
	break; case 363:
#line 3730 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (TailMatches("EDGE|RELATIONSHIP", "TABLES"))
		COMPLETE_WITH("(");
	break; case 364:
#line 3732 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (TailMatches("EDGE|RELATIONSHIP", "TABLES", "("))
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);

/* CREATE PUBLICATION */
	break; case 365:
		COMPLETE_WITH("FOR TABLE", "FOR TABLES IN SCHEMA", "FOR ALL TABLES", "FOR ALL SEQUENCES", "WITH (");
	break; case 366:
		COMPLETE_WITH("TABLE", "TABLES IN SCHEMA", "ALL TABLES", "ALL SEQUENCES");
	break; case 367:
		COMPLETE_WITH("TABLES", "SEQUENCES");
	break; case 368:
		COMPLETE_WITH("EXCEPT ( TABLE", "WITH (");
	break; case 369:
		COMPLETE_WITH("( TABLE");
	break; case 370:
		COMPLETE_WITH("TABLE");
	break; case 371:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 372:
#line 3750 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (ends_with(prev_wd, ','))
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 373:
#line 3752 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!ends_with(prev_wd, ','))
		COMPLETE_WITH(")");
	break; case 374:
		COMPLETE_WITH("IN SCHEMA");
	break; case 375:
#line 3756 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!ends_with(prev_wd, ','))
		COMPLETE_WITH("WHERE (", "WITH (");
	/* Complete "CREATE PUBLICATION <name> FOR TABLE" with "<table>, ..." */
	break; case 376:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);

	/*
	 * "CREATE PUBLICATION <name> FOR TABLE <name> WHERE (" - complete with
	 * table attributes
	 */
	break; case 377:
		COMPLETE_WITH("(");
	break; case 378:
		COMPLETE_WITH_ATTR(prev3_wd);
	break; case 379:
		COMPLETE_WITH(" WITH (");

	/*
	 * Complete "CREATE PUBLICATION <name> FOR TABLES IN SCHEMA <schema>, ..."
	 */
	break; case 380:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_schemas
								 " AND nspname NOT LIKE E'pg\\\\_%%'",
								 "CURRENT_SCHEMA");
	break; case 381:
#line 3780 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if ((!ends_with(prev_wd, ',')))
		COMPLETE_WITH("WITH (");
	/* Complete "CREATE PUBLICATION <name> [...] WITH" */
	break; case 382:
		COMPLETE_WITH("publish", "publish_generated_columns", "publish_via_partition_root");

/* CREATE RULE */
	/* Complete "CREATE [ OR REPLACE ] RULE <sth>" with "AS ON" */
	break; case 383:
#line 3790 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS ON");
	/* Complete "CREATE [ OR REPLACE ] RULE <sth> AS" with "ON" */
	break; case 384:
#line 3794 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON");

	/*
	 * Complete "CREATE [ OR REPLACE ] RULE <sth> AS ON" with
	 * SELECT|UPDATE|INSERT|DELETE
	 */
	break; case 385:
#line 3802 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SELECT", "UPDATE", "INSERT", "DELETE");
	/* Complete "AS ON SELECT|UPDATE|INSERT|DELETE" with a "TO" */
	break; case 386:
		COMPLETE_WITH("TO");
	/* Complete "AS ON <sth> TO" with a table name */
	break; case 387:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);

/* CREATE SCHEMA [ <name> ] [ AUTHORIZATION ] */
	break; case 388:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_schemas,
								 "AUTHORIZATION");
	break; case 389:
#line 3816 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_owner_roles);
	break; case 390:
#line 3820 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("CREATE", "GRANT");
	break; case 391:
		COMPLETE_WITH("AUTHORIZATION", "CREATE", "GRANT");

/* CREATE SEQUENCE --- is allowed inside CREATE SCHEMA, so use TailMatches */
	break; case 392:
#line 3827 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS", "INCREMENT BY", "MINVALUE", "MAXVALUE", "NO",
					  "CACHE", "CYCLE", "OWNED BY", "START WITH");
	break; case 393:
#line 3831 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_CS("smallint", "integer", "bigint");
	break; case 394:
#line 3834 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("MINVALUE", "MAXVALUE", "CYCLE");

/* CREATE SERVER <name> */
	break; case 395:
		COMPLETE_WITH("TYPE", "VERSION", "FOREIGN DATA WRAPPER");

/* CREATE STATISTICS <name> */
	break; case 396:
		COMPLETE_WITH("(", "ON");
	break; case 397:
		COMPLETE_WITH("ndistinct", "dependencies", "mcv");
	break; case 398:
		COMPLETE_WITH("ON");
	break; case 399:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);

/* CREATE TABLE --- is allowed inside CREATE SCHEMA, so use TailMatches */
	/* Complete "CREATE TEMP/TEMPORARY" with the possible temp objects */
	break; case 400:
		COMPLETE_WITH("SEQUENCE", "TABLE", "VIEW");
	/* Complete "CREATE UNLOGGED" with TABLE or SEQUENCE */
	break; case 401:
		COMPLETE_WITH("TABLE", "SEQUENCE");
	/* Complete PARTITION BY with RANGE ( or LIST ( or ... */
	break; case 402:
		COMPLETE_WITH("RANGE (", "LIST (", "HASH (");
	/* If we have xxx PARTITION OF, provide a list of partitioned tables */
	break; case 403:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_partitioned_tables);
	/* Limited completion support for partition bound specification */
	break; case 404:
		COMPLETE_WITH("FOR VALUES", "DEFAULT");
	/* Complete CREATE TABLE <name> with '(', AS, OF or PARTITION OF */
	break; case 405:
#line 3869 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(", "AS", "OF", "PARTITION OF");
	/* Complete CREATE TABLE <name> OF with list of composite types */
	break; case 406:
#line 3873 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_composite_datatypes);
	/* Complete CREATE TABLE <name> [ (...) ] AS with list of keywords */
	break; case 407:
#line 3879 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("EXECUTE", "SELECT", "TABLE", "VALUES", "WITH");
	/* Complete CREATE TABLE name (...) with supported options */
	break; case 408:
		COMPLETE_WITH("AS", "INHERITS (", "PARTITION BY", "USING", "TABLESPACE", "WITH (");
	break; case 409:
		COMPLETE_WITH("AS", "INHERITS (", "USING", "TABLESPACE", "WITH (");
	break; case 410:
		COMPLETE_WITH("AS", "INHERITS (", "ON COMMIT", "PARTITION BY", "USING",
					  "TABLESPACE", "WITH (");
	/* Complete CREATE TABLE (...) USING with table access methods */
	break; case 411:
#line 3891 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY(Query_for_list_of_table_access_methods);
	/* Complete CREATE TABLE (...) WITH with storage parameters */
	break; case 412:
#line 3895 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_LIST(table_storage_parameters);
	/* Complete CREATE TABLE ON COMMIT with actions */
	break; case 413:
		COMPLETE_WITH("DELETE ROWS", "DROP", "PRESERVE ROWS");

/* CREATE TABLESPACE */
	break; case 414:
		COMPLETE_WITH("OWNER", "LOCATION");
	/* Complete CREATE TABLESPACE name OWNER name with "LOCATION" */
	break; case 415:
		COMPLETE_WITH("LOCATION");

/* CREATE TEXT SEARCH --- is allowed inside CREATE SCHEMA, so use TailMatches */
	break; case 416:
		COMPLETE_WITH("CONFIGURATION", "DICTIONARY", "PARSER", "TEMPLATE");
	break; case 417:
		COMPLETE_WITH("(");

/* CREATE TRANSFORM */
	break; case 418:
#line 3916 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("FOR");
	break; case 419:
#line 3919 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 420:
#line 3922 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("LANGUAGE");
	break; case 421:
#line 3925 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_QUERY(Query_for_list_of_languages);
	}

/* CREATE SUBSCRIPTION */
	break; case 422:
		COMPLETE_WITH("CONNECTION", "SERVER");
	break; case 423:
		COMPLETE_WITH_QUERY(Query_for_list_of_servers);
	break; case 424:
		COMPLETE_WITH("PUBLICATION");
	break; case 425:
		COMPLETE_WITH("PUBLICATION");
	break; case 426:
#line 3941 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/* complete with nothing here as this refers to remote publications */
	}
	break; case 427:
#line 3946 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/* complete with nothing here as this refers to remote publications */
	}
	break; case 428:
		COMPLETE_WITH("WITH (");
	/* Complete "CREATE SUBSCRIPTION <name> ...  WITH ( <opt>" */
	break; case 429:
		COMPLETE_WITH("binary", "conflict_log_destination", "connect", "copy_data",
					  "create_slot", "disable_on_error", "enabled", "failover",
					  "max_retention_duration", "origin",
					  "password_required", "retain_dead_tuples",
					  "run_as_owner", "slot_name", "streaming",
					  "synchronous_commit", "two_phase",
					  "wal_receiver_timeout");

/* CREATE TRIGGER --- is allowed inside CREATE SCHEMA, so use TailMatches */

	/*
	 * Complete CREATE [ OR REPLACE ] TRIGGER <name> with BEFORE|AFTER|INSTEAD
	 * OF.
	 */
	break; case 430:
#line 3969 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("BEFORE", "AFTER", "INSTEAD OF");

	/*
	 * Complete CREATE [ OR REPLACE ] TRIGGER <name> BEFORE,AFTER with an
	 * event.
	 */
	break; case 431:
#line 3977 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("INSERT", "DELETE", "UPDATE", "TRUNCATE");
	/* Complete CREATE [ OR REPLACE ] TRIGGER <name> INSTEAD OF with an event */
	break; case 432:
#line 3981 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("INSERT", "DELETE", "UPDATE");

	/*
	 * Complete CREATE [ OR REPLACE ] TRIGGER <name> BEFORE,AFTER sth with
	 * OR|ON.
	 */
	break; case 433:
#line 3991 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON", "OR");

	/*
	 * Complete CREATE [ OR REPLACE ] TRIGGER <name> BEFORE,AFTER event ON
	 * with a list of tables.  EXECUTE FUNCTION is the recommended grammar
	 * instead of EXECUTE PROCEDURE in version 11 and upwards.
	 */
	break; case 434:
#line 4000 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);

	/*
	 * Complete CREATE [ OR REPLACE ] TRIGGER ... INSTEAD OF event ON with a
	 * list of views.
	 */
	break; case 435:
#line 4008 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_views);
	break; case 436:
#line 4013 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("NOT DEFERRABLE", "DEFERRABLE", "INITIALLY",
						  "REFERENCING", "FOR", "WHEN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("NOT DEFERRABLE", "DEFERRABLE", "INITIALLY",
						  "REFERENCING", "FOR", "WHEN (", "EXECUTE PROCEDURE");
	}
	break; case 437:
#line 4029 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("REFERENCING", "FOR", "WHEN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("REFERENCING", "FOR", "WHEN (", "EXECUTE PROCEDURE");
	}
	break; case 438:
#line 4039 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("OLD TABLE", "NEW TABLE");
	break; case 439:
#line 4044 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS");
	break; case 440:
#line 4053 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("NEW TABLE", "FOR", "WHEN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("NEW TABLE", "FOR", "WHEN (", "EXECUTE PROCEDURE");
	}
	break; case 441:
#line 4067 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("OLD TABLE", "FOR", "WHEN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("OLD TABLE", "FOR", "WHEN (", "EXECUTE PROCEDURE");
	}
	break; case 442:
#line 4089 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("FOR", "WHEN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("FOR", "WHEN (", "EXECUTE PROCEDURE");
	}
	break; case 443:
#line 4099 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("EACH", "ROW", "STATEMENT");
	break; case 444:
#line 4104 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ROW", "STATEMENT");
	break; case 445:
#line 4113 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("WHEN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("WHEN (", "EXECUTE PROCEDURE");
	}
	break; case 446:
#line 4123 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("EXECUTE FUNCTION");
		else
			COMPLETE_WITH("EXECUTE PROCEDURE");
	}

	/*
	 * Complete CREATE [ OR REPLACE ] TRIGGER ... EXECUTE with
	 * PROCEDURE|FUNCTION.
	 */
	break; case 447:
#line 4138 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("FUNCTION");
		else
			COMPLETE_WITH("PROCEDURE");
	}
	break; case 448:
#line 4148 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(Query_for_list_of_functions);

/* CREATE ROLE,USER,GROUP <name> */
	break; case 449:
#line 4152 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("USER", "MAPPING"))
#line 4153 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ADMIN", "BYPASSRLS", "CONNECTION LIMIT", "CREATEDB",
					  "CREATEROLE", "ENCRYPTED PASSWORD", "IN", "INHERIT",
					  "LOGIN", "NOBYPASSRLS",
					  "NOCREATEDB", "NOCREATEROLE", "NOINHERIT",
					  "NOLOGIN", "NOREPLICATION", "NOSUPERUSER", "PASSWORD",
					  "REPLICATION", "ROLE", "SUPERUSER", "SYSID",
					  "VALID UNTIL", "WITH");

/* CREATE ROLE,USER,GROUP <name> WITH */
	break; case 450:
		/* Similar to the above, but don't complete "WITH" again. */
		COMPLETE_WITH("ADMIN", "BYPASSRLS", "CONNECTION LIMIT", "CREATEDB",
					  "CREATEROLE", "ENCRYPTED PASSWORD", "IN", "INHERIT",
					  "LOGIN", "NOBYPASSRLS",
					  "NOCREATEDB", "NOCREATEROLE", "NOINHERIT",
					  "NOLOGIN", "NOREPLICATION", "NOSUPERUSER", "PASSWORD",
					  "REPLICATION", "ROLE", "SUPERUSER", "SYSID",
					  "VALID UNTIL");

	/* complete CREATE ROLE,USER,GROUP <name> IN with ROLE,GROUP */
	break; case 451:
		COMPLETE_WITH("GROUP", "ROLE");

/* CREATE TYPE */
	break; case 452:
		COMPLETE_WITH("(", "AS");
	break; case 453:
		COMPLETE_WITH("ENUM", "RANGE", "(");
	break; case 454:
	{
		if (TailMatches("(|*,", MatchAny))
			COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
		else if (TailMatches("(|*,", MatchAny, MatchAnyExcept("*)")))
			COMPLETE_WITH("COLLATE", ",", ")");
	}
	break; case 455:
		COMPLETE_WITH("(");
	break; case 456:
	{
		if (TailMatches("(|*,"))
			COMPLETE_WITH("INPUT", "OUTPUT", "RECEIVE", "SEND",
						  "TYPMOD_IN", "TYPMOD_OUT", "ANALYZE", "SUBSCRIPT",
						  "INTERNALLENGTH", "PASSEDBYVALUE", "ALIGNMENT",
						  "STORAGE", "LIKE", "CATEGORY", "PREFERRED",
						  "DEFAULT", "ELEMENT", "DELIMITER",
						  "COLLATABLE");
		else if (TailMatches("(*|*,", MatchAnyExcept("*=")))
			COMPLETE_WITH("=");
		else if (TailMatches("=", MatchAnyExcept("*)")))
			COMPLETE_WITH(",", ")");
	}
	break; case 457:
	{
		if (TailMatches("(|*,"))
			COMPLETE_WITH("SUBTYPE", "SUBTYPE_OPCLASS", "COLLATION",
						  "CANONICAL", "SUBTYPE_DIFF",
						  "MULTIRANGE_TYPE_NAME");
		else if (TailMatches("(*|*,", MatchAnyExcept("*=")))
			COMPLETE_WITH("=");
		else if (TailMatches("=", MatchAnyExcept("*)")))
			COMPLETE_WITH(",", ")");
	}

/* CREATE VIEW --- is allowed inside CREATE SCHEMA, so use TailMatches */
	/* Complete CREATE [ OR REPLACE ] VIEW <name> with AS or WITH */
	break; case 458:
#line 4220 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS", "WITH");
	/* Complete "CREATE [ OR REPLACE ] VIEW <sth> AS with "SELECT" */
	break; case 459:
#line 4224 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SELECT");
	/* CREATE [ OR REPLACE ] VIEW <name> WITH ( yyy [= zzz] ) */
	break; case 460:
#line 4228 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(");
	break; case 461:
#line 4231 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_LIST(view_optional_parameters);
	break; case 462:
#line 4234 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("=");
	break; case 463:
#line 4237 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("local", "cascaded");
	/* CREATE [ OR REPLACE ] VIEW <name> WITH ( ... ) AS */
	break; case 464:
#line 4241 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS");
	/* CREATE [ OR REPLACE ] VIEW <name> WITH ( ... ) AS SELECT */
	break; case 465:
#line 4245 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SELECT");

/* CREATE MATERIALIZED VIEW */
	break; case 466:
		COMPLETE_WITH("VIEW");
	/* Complete CREATE MATERIALIZED VIEW <name> with AS or USING */
	break; case 467:
		COMPLETE_WITH("AS", "USING");

	/*
	 * Complete CREATE MATERIALIZED VIEW <name> USING with list of access
	 * methods
	 */
	break; case 468:
		COMPLETE_WITH_QUERY(Query_for_list_of_table_access_methods);
	/* Complete CREATE MATERIALIZED VIEW <name> USING <access method> with AS */
	break; case 469:
		COMPLETE_WITH("AS");

	/*
	 * Complete CREATE MATERIALIZED VIEW <name> [USING <access method> ] AS
	 * with "SELECT"
	 */
	break; case 470:
#line 4270 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SELECT");

/* CREATE EVENT TRIGGER */
	break; case 471:
		COMPLETE_WITH("TRIGGER");
	/* Complete CREATE EVENT TRIGGER <name> with ON */
	break; case 472:
		COMPLETE_WITH("ON");
	/* Complete CREATE EVENT TRIGGER <name> ON with event_type */
	break; case 473:
		COMPLETE_WITH("ddl_command_start", "ddl_command_end", "login",
					  "sql_drop", "table_rewrite");

	/*
	 * Complete CREATE EVENT TRIGGER <name> ON <event_type>.  EXECUTE FUNCTION
	 * is the recommended grammar instead of EXECUTE PROCEDURE in version 11
	 * and upwards.
	 */
	break; case 474:
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("WHEN TAG IN (", "EXECUTE FUNCTION");
		else
			COMPLETE_WITH("WHEN TAG IN (", "EXECUTE PROCEDURE");
	}
	break; case 475:
	{
		if (pset.sversion >= 110000)
			COMPLETE_WITH("EXECUTE FUNCTION");
		else
			COMPLETE_WITH("EXECUTE PROCEDURE");
	}
	break; case 476:
		COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(Query_for_list_of_functions);

/* DEALLOCATE */
	break; case 477:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_prepared_statements,
								 "ALL");

/* DECLARE */

	/*
	 * Complete DECLARE <name> with one of BINARY, ASENSITIVE, INSENSITIVE,
	 * SCROLL, NO SCROLL, and CURSOR.
	 */
	break; case 478:
		COMPLETE_WITH("BINARY", "ASENSITIVE", "INSENSITIVE", "SCROLL", "NO SCROLL",
					  "CURSOR");

	/*
	 * Complete DECLARE ... <option> with other options. The PostgreSQL parser
	 * allows DECLARE options to be specified in any order. But the
	 * tab-completion follows the ordering of them that the SQL standard
	 * provides, like the syntax of DECLARE command in the documentation
	 * indicates.
	 */
	break; case 479:
		COMPLETE_WITH("ASENSITIVE", "INSENSITIVE", "SCROLL", "NO SCROLL", "CURSOR");
	break; case 480:
		COMPLETE_WITH("SCROLL", "NO SCROLL", "CURSOR");
	break; case 481:
		COMPLETE_WITH("CURSOR");
	/* Complete DECLARE ... [options] NO with SCROLL */
	break; case 482:
		COMPLETE_WITH("SCROLL");

	/*
	 * Complete DECLARE ... CURSOR with one of WITH HOLD, WITHOUT HOLD, and
	 * FOR
	 */
	break; case 483:
		COMPLETE_WITH("WITH HOLD", "WITHOUT HOLD", "FOR");
	/* Complete DECLARE ... CURSOR WITH|WITHOUT with HOLD */
	break; case 484:
		COMPLETE_WITH("HOLD");
	/* Complete DECLARE ... CURSOR WITH|WITHOUT HOLD with FOR */
	break; case 485:
		COMPLETE_WITH("FOR");

/* DELETE --- can be inside EXPLAIN, RULE, etc */
	/* Complete DELETE with "FROM" */
	break; case 486:
		COMPLETE_WITH("FROM");
	/* Complete DELETE FROM with a list of tables */
	break; case 487:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_updatables);
	/* Complete DELETE FROM <table> */
	break; case 488:
		COMPLETE_WITH("FOR", "USING", "WHERE");
	/* Complete DELETE FROM <table> FOR with PORTION */
	break; case 489:
		COMPLETE_WITH("PORTION");
	/* Complete DELETE FROM <table> FOR PORTION with OF */
	break; case 490:
		COMPLETE_WITH("OF");
	/* Complete DELETE FROM <table> FOR PORTION OF with column names */
	break; case 491:
		COMPLETE_WITH_ATTR(prev4_wd);
	/* Complete DELETE FROM <table> FOR PORTION OF <period> with FROM */
	break; case 492:
		COMPLETE_WITH("FROM");
	/* Complete DELETE FROM <table> USING with relations supporting SELECT */
	break; case 493:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_selectables);

/* DISCARD */
	break; case 494:
		COMPLETE_WITH("ALL", "PLANS", "SEQUENCES", "TEMP");

/* DO */
	break; case 495:
		COMPLETE_WITH("LANGUAGE");

/* DROP */
	/* Complete DROP object with CASCADE / RESTRICT */
	break; case 496:
#line 4394 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("CASCADE", "RESTRICT");
	break; case 497:
#line 4396 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (ends_with(prev_wd, ')'))
#line 4397 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* help completing some of the variants */
	break; case 498:
		COMPLETE_WITH("(");
	break; case 499:
		COMPLETE_WITH_FUNCTION_ARG(prev2_wd);
	break; case 500:
		COMPLETE_WITH("DATA WRAPPER", "TABLE");
	break; case 501:
		COMPLETE_WITH("WITH (");
	break; case 502:
#line 4408 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if ((ends_with(prev_wd, '(')))
		COMPLETE_WITH("FORCE");

	/* DROP INDEX */
	break; case 503:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_indexes,
										"CONCURRENTLY");
	break; case 504:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_indexes);
	break; case 505:
		COMPLETE_WITH("CASCADE", "RESTRICT");
	break; case 506:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP MATERIALIZED VIEW */
	break; case 507:
		COMPLETE_WITH("VIEW");
	break; case 508:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_matviews);
	break; case 509:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP OWNED BY */
	break; case 510:
		COMPLETE_WITH("BY");
	break; case 511:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	break; case 512:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP TEXT SEARCH */
	break; case 513:
		COMPLETE_WITH("CONFIGURATION", "DICTIONARY", "PARSER", "TEMPLATE");

	/* DROP TRIGGER */
	break; case 514:
		COMPLETE_WITH("ON");
	break; case 515:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_trigger);
	}
	break; case 516:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP ACCESS METHOD */
	break; case 517:
		COMPLETE_WITH("METHOD");
	break; case 518:
		COMPLETE_WITH_QUERY(Query_for_list_of_access_methods);

	/* DROP EVENT TRIGGER */
	break; case 519:
		COMPLETE_WITH("TRIGGER");
	break; case 520:
		COMPLETE_WITH_QUERY(Query_for_list_of_event_triggers);

	/* DROP POLICY <name>  */
	break; case 521:
		COMPLETE_WITH_QUERY(Query_for_list_of_policies);
	/* DROP POLICY <name> ON */
	break; case 522:
		COMPLETE_WITH("ON");
	/* DROP POLICY <name> ON <table> */
	break; case 523:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_policy);
	}
	break; case 524:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP PROPERTY GRAPH */
	break; case 525:
		COMPLETE_WITH("GRAPH");
	break; case 526:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_propgraphs);
	break; case 527:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP RULE */
	break; case 528:
		COMPLETE_WITH("ON");
	break; case 529:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables_for_rule);
	}
	break; case 530:
		COMPLETE_WITH("CASCADE", "RESTRICT");

	/* DROP TRANSFORM */
	break; case 531:
		COMPLETE_WITH("FOR");
	break; case 532:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 533:
		COMPLETE_WITH("LANGUAGE");
	break; case 534:
	{
		set_completion_reference(prev2_wd);
		COMPLETE_WITH_QUERY(Query_for_list_of_languages);
	}
	break; case 535:
		COMPLETE_WITH("CASCADE", "RESTRICT");

/* EXECUTE */
	break; case 536:
		COMPLETE_WITH_QUERY(Query_for_list_of_prepared_statements);

/*
 * EXPLAIN [ ( option [, ...] ) ] statement
 * EXPLAIN [ ANALYZE ] [ VERBOSE ] statement
 */
	break; case 537:
		COMPLETE_WITH("SELECT", "INSERT INTO", "DELETE FROM", "UPDATE", "DECLARE",
					  "MERGE INTO", "EXECUTE", "ANALYZE", "VERBOSE");
	break; case 538:
#line 4526 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("EXPLAIN", "(*)"))
#line 4527 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("ANALYZE", "VERBOSE", "COSTS", "SETTINGS", "GENERIC_PLAN",
						  "BUFFERS", "SERIALIZE", "WAL", "TIMING", "SUMMARY",
						  "MEMORY", "IO", "FORMAT");
		else if (TailMatches("ANALYZE|VERBOSE|COSTS|SETTINGS|GENERIC_PLAN|BUFFERS|WAL|TIMING|SUMMARY|MEMORY|IO"))
			COMPLETE_WITH("ON", "OFF");
		else if (TailMatches("SERIALIZE"))
			COMPLETE_WITH("TEXT", "NONE", "BINARY");
		else if (TailMatches("FORMAT"))
			COMPLETE_WITH("TEXT", "XML", "JSON", "YAML");
	}
	break; case 539:
		COMPLETE_WITH("SELECT", "INSERT INTO", "DELETE FROM", "UPDATE", "DECLARE",
					  "MERGE INTO", "EXECUTE", "VERBOSE");
	break; case 540:
#line 4550 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SELECT", "INSERT INTO", "DELETE FROM", "UPDATE", "DECLARE",
					  "MERGE INTO", "EXECUTE");

/* FETCH && MOVE */

	/*
	 * Complete FETCH with one of ABSOLUTE, BACKWARD, FORWARD, RELATIVE, ALL,
	 * NEXT, PRIOR, FIRST, LAST, FROM, IN, and a list of cursors
	 */
	break; case 541:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_cursors,
								 "ABSOLUTE",
								 "BACKWARD",
								 "FORWARD",
								 "RELATIVE",
								 "ALL",
								 "NEXT",
								 "PRIOR",
								 "FIRST",
								 "LAST",
								 "FROM",
								 "IN");

	/*
	 * Complete FETCH BACKWARD or FORWARD with one of ALL, FROM, IN, and a
	 * list of cursors
	 */
	break; case 542:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_cursors,
								 "ALL",
								 "FROM",
								 "IN");

	/*
	 * Complete FETCH <direction> with "FROM" or "IN". These are equivalent,
	 * but we may as well tab-complete both: perhaps some users prefer one
	 * variant or the other.
	 */
	break; case 543:
#line 4591 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_cursors,
								 "FROM",
								 "IN");
	/* Complete FETCH <direction> "FROM" or "IN" with a list of cursors */
	break; case 544:
		COMPLETE_WITH_QUERY(Query_for_list_of_cursors);

/* FOREIGN DATA WRAPPER */
	/* applies in ALTER/DROP FDW and in CREATE SERVER */
	break; case 545:
#line 4601 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("CREATE", MatchAny, MatchAny, MatchAny))
#line 4602 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY(Query_for_list_of_fdws);
	/* applies in CREATE SERVER */
	break; case 546:
		COMPLETE_WITH("OPTIONS");

/* FOREIGN TABLE */
	break; case 547:
#line 4609 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("CREATE", MatchAny, MatchAny))
#line 4610 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_foreign_tables);

/* FOREIGN SERVER */
	break; case 548:
		COMPLETE_WITH_QUERY(Query_for_list_of_servers);

/*
 * GRANT and REVOKE are allowed inside CREATE SCHEMA and
 * ALTER DEFAULT PRIVILEGES, so use TailMatches
 */
	/* Complete GRANT/REVOKE with a list of roles and privileges */
	break; case 549:
#line 4623 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * With ALTER DEFAULT PRIVILEGES, restrict completion to grantable
		 * privileges (can't grant roles)
		 */
		if (HeadMatches("ALTER", "DEFAULT", "PRIVILEGES"))
		{
			if (TailMatches("GRANT") ||
				TailMatches("REVOKE", "GRANT", "OPTION", "FOR"))
				COMPLETE_WITH("SELECT", "INSERT", "UPDATE",
							  "DELETE", "TRUNCATE", "REFERENCES", "TRIGGER",
							  "CREATE", "EXECUTE", "USAGE", "MAINTAIN", "ALL");
			else if (TailMatches("REVOKE"))
				COMPLETE_WITH("SELECT", "INSERT", "UPDATE",
							  "DELETE", "TRUNCATE", "REFERENCES", "TRIGGER",
							  "CREATE", "EXECUTE", "USAGE", "MAINTAIN", "ALL",
							  "GRANT OPTION FOR");
		}
		else if (TailMatches("GRANT"))
			COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
									 Privilege_options_of_grant_and_revoke);
		else if (TailMatches("REVOKE"))
			COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
									 Privilege_options_of_grant_and_revoke,
									 "GRANT OPTION FOR",
									 "ADMIN OPTION FOR",
									 "INHERIT OPTION FOR",
									 "SET OPTION FOR");
		else if (TailMatches("REVOKE", "GRANT", "OPTION", "FOR"))
			COMPLETE_WITH(Privilege_options_of_grant_and_revoke);
		else if (TailMatches("REVOKE", "ADMIN|INHERIT|SET", "OPTION", "FOR"))
			COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	}

	break; case 550:
#line 4659 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SYSTEM");

	break; case 551:
		COMPLETE_WITH("ON PARAMETER", "OPTION FOR");
	break; case 552:
#line 4667 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON PARAMETER");

	break; case 553:
#line 4673 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_VERBATIM(Query_for_list_of_alter_system_set_vars);

	break; case 554:
#line 4677 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("TO");

	break; case 555:
#line 4683 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("FROM");

	/*
	 * Complete GRANT/REVOKE <privilege> with "ON", GRANT/REVOKE <role> with
	 * TO/FROM
	 */
	break; case 556:
#line 4691 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("SELECT|INSERT|UPDATE|DELETE|TRUNCATE|REFERENCES|TRIGGER|CREATE|CONNECT|TEMPORARY|TEMP|EXECUTE|USAGE|MAINTAIN|ALL"))
			COMPLETE_WITH("ON");
		else if (TailMatches("GRANT", MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/*
	 * Complete GRANT/REVOKE <sth> ON with a list of appropriate relations.
	 *
	 * Note: GRANT/REVOKE can get quite complex; tab-completion as implemented
	 * here will only work if the privilege list contains exactly one
	 * privilege.
	 */
	break; case 557:
#line 4709 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * With ALTER DEFAULT PRIVILEGES, restrict completion to the kinds of
		 * objects supported.
		 */
		if (HeadMatches("ALTER", "DEFAULT", "PRIVILEGES"))
			COMPLETE_WITH("TABLES", "SEQUENCES", "FUNCTIONS", "PROCEDURES", "ROUTINES", "TYPES", "SCHEMAS", "LARGE OBJECTS");
		else
			COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_grantables,
											"ALL FUNCTIONS IN SCHEMA",
											"ALL PROCEDURES IN SCHEMA",
											"ALL ROUTINES IN SCHEMA",
											"ALL SEQUENCES IN SCHEMA",
											"ALL TABLES IN SCHEMA",
											"DATABASE",
											"DOMAIN",
											"FOREIGN DATA WRAPPER",
											"FOREIGN SERVER",
											"FUNCTION",
											"LANGUAGE",
											"LARGE OBJECT",
											"PARAMETER",
											"PROCEDURE",
											"PROPERTY GRAPH",
											"ROUTINE",
											"SCHEMA",
											"SEQUENCE",
											"TABLE",
											"TABLESPACE",
											"TYPE");
	}
	break; case 558:
#line 4742 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("FUNCTIONS IN SCHEMA",
					  "PROCEDURES IN SCHEMA",
					  "ROUTINES IN SCHEMA",
					  "SEQUENCES IN SCHEMA",
					  "TABLES IN SCHEMA");

	/*
	 * Complete "GRANT/REVOKE * ON DATABASE/DOMAIN/..." with a list of
	 * appropriate objects or keywords.
	 *
	 * Complete "GRANT/REVOKE * ON *" with "TO/FROM".
	 */
	break; case 559:
#line 4756 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("DATABASE"))
			COMPLETE_WITH_QUERY(Query_for_list_of_databases);
		else if (TailMatches("DOMAIN"))
			COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_domains);
		else if (TailMatches("FUNCTION"))
			COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(Query_for_list_of_functions);
		else if (TailMatches("FOREIGN"))
			COMPLETE_WITH("DATA WRAPPER", "SERVER");
		else if (TailMatches("LANGUAGE"))
			COMPLETE_WITH_QUERY(Query_for_list_of_languages);
		else if (TailMatches("LARGE"))
		{
			if (HeadMatches("ALTER", "DEFAULT", "PRIVILEGES"))
				COMPLETE_WITH("OBJECTS");
			else
				COMPLETE_WITH("OBJECT");
		}
		else if (TailMatches("PROCEDURE"))
			COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(Query_for_list_of_procedures);
		else if (TailMatches("ROUTINE"))
			COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_routines);
		else if (TailMatches("SCHEMA"))
			COMPLETE_WITH_QUERY(Query_for_list_of_schemas);
		else if (TailMatches("SEQUENCE"))
			COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_sequences);
		else if (TailMatches("TABLE"))
			COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_grantables);
		else if (TailMatches("TABLESPACE"))
			COMPLETE_WITH_QUERY(Query_for_list_of_tablespaces);
		else if (TailMatches("TYPE"))
			COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
		else if (TailMatches("GRANT", MatchAny, MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/*
	 * Complete "GRANT/REVOKE ... TO/FROM" with username, PUBLIC,
	 * CURRENT_ROLE, CURRENT_USER, or SESSION_USER.
	 */
	break; case 560:
#line 4800 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_grant_roles);

	/*
	 * Offer grant options after that.
	 */
	break; case 561:
		COMPLETE_WITH("WITH ADMIN",
					  "WITH INHERIT",
					  "WITH SET",
					  "WITH GRANT OPTION",
					  "GRANTED BY");
	break; case 562:
		COMPLETE_WITH("ADMIN",
					  "INHERIT",
					  "SET",
					  "GRANT OPTION");
	break; case 563:
		COMPLETE_WITH("OPTION", "TRUE", "FALSE");
	break; case 564:
		COMPLETE_WITH("GRANTED BY");
	break; case 565:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_grant_roles);
	/* Complete "ALTER DEFAULT PRIVILEGES ... GRANT/REVOKE ... TO/FROM */
	break; case 566:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_grant_roles);
	/* Offer WITH GRANT OPTION after that */
	break; case 567:
		COMPLETE_WITH("WITH GRANT OPTION");
	/* Complete "GRANT/REVOKE ... ON * *" with TO/FROM */
	break; case 568:
#line 4833 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("FOREIGN", "SERVER") && !TailMatches("LARGE", "OBJECT"))
#line 4834 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (Matches("GRANT", MatchAnyN, "ON", MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/* Complete "GRANT/REVOKE * ON ALL * IN SCHEMA *" with TO/FROM */
	break; case 569:
#line 4844 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("GRANT", MatchAny, MatchAny, MatchAny, MatchAny, MatchAny, MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/* Complete "GRANT/REVOKE * ON FOREIGN DATA WRAPPER *" with TO/FROM */
	break; case 570:
#line 4854 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("GRANT", MatchAny, MatchAny, MatchAny, MatchAny, MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/* Complete "GRANT/REVOKE * ON FOREIGN SERVER *" with TO/FROM */
	break; case 571:
#line 4864 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("GRANT", MatchAny, MatchAny, MatchAny, MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/* Complete "GRANT/REVOKE * ON LARGE OBJECT *" with TO/FROM */
	break; case 572:
#line 4874 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("GRANT", MatchAny, MatchAny, MatchAny, MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

	/* Complete "GRANT/REVOKE * ON LARGE OBJECTS" with TO/FROM */
	break; case 573:
#line 4884 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (TailMatches("GRANT", MatchAny, MatchAny, MatchAny, MatchAny))
			COMPLETE_WITH("TO");
		else
			COMPLETE_WITH("FROM");
	}

/* GRAPH_TABLE */
	break; case 574:
		COMPLETE_WITH("(");
	break; case 575:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_propgraphs);
	break; case 576:
		COMPLETE_WITH("MATCH");

/* GROUP BY */
	break; case 577:
		COMPLETE_WITH("BY");

/* IMPORT FOREIGN SCHEMA */
	break; case 578:
		COMPLETE_WITH("FOREIGN SCHEMA");
	break; case 579:
		COMPLETE_WITH("SCHEMA");
	break; case 580:
		COMPLETE_WITH("EXCEPT (", "FROM SERVER", "LIMIT TO (");
	break; case 581:
#line 4912 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("FROM SERVER");
	break; case 582:
		COMPLETE_WITH("INTO");
	break; case 583:
		COMPLETE_WITH_QUERY(Query_for_list_of_schemas);
	break; case 584:
		COMPLETE_WITH("OPTIONS (");

/* INSERT --- can be inside EXPLAIN, RULE, etc */
	/* Complete NOT MATCHED THEN INSERT */
	break; case 585:
		COMPLETE_WITH("VALUES", "(");
	/* Complete INSERT with "INTO" */
	break; case 586:
		COMPLETE_WITH("INTO");
	/* Complete INSERT INTO with table names */
	break; case 587:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_updatables);
	/* Complete "INSERT INTO <table> (" with attribute names */
	break; case 588:
		COMPLETE_WITH_ATTR(prev2_wd);

	/*
	 * Complete INSERT INTO <table> with "(" or "VALUES" or "SELECT" or
	 * "TABLE" or "DEFAULT VALUES" or "OVERRIDING"
	 */
	break; case 589:
		COMPLETE_WITH("(", "DEFAULT VALUES", "SELECT", "TABLE", "VALUES", "OVERRIDING");

	/*
	 * Complete INSERT INTO <table> (attribs) with "VALUES" or "SELECT" or
	 * "TABLE" or "OVERRIDING"
	 */
	break; case 590:
#line 4946 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (ends_with(prev_wd, ')'))
#line 4947 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("SELECT", "TABLE", "VALUES", "OVERRIDING");

	/* Complete OVERRIDING */
	break; case 591:
		COMPLETE_WITH("SYSTEM VALUE", "USER VALUE");

	/* Complete after OVERRIDING clause */
	break; case 592:
		COMPLETE_WITH("SELECT", "TABLE", "VALUES");

	/* Insert an open parenthesis after "VALUES" */
	break; case 593:
#line 4958 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("DEFAULT", "VALUES"))
		COMPLETE_WITH("(");

/* LOCK */
	/* Complete LOCK [TABLE] [ONLY] with a list of tables */
	break; case 594:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_tables,
										"TABLE", "ONLY");
	break; case 595:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_tables,
										"ONLY");
	break; case 596:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	/* For the following, handle the case of a single table only for now */

	/* Complete LOCK [TABLE] [ONLY] <table> with IN or NOWAIT */
	break; case 597:
#line 4978 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("IN", "NOWAIT");

	/* Complete LOCK [TABLE] [ONLY] <table> IN with a lock mode */
	break; case 598:
		COMPLETE_WITH("ACCESS SHARE MODE",
					  "ROW SHARE MODE", "ROW EXCLUSIVE MODE",
					  "SHARE UPDATE EXCLUSIVE MODE", "SHARE MODE",
					  "SHARE ROW EXCLUSIVE MODE",
					  "EXCLUSIVE MODE", "ACCESS EXCLUSIVE MODE");

	/*
	 * Complete LOCK [TABLE][ONLY] <table> IN ACCESS|ROW with rest of lock
	 * mode
	 */
	break; case 599:
		COMPLETE_WITH("EXCLUSIVE MODE", "SHARE MODE");

	/* Complete LOCK [TABLE] [ONLY] <table> IN SHARE with rest of lock mode */
	break; case 600:
		COMPLETE_WITH("MODE", "ROW EXCLUSIVE MODE",
					  "UPDATE EXCLUSIVE MODE");

	/* Complete LOCK [TABLE] [ONLY] <table> [IN lockmode MODE] with "NOWAIT" */
	break; case 601:
		COMPLETE_WITH("NOWAIT");

/* MERGE --- can be inside EXPLAIN */
	break; case 602:
		COMPLETE_WITH("INTO");
	break; case 603:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_mergetargets);

	/* Complete MERGE INTO <table> [[AS] <alias>] with USING */
	break; case 604:
		COMPLETE_WITH("USING", "AS");
	break; case 605:
#line 5015 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("USING");

	/*
	 * Complete MERGE INTO ... USING with a list of relations supporting
	 * SELECT
	 */
	break; case 606:
#line 5024 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_selectables);

	/*
	 * Complete MERGE INTO <table> [[AS] <alias>] USING <relations> [[AS]
	 * alias] with ON
	 */
	break; case 607:
#line 5033 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("AS", "ON");
	break; case 608:
#line 5040 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON");

	/* Complete MERGE INTO ... ON with target table attributes */
	break; case 609:
		COMPLETE_WITH_ATTR(prev4_wd);
	break; case 610:
		COMPLETE_WITH_ATTR(prev8_wd);
	break; case 611:
		COMPLETE_WITH_ATTR(prev6_wd);

	/*
	 * Complete ... USING <relation> [[AS] alias] ON join condition
	 * (consisting of one or three words typically used) with WHEN [NOT]
	 * MATCHED
	 */
	break; case 612:
#line 5061 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("WHEN MATCHED", "WHEN NOT MATCHED");
	break; case 613:
#line 5068 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("MATCHED", "NOT MATCHED");

	/*
	 * Complete ... WHEN MATCHED and WHEN NOT MATCHED BY SOURCE|TARGET with
	 * THEN/AND
	 */
	break; case 614:
#line 5076 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("THEN", "AND");

	/* Complete ... WHEN NOT MATCHED with BY/THEN/AND */
	break; case 615:
		COMPLETE_WITH("BY", "THEN", "AND");

	/* Complete ... WHEN NOT MATCHED BY with SOURCE/TARGET */
	break; case 616:
		COMPLETE_WITH("SOURCE", "TARGET");

	/*
	 * Complete ... WHEN MATCHED THEN and WHEN NOT MATCHED BY SOURCE THEN with
	 * UPDATE SET/DELETE/DO NOTHING
	 */
	break; case 617:
#line 5092 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("UPDATE SET", "DELETE", "DO NOTHING");

	/*
	 * Complete ... WHEN NOT MATCHED [BY TARGET] THEN with INSERT/DO NOTHING
	 */
	break; case 618:
#line 5099 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("INSERT", "DO NOTHING");

/* NOTIFY --- can be inside EXPLAIN, RULE, etc */
	break; case 619:
		COMPLETE_WITH_QUERY(Query_for_list_of_channels);

/* OPTIONS */
	break; case 620:
		COMPLETE_WITH("(");

/* OWNER TO  - complete with available roles */
	break; case 621:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 Keywords_for_list_of_owner_roles);

/* ORDER BY */
	break; case 622:
		COMPLETE_WITH("BY");
	break; case 623:
		COMPLETE_WITH_ATTR(prev3_wd);

/* PREPARE xx AS */
	break; case 624:
		COMPLETE_WITH("SELECT", "UPDATE", "INSERT INTO", "DELETE FROM",
					  "MERGE INTO", "VALUES", "WITH", "TABLE");

/*
 * PREPARE TRANSACTION is missing on purpose. It's intended for transaction
 * managers, not for manual use in interactive sessions.
 */

/* REASSIGN OWNED BY xxx TO yyy */
	break; case 625:
		COMPLETE_WITH("OWNED BY");
	break; case 626:
		COMPLETE_WITH("BY");
	break; case 627:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	break; case 628:
		COMPLETE_WITH("TO");
	break; case 629:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);

/* REFRESH MATERIALIZED VIEW */
	break; case 630:
		COMPLETE_WITH("MATERIALIZED VIEW");
	break; case 631:
		COMPLETE_WITH("VIEW");
	break; case 632:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_matviews,
										"CONCURRENTLY");
	break; case 633:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_matviews);
	break; case 634:
		COMPLETE_WITH("WITH");
	break; case 635:
		COMPLETE_WITH("WITH");
	break; case 636:
		COMPLETE_WITH("NO DATA", "DATA");
	break; case 637:
		COMPLETE_WITH("NO DATA", "DATA");
	break; case 638:
		COMPLETE_WITH("DATA");
	break; case 639:
		COMPLETE_WITH("DATA");

/* REINDEX */
	break; case 640:
#line 5168 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("TABLE", "INDEX", "SYSTEM", "SCHEMA", "DATABASE");
	break; case 641:
#line 5171 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_indexables,
										"CONCURRENTLY");
	break; case 642:
#line 5175 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_indexes,
										"CONCURRENTLY");
	break; case 643:
#line 5179 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_schemas,
								 "CONCURRENTLY");
	break; case 644:
#line 5183 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_databases,
								 "CONCURRENTLY");
	break; case 645:
#line 5187 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_indexables);
	break; case 646:
#line 5190 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_indexes);
	break; case 647:
#line 5193 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY(Query_for_list_of_schemas);
	break; case 648:
#line 5196 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY(Query_for_list_of_databases);
	break; case 649:
#line 5198 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("REINDEX", "(*)"))
#line 5199 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("CONCURRENTLY", "TABLESPACE", "VERBOSE");
		else if (TailMatches("TABLESPACE"))
			COMPLETE_WITH_QUERY(Query_for_list_of_tablespaces);
	}

/* REPACK */
	break; case 650:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_clusterables,
										"(", "USING INDEX");
	break; case 651:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_clusterables,
										"USING INDEX");
	break; case 652:
		COMPLETE_WITH("USING INDEX");
	break; case 653:
		COMPLETE_WITH("USING INDEX");
	break; case 654:
#line 5224 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_index_of_table);
	}

	/*
	 * Complete ... [ (*) ] <sth> USING INDEX, with a list of indexes for
	 * <sth>.
	 */
	break; case 655:
	{
		set_completion_reference(prev3_wd);
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_index_of_table);
	}
	break; case 656:
#line 5239 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("REPACK", "(*)"))
#line 5240 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("ANALYZE", "CONCURRENTLY", "VERBOSE");
		else if (TailMatches("ANALYZE|CONCURRENTLY|VERBOSE"))
			COMPLETE_WITH("ON", "OFF");
	}

/* SECURITY LABEL */
	break; case 657:
		COMPLETE_WITH("LABEL");
	break; case 658:
		COMPLETE_WITH("ON", "FOR");
	break; case 659:
		COMPLETE_WITH("ON");
	break; case 660:
#line 5261 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("TABLE", "COLUMN", "AGGREGATE", "DATABASE", "DOMAIN",
					  "EVENT TRIGGER", "FOREIGN TABLE", "FUNCTION",
					  "LARGE OBJECT", "MATERIALIZED VIEW", "LANGUAGE",
					  "PUBLICATION", "PROCEDURE", "ROLE", "ROUTINE", "SCHEMA",
					  "SEQUENCE", "SUBSCRIPTION", "TABLESPACE", "TYPE", "VIEW");
	break; case 661:
		COMPLETE_WITH("IS");

/* SELECT */
	/* naah . . . */

/* SET, RESET, SHOW */
	/* Complete with a variable name */
	break; case 662:
#line 5276 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("UPDATE", MatchAny, "SET") && !TailMatches("ALTER", "DATABASE|USER|ROLE", MatchAny, "RESET"))
#line 5277 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY_VERBATIM_PLUS(Query_for_list_of_set_vars,
										  "CONSTRAINTS",
										  "TRANSACTION",
										  "SESSION",
										  "ROLE",
										  "TABLESPACE",
										  "ALL");
	break; case 663:
		COMPLETE_WITH_QUERY_VERBATIM_PLUS(Query_for_list_of_show_vars,
										  "SESSION AUTHORIZATION",
										  "ALL");
	break; case 664:
		COMPLETE_WITH("AUTHORIZATION");
	/* Complete "SET TRANSACTION" */
	break; case 665:
		COMPLETE_WITH("SNAPSHOT", "ISOLATION LEVEL", "READ", "DEFERRABLE", "NOT DEFERRABLE");
	break; case 666:
#line 5297 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ISOLATION LEVEL", "READ", "DEFERRABLE", "NOT DEFERRABLE");
	break; case 667:
#line 5301 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("DEFERRABLE");
	break; case 668:
#line 5305 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("LEVEL");
	break; case 669:
#line 5309 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("READ", "REPEATABLE READ", "SERIALIZABLE");
	break; case 670:
#line 5313 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("UNCOMMITTED", "COMMITTED");
	break; case 671:
#line 5317 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("READ");
	break; case 672:
#line 5321 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ONLY", "WRITE");
	/* SET CONSTRAINTS */
	break; case 673:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_constraints_with_schema,
										"ALL");
	/* Complete SET CONSTRAINTS <foo> with DEFERRED|IMMEDIATE */
	break; case 674:
		COMPLETE_WITH("DEFERRED", "IMMEDIATE");
	/* Complete SET ROLE */
	break; case 675:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	/* Complete SET SESSION with AUTHORIZATION or CHARACTERISTICS... */
	break; case 676:
		COMPLETE_WITH("AUTHORIZATION", "CHARACTERISTICS AS TRANSACTION");
	/* Complete SET SESSION AUTHORIZATION with username */
	break; case 677:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 "DEFAULT");
	/* Complete RESET SESSION with AUTHORIZATION */
	break; case 678:
		COMPLETE_WITH("AUTHORIZATION");
	/* Complete SET <var> with "TO" */
	break; case 679:
		COMPLETE_WITH("TO");

	/*
	 * Complete ALTER DATABASE|FUNCTION|PROCEDURE|ROLE|ROUTINE|USER ... SET
	 * <name>
	 */
	break; case 680:
		COMPLETE_WITH("FROM CURRENT", "TO");

	/*
	 * Suggest possible variable values in SET variable TO|=, along with the
	 * preceding ALTER syntaxes.
	 */
	break; case 681:
#line 5358 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("UPDATE", MatchAny, "SET", MatchAny, "TO|="))
#line 5359 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/* special cased code for individual GUCs */
		if (TailMatches("DateStyle", "TO|="))
			COMPLETE_WITH("ISO", "SQL", "Postgres", "German",
						  "YMD", "DMY", "MDY",
						  "US", "European", "NonEuropean",
						  "DEFAULT");
		else if (TailMatches("search_path", "TO|="))
		{
			/* Here, we want to allow pg_catalog, so use narrower exclusion */
			COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_schemas
									 " AND nspname NOT LIKE E'pg\\\\_toast%%'"
									 " AND nspname NOT LIKE E'pg\\\\_temp%%'",
									 "DEFAULT");
		}
		else if (TailMatches("TimeZone", "TO|="))
			COMPLETE_WITH_TIMEZONE_NAME();
		else
		{
			/* generic, type based, GUC support */
			char	   *guctype = get_guctype(prev2_wd);

			/*
			 * Note: if we don't recognize the GUC name, it's important to not
			 * offer any completions, as most likely we've misinterpreted the
			 * context and this isn't a GUC-setting command at all.
			 */
			if (guctype)
			{
				if (strcmp(guctype, "enum") == 0)
				{
					set_completion_reference_verbatim(prev2_wd);
					COMPLETE_WITH_QUERY_PLUS(Query_for_values_of_enum_GUC,
											 "DEFAULT");
				}
				else if (strcmp(guctype, "bool") == 0)
					COMPLETE_WITH("on", "off", "true", "false", "yes", "no",
								  "1", "0", "DEFAULT");
				else
					COMPLETE_WITH("DEFAULT");

				free(guctype);
			}
		}
	}

/* START TRANSACTION */
	break; case 682:
		COMPLETE_WITH("TRANSACTION");

/* TABLE, but not TABLE embedded in other commands */
	break; case 683:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_selectables);

/* TABLESAMPLE */
	break; case 684:
		COMPLETE_WITH_QUERY(Query_for_list_of_tablesample_methods);
	break; case 685:
		COMPLETE_WITH("(");

/* TRUNCATE */
	break; case 686:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_truncatables,
										"TABLE", "ONLY");
	break; case 687:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_truncatables,
										"ONLY");
	break; case 688:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_truncatables);
	break; case 689:
#line 5431 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("RESTART IDENTITY", "CONTINUE IDENTITY", "CASCADE", "RESTRICT");
	break; case 690:
		COMPLETE_WITH("CASCADE", "RESTRICT");

/* UNLISTEN */
	break; case 691:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_channels, "*");

/* UPDATE --- can be inside EXPLAIN, RULE, etc */
	/* If prev. word is UPDATE suggest a list of tables */
	break; case 692:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_updatables);
	/* Complete UPDATE <table> with "SET" or "FOR" (for FOR PORTION OF) */
	break; case 693:
		COMPLETE_WITH("FOR", "SET");
	/* Complete UPDATE <table> FOR with PORTION */
	break; case 694:
		COMPLETE_WITH("PORTION");
	/* Complete UPDATE <table> FOR PORTION with OF */
	break; case 695:
		COMPLETE_WITH("OF");
	/* Complete UPDATE <table> FOR PORTION OF with column names */
	break; case 696:
		COMPLETE_WITH_ATTR(prev4_wd);
	/* Complete UPDATE <table> FOR PORTION OF <period> with FROM */
	break; case 697:
		COMPLETE_WITH("FROM");
	/* Complete UPDATE <table> SET with list of attributes */
	break; case 698:
		COMPLETE_WITH_ATTR(prev2_wd);
	/* UPDATE <table> SET <attr> = */
	break; case 699:
		COMPLETE_WITH("=");

/* USER MAPPING */
	break; case 700:
		COMPLETE_WITH("FOR");
	break; case 701:
		COMPLETE_WITH_QUERY_PLUS(Query_for_list_of_roles,
								 "CURRENT_ROLE",
								 "CURRENT_USER",
								 "PUBLIC",
								 "USER");
	break; case 702:
		COMPLETE_WITH_QUERY(Query_for_list_of_user_mappings);
	break; case 703:
		COMPLETE_WITH("SERVER");
	break; case 704:
		COMPLETE_WITH("OPTIONS");

/*
 * VACUUM [ ( option [, ...] ) ] [ [ ONLY ] table_and_columns [, ...] ]
 * VACUUM [ FULL ] [ FREEZE ] [ VERBOSE ] [ ANALYZE ] [ [ ONLY ] table_and_columns [, ...] ]
 */
	break; case 705:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_vacuumables,
										"(",
										"FULL",
										"FREEZE",
										"VERBOSE",
										"ANALYZE",
										"ONLY");
	break; case 706:
#line 5494 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("VACUUM", "(*)"))
#line 5495 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		/*
		 * This fires if we're in an unfinished parenthesized option list.
		 * get_previous_words treats a completed parenthesized option list as
		 * one word, so the above test is correct.
		 */
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("FULL", "FREEZE", "ANALYZE", "VERBOSE",
						  "DISABLE_PAGE_SKIPPING", "SKIP_LOCKED",
						  "INDEX_CLEANUP", "PROCESS_MAIN", "PROCESS_TOAST",
						  "TRUNCATE", "PARALLEL", "SKIP_DATABASE_STATS",
						  "ONLY_DATABASE_STATS", "BUFFER_USAGE_LIMIT");
		else if (TailMatches("FULL|FREEZE|ANALYZE|VERBOSE|DISABLE_PAGE_SKIPPING|SKIP_LOCKED|PROCESS_MAIN|PROCESS_TOAST|TRUNCATE|SKIP_DATABASE_STATS|ONLY_DATABASE_STATS"))
			COMPLETE_WITH("ON", "OFF");
		else if (TailMatches("INDEX_CLEANUP"))
			COMPLETE_WITH("AUTO", "ON", "OFF");
	}
	break; case 707:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_vacuumables,
										"ONLY");
	break; case 708:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_vacuumables,
										"FREEZE",
										"VERBOSE",
										"ANALYZE",
										"ONLY");
	break; case 709:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_vacuumables,
										"VERBOSE",
										"ANALYZE",
										"ONLY");
	break; case 710:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_vacuumables,
										"ANALYZE",
										"ONLY");
	break; case 711:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_vacuumables,
										"ONLY");
	break; case 712:
		/* "VACUUM (" should be caught above, so assume we want columns */
		COMPLETE_WITH_ATTR(prev2_wd);
	break; case 713:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_vacuumables);

/*
 * WAIT FOR LSN '<lsn>' [ WITH ( option [, ...] ) ]
 * where option can be:
 *   MODE '<mode>'
 *   TIMEOUT '<timeout>'
 *   NO_THROW
 * and mode can be:
 *   standby_replay | standby_write | standby_flush | primary_flush
 */
	break; case 714:
		COMPLETE_WITH("FOR");
	break; case 715:
		COMPLETE_WITH("LSN");
	break; case 716:
		/* No completion for LSN value - user must provide manually */
		;
	break; case 717:
		COMPLETE_WITH("WITH");
	break; case 718:
		COMPLETE_WITH("(");

	/*
	 * Handle parenthesized option list.  This fires when we're in an
	 * unfinished parenthesized option list.  get_previous_words treats a
	 * completed parenthesized option list as one word, so the above test is
	 * correct.
	 *
	 * 'mode' takes a string value (one of the listed above), 'timeout' takes
	 * a string value, and 'no_throw' takes no value.  We do not offer
	 * completions for the *values* of 'timeout' or 'no_throw'.
	 */
	break; case 719:
#line 5571 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!HeadMatches("WAIT", "FOR", "LSN", MatchAny, "WITH", "(*)"))
#line 5572 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	{
		if (ends_with(prev_wd, '(') || ends_with(prev_wd, ','))
			COMPLETE_WITH("mode", "timeout", "no_throw");
		else if (TailMatches("mode"))
			COMPLETE_WITH("'standby_replay'", "'standby_write'", "'standby_flush'", "'primary_flush'");
	}

/* WITH [RECURSIVE] */

	/*
	 * Only match when WITH is the first word, as WITH may appear in many
	 * other contexts.
	 */
	break; case 720:
		COMPLETE_WITH("RECURSIVE");

/* WHERE */
	/* Simple case of the word before the where being the table name */
	break; case 721:
		COMPLETE_WITH_ATTR(prev2_wd);

/* ... FROM ... */
/* TODO: also include SRF ? */
	break; case 722:
#line 5595 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!Matches("COPY|\\copy", MatchAny, "FROM"))
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_selectables);

/* ... JOIN ... */
	break; case 723:
		COMPLETE_WITH_SCHEMA_QUERY_PLUS(Query_for_list_of_selectables, "LATERAL");
	break; case 724:
#line 5601 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("CROSS|NATURAL", "JOIN", MatchAny))
		COMPLETE_WITH("ON", "USING (");
	break; case 725:
#line 5604 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("CROSS|NATURAL", "JOIN", MatchAny, MatchAny) && !TailMatches("ON|USING"))
#line 5605 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON", "USING (");
	break; case 726:
#line 5607 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatches("CROSS|NATURAL", "JOIN", "LATERAL", MatchAny, MatchAny) && !TailMatches("ON|USING"))
#line 5608 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("ON", "USING (");
	break; case 727:
#line 5612 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH("(");
	break; case 728:
		COMPLETE_WITH_ATTR(prev3_wd);
	break; case 729:
		COMPLETE_WITH_ATTR(prev4_wd);

/* ... AT [ LOCAL | TIME ZONE ] ... */
	break; case 730:
		COMPLETE_WITH("LOCAL", "TIME ZONE");
	break; case 731:
		COMPLETE_WITH_TIMEZONE_NAME();

/* Backslash commands */
/* TODO:  \dc \dd \dl */
	break; case 732:
		COMPLETE_WITH_CS("commands", "options", "variables");
	break; case 733:
	{
		if (!recognized_connection_string(text))
			COMPLETE_WITH_QUERY(Query_for_list_of_databases);
	}
	break; case 734:
	{
		if (!recognized_connection_string(prev_wd))
			COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	}
	break; case 735:
		COMPLETE_WITH_VERSIONED_SCHEMA_QUERY(Query_for_list_of_aggregates);
	break; case 736:
#line 5642 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 737:
#line 5645 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_operator_families);
	break; case 738:
		COMPLETE_WITH_QUERY(Query_for_list_of_access_methods);
	break; case 739:
		COMPLETE_WITH_QUERY(Query_for_list_of_tablespaces);
	break; case 740:
		COMPLETE_WITH_QUERY_VERBATIM(Query_for_list_of_show_vars);
	break; case 741:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_domains);
	break; case 742:
		COMPLETE_WITH_QUERY(Query_for_list_of_servers);
	break; case 743:
		COMPLETE_WITH_QUERY(Query_for_list_of_user_mappings);
	break; case 744:
		COMPLETE_WITH_QUERY(Query_for_list_of_fdws);
	break; case 745:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_routines);
	break; case 746:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);

	break; case 747:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_dictionaries);
	break; case 748:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_parsers);
	break; case 749:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_templates);
	/* must be at end of \dF alternatives: */
	break; case 750:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_ts_configurations);

	break; case 751:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_indexes);
	break; case 752:
		COMPLETE_WITH_QUERY(Query_for_list_of_languages);
	break; case 753:
		COMPLETE_WITH_QUERY(Query_for_list_of_schemas);
	/* no support for completing operators, but we can complete types: */
	break; case 754:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 755:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_grantables);
	break; case 756:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_partitioned_indexes);
	break; case 757:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_partitioned_tables);
	break; case 758:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_partitioned_relations);
	break; case 759:
		COMPLETE_WITH_QUERY(Query_for_list_of_publications);
	break; case 760:
		COMPLETE_WITH_QUERY(Query_for_list_of_subscriptions);
	break; case 761:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_sequences);
	break; case 762:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_tables);
	break; case 763:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_datatypes);
	break; case 764:
#line 5705 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	break; case 765:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_views);
	break; case 766:
		COMPLETE_WITH_QUERY(Query_for_list_of_extensions);
	break; case 767:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_statistics);
	break; case 768:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_matviews);
	break; case 769:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_foreign_tables);
	break; case 770:
		COMPLETE_WITH_QUERY(Query_for_list_of_event_triggers);

	/* must be at end of \d alternatives: */
	break; case 771:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_relations);

	break; case 772:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_routines);
	break; case 773:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_views);

	break; case 774:
		COMPLETE_WITH_QUERY_VERBATIM(Query_for_list_of_encodings);
	break; case 775:
		COMPLETE_WITH_LIST(sql_commands);
	break; case 776:
	{
		if (TailMatches("DROP"))
			COMPLETE_WITH_GENERATOR(drop_command_generator);
		else if (TailMatches("ALTER"))
			COMPLETE_WITH_GENERATOR(alter_command_generator);

		/*
		 * CREATE is recognized by tail match elsewhere, so doesn't need to be
		 * repeated here
		 */
	}
	break; case 777:
	{
		if (TailMatches("CREATE|DROP", "ACCESS"))
			COMPLETE_WITH("METHOD");
		else if (TailMatches("ALTER", "DEFAULT"))
			COMPLETE_WITH("PRIVILEGES");
		else if (TailMatches("CREATE|ALTER|DROP", "EVENT"))
			COMPLETE_WITH("TRIGGER");
		else if (TailMatches("CREATE|ALTER|DROP", "FOREIGN"))
			COMPLETE_WITH("DATA WRAPPER", "TABLE");
		else if (TailMatches("ALTER", "LARGE"))
			COMPLETE_WITH("OBJECT");
		else if (TailMatches("CREATE|ALTER|DROP", "MATERIALIZED"))
			COMPLETE_WITH("VIEW");
		else if (TailMatches("CREATE|ALTER|DROP", "PROPERTY"))
			COMPLETE_WITH("GRAPH");
		else if (TailMatches("CREATE|ALTER|DROP", "TEXT"))
			COMPLETE_WITH("SEARCH");
		else if (TailMatches("CREATE|ALTER|DROP", "USER"))
			COMPLETE_WITH("MAPPING FOR");
	}
	break; case 778:
	{
		if (TailMatches("CREATE|ALTER|DROP", "FOREIGN", "DATA"))
			COMPLETE_WITH("WRAPPER");
		else if (TailMatches("CREATE|ALTER|DROP", "TEXT", "SEARCH"))
			COMPLETE_WITH("CONFIGURATION", "DICTIONARY", "PARSER", "TEMPLATE");
		else if (TailMatches("CREATE|ALTER|DROP", "USER", "MAPPING"))
			COMPLETE_WITH("FOR");
	}
	break; case 779:
#line 5774 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
	if (!TailMatchesCS("\\lo*"))
		COMPLETE_WITH_QUERY(Query_for_list_of_databases);
	break; case 780:
		COMPLETE_WITH_QUERY(Query_for_list_of_roles);
	break; case 781:
		COMPLETE_WITH_CS("border", "columns", "csv_fieldsep",
						 "display_false", "display_true", "expanded",
						 "fieldsep", "fieldsep_zero", "footer", "format",
						 "linestyle", "null", "numericlocale",
						 "pager", "pager_min_lines",
						 "recordsep", "recordsep_zero",
						 "tableattr", "title", "tuples_only",
						 "unicode_border_linestyle",
						 "unicode_column_linestyle",
						 "unicode_header_linestyle",
						 "xheader_width");
	break; case 782:
	{
		if (TailMatchesCS("format"))
			COMPLETE_WITH_CS("aligned", "asciidoc", "csv", "html", "latex",
							 "latex-longtable", "troff-ms", "unaligned",
							 "wrapped");
		else if (TailMatchesCS("xheader_width"))
			COMPLETE_WITH_CS("full", "column", "page");
		else if (TailMatchesCS("linestyle"))
			COMPLETE_WITH_CS("ascii", "old-ascii", "unicode");
		else if (TailMatchesCS("pager"))
			COMPLETE_WITH_CS("on", "off", "always");
		else if (TailMatchesCS("unicode_border_linestyle|"
							   "unicode_column_linestyle|"
							   "unicode_header_linestyle"))
			COMPLETE_WITH_CS("single", "double");
	}
	break; case 783:
		matches = complete_from_variables(text, "", "", true);
	break; case 784:
		matches = complete_from_variables(text, "", "", false);
	break; case 785:
	{
		if (TailMatchesCS("AUTOCOMMIT|ON_ERROR_STOP|QUIET|SHOW_ALL_RESULTS|"
						  "SINGLELINE|SINGLESTEP"))
			COMPLETE_WITH_CS("on", "off");
		else if (TailMatchesCS("COMP_KEYWORD_CASE"))
			COMPLETE_WITH_CS("lower", "upper",
							 "preserve-lower", "preserve-upper");
		else if (TailMatchesCS("ECHO"))
			COMPLETE_WITH_CS("errors", "queries", "all", "none");
		else if (TailMatchesCS("ECHO_HIDDEN"))
			COMPLETE_WITH_CS("noexec", "off", "on");
		else if (TailMatchesCS("HISTCONTROL"))
			COMPLETE_WITH_CS("ignorespace", "ignoredups",
							 "ignoreboth", "none");
		else if (TailMatchesCS("ON_ERROR_ROLLBACK"))
			COMPLETE_WITH_CS("on", "off", "interactive");
		else if (TailMatchesCS("SHOW_CONTEXT"))
			COMPLETE_WITH_CS("never", "errors", "always");
		else if (TailMatchesCS("VERBOSITY"))
			COMPLETE_WITH_CS("default", "verbose", "terse", "sqlstate");
	}
	break; case 786:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_routines);
	break; case 787:
		COMPLETE_WITH_SCHEMA_QUERY(Query_for_list_of_views);
	break; case 788:
#line 5841 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"
		COMPLETE_WITH_FILES("\\", false);

	/* gen_tabcomplete.pl ends special processing here */
	break;
	default:
		Assert(false);
		break;
	}
#line 5845 "/home/postgres/Learn-PG/build/../src/bin/psql/tab-complete.in.c"

	return matches;
}


/*
 * GENERATOR FUNCTIONS
 *
 * These functions do all the actual work of completing the input. They get
 * passed the text so far and the count how many times they have been called
 * so far with the same text.
 * If you read the above carefully, you'll see that these don't get called
 * directly but through the readline interface.
 * The return value is expected to be the full completion of the text, going
 * through a list each time, or NULL if there are no more matches. The string
 * will be free()'d by readline, so you must run it through strdup() or
 * something of that sort.
 */

/*
 * Common routine for create_command_generator and drop_command_generator.
 * Entries that have 'excluded' flags are not returned.
 */
static char *
create_or_drop_command_generator(const char *text, int state, uint32 excluded)
{
	static int	list_index,
				string_length;
	const char *name;

	/* If this is the first time for this completion, init some values */
	if (state == 0)
	{
		list_index = 0;
		string_length = strlen(text);
	}

	/* find something that matches */
	while ((name = words_after_create[list_index++].name))
	{
		if ((pg_strncasecmp(name, text, string_length) == 0) &&
			!(words_after_create[list_index - 1].flags & excluded))
			return pg_strdup_keyword_case(name, text);
	}
	/* if nothing matches, return NULL */
	return NULL;
}

/*
 * This one gives you one from a list of things you can put after CREATE
 * as defined above.
 */
static char *
create_command_generator(const char *text, int state)
{
	return create_or_drop_command_generator(text, state, THING_NO_CREATE);
}

/*
 * This function gives you a list of things you can put after a DROP command.
 */
static char *
drop_command_generator(const char *text, int state)
{
	return create_or_drop_command_generator(text, state, THING_NO_DROP);
}

/*
 * This function gives you a list of things you can put after an ALTER command.
 */
static char *
alter_command_generator(const char *text, int state)
{
	return create_or_drop_command_generator(text, state, THING_NO_ALTER);
}

/*
 * These functions generate lists using server queries.
 * They are all wrappers for _complete_from_query.
 */

static char *
complete_from_query(const char *text, int state)
{
	/* query is assumed to work for any server version */
	return _complete_from_query(completion_charp, NULL, completion_charpp,
								completion_verbatim, text, state);
}

static char *
complete_from_versioned_query(const char *text, int state)
{
	const VersionedQuery *vquery = completion_vquery;

	/* Find appropriate array element */
	while (pset.sversion < vquery->min_server_version)
		vquery++;
	/* Fail completion if server is too old */
	if (vquery->query == NULL)
		return NULL;

	return _complete_from_query(vquery->query, NULL, completion_charpp,
								completion_verbatim, text, state);
}

static char *
complete_from_schema_query(const char *text, int state)
{
	/* query is assumed to work for any server version */
	return _complete_from_query(NULL, completion_squery, completion_charpp,
								completion_verbatim, text, state);
}

static char *
complete_from_versioned_schema_query(const char *text, int state)
{
	const SchemaQuery *squery = completion_squery;

	/* Find appropriate array element */
	while (pset.sversion < squery->min_server_version)
		squery++;
	/* Fail completion if server is too old */
	if (squery->catname == NULL)
		return NULL;

	return _complete_from_query(NULL, squery, completion_charpp,
								completion_verbatim, text, state);
}


/*
 * This creates a list of matching things, according to a query described by
 * the initial arguments.  The caller has already done any work needed to
 * select the appropriate query for the server's version.
 *
 * The query can be one of two kinds:
 *
 * 1. A simple query, which must contain a restriction clause of the form
 *		output LIKE '%s'
 * where "output" is the same string that the query returns.  The %s
 * will be replaced by a LIKE pattern to match the already-typed text.
 * There can be a second '%s', which will be replaced by a suitably-escaped
 * version of the string provided in completion_ref_object.  If there is a
 * third '%s', it will be replaced by a suitably-escaped version of the string
 * provided in completion_ref_schema.  Those strings should be set up
 * by calling set_completion_reference or set_completion_reference_verbatim.
 * Simple queries should return a single column of matches.  If "verbatim"
 * is true, the matches are returned as-is; otherwise, they are taken to
 * be SQL identifiers and quoted if necessary.
 *
 * 2. A schema query used for completion of both schema and relation names.
 * This is represented by a SchemaQuery object; see that typedef for details.
 *
 * See top of file for examples of both kinds of query.
 *
 * In addition to the query itself, we accept a null-terminated array of
 * literal keywords, which will be returned if they match the input-so-far
 * (case insensitively).  (These are in addition to keywords specified
 * within the schema_query, if any.)
 *
 * If "verbatim" is true, then we use the given text as-is to match the
 * query results; otherwise we parse it as a possibly-qualified identifier,
 * and reconstruct suitable quoting afterward.
 *
 * "text" and "state" are supplied by Readline.  "text" is the word we are
 * trying to complete.  "state" is zero on first call, nonzero later.
 *
 * readline will call this repeatedly with the same text and varying
 * state.  On each call, we are supposed to return a malloc'd string
 * that is a candidate completion.  Return NULL when done.
 */
static char *
_complete_from_query(const char *simple_query,
					 const SchemaQuery *schema_query,
					 const char *const *keywords,
					 bool verbatim,
					 const char *text, int state)
{
	static int	list_index,
				num_schema_only,
				num_query_other,
				num_keywords;
	static PGresult *result = NULL;
	static bool non_empty_object;
	static bool schemaquoted;
	static bool objectquoted;

	/*
	 * If this is the first time for this completion, we fetch a list of our
	 * "things" from the backend.
	 */
	if (state == 0)
	{
		PQExpBufferData query_buffer;
		char	   *schemaname;
		char	   *objectname;
		char	   *e_object_like;
		char	   *e_schemaname;
		char	   *e_ref_object;
		char	   *e_ref_schema;

		/* Reset static state, ensuring no memory leaks */
		list_index = 0;
		num_schema_only = 0;
		num_query_other = 0;
		num_keywords = 0;
		PQclear(result);
		result = NULL;

		/* Parse text, splitting into schema and object name if needed */
		if (verbatim)
		{
			objectname = pg_strdup(text);
			schemaname = NULL;
		}
		else
		{
			parse_identifier(text,
							 &schemaname, &objectname,
							 &schemaquoted, &objectquoted);
		}

		/* Remember whether the user has typed anything in the object part */
		non_empty_object = (*objectname != '\0');

		/*
		 * Convert objectname to a LIKE prefix pattern (e.g. 'foo%'), and set
		 * up suitably-escaped copies of all the strings we need.
		 */
		e_object_like = make_like_pattern(objectname);

		if (schemaname)
			e_schemaname = escape_string(schemaname);
		else
			e_schemaname = NULL;

		if (completion_ref_object)
			e_ref_object = escape_string(completion_ref_object);
		else
			e_ref_object = NULL;

		if (completion_ref_schema)
			e_ref_schema = escape_string(completion_ref_schema);
		else
			e_ref_schema = NULL;

		initPQExpBuffer(&query_buffer);

		if (schema_query)
		{
			Assert(simple_query == NULL);

			/*
			 * We issue different queries depending on whether the input is
			 * already qualified or not.  schema_query gives us the pieces to
			 * assemble.
			 */
			if (schemaname == NULL || schema_query->namespace == NULL)
			{
				/* Get unqualified names matching the input-so-far */
				appendPQExpBufferStr(&query_buffer, "SELECT ");
				if (schema_query->use_distinct)
					appendPQExpBufferStr(&query_buffer, "DISTINCT ");
				appendPQExpBuffer(&query_buffer,
								  "%s, NULL::pg_catalog.text FROM %s",
								  schema_query->result,
								  schema_query->catname);
				if (schema_query->refnamespace && completion_ref_schema)
					appendPQExpBufferStr(&query_buffer,
										 ", pg_catalog.pg_namespace nr");
				appendPQExpBufferStr(&query_buffer, " WHERE ");
				if (schema_query->selcondition)
					appendPQExpBuffer(&query_buffer, "%s AND ",
									  schema_query->selcondition);
				appendPQExpBuffer(&query_buffer, "(%s) LIKE '%s'",
								  schema_query->result,
								  e_object_like);
				if (schema_query->viscondition)
					appendPQExpBuffer(&query_buffer, " AND %s",
									  schema_query->viscondition);
				if (schema_query->refname)
				{
					Assert(completion_ref_object);
					appendPQExpBuffer(&query_buffer, " AND %s = '%s'",
									  schema_query->refname, e_ref_object);
					if (schema_query->refnamespace && completion_ref_schema)
						appendPQExpBuffer(&query_buffer,
										  " AND %s = nr.oid AND nr.nspname = '%s'",
										  schema_query->refnamespace,
										  e_ref_schema);
					else if (schema_query->refviscondition)
						appendPQExpBuffer(&query_buffer,
										  " AND %s",
										  schema_query->refviscondition);
				}

				/*
				 * When fetching relation names, suppress system catalogs
				 * unless the input-so-far begins with "pg_".  This is a
				 * compromise between not offering system catalogs for
				 * completion at all, and having them swamp the result when
				 * the input is just "p".
				 */
				if (strcmp(schema_query->catname,
						   "pg_catalog.pg_class c") == 0 &&
					strncmp(objectname, "pg_", 3) != 0)
				{
					appendPQExpBufferStr(&query_buffer,
										 " AND c.relnamespace <> (SELECT oid FROM"
										 " pg_catalog.pg_namespace WHERE nspname = 'pg_catalog')");
				}

				/*
				 * If the target object type can be schema-qualified, add in
				 * schema names matching the input-so-far.
				 */
				if (schema_query->namespace)
				{
					appendPQExpBuffer(&query_buffer, "\nUNION ALL\n"
									  "SELECT NULL::pg_catalog.text, n.nspname "
									  "FROM pg_catalog.pg_namespace n "
									  "WHERE n.nspname LIKE '%s'",
									  e_object_like);

					/*
					 * Likewise, suppress system schemas unless the
					 * input-so-far begins with "pg_".
					 */
					if (strncmp(objectname, "pg_", 3) != 0)
						appendPQExpBufferStr(&query_buffer,
											 " AND n.nspname NOT LIKE E'pg\\\\_%'");

					/*
					 * Since we're matching these schema names to the object
					 * name, handle their quoting using the object name's
					 * quoting state.
					 */
					schemaquoted = objectquoted;
				}
			}
			else
			{
				/* Input is qualified, so produce only qualified names */
				appendPQExpBufferStr(&query_buffer, "SELECT ");
				if (schema_query->use_distinct)
					appendPQExpBufferStr(&query_buffer, "DISTINCT ");
				appendPQExpBuffer(&query_buffer, "%s, n.nspname "
								  "FROM %s, pg_catalog.pg_namespace n",
								  schema_query->result,
								  schema_query->catname);
				if (schema_query->refnamespace && completion_ref_schema)
					appendPQExpBufferStr(&query_buffer,
										 ", pg_catalog.pg_namespace nr");
				appendPQExpBuffer(&query_buffer, " WHERE %s = n.oid AND ",
								  schema_query->namespace);
				if (schema_query->selcondition)
					appendPQExpBuffer(&query_buffer, "%s AND ",
									  schema_query->selcondition);
				appendPQExpBuffer(&query_buffer, "(%s) LIKE '%s' AND ",
								  schema_query->result,
								  e_object_like);
				appendPQExpBuffer(&query_buffer, "n.nspname = '%s'",
								  e_schemaname);
				if (schema_query->refname)
				{
					Assert(completion_ref_object);
					appendPQExpBuffer(&query_buffer, " AND %s = '%s'",
									  schema_query->refname, e_ref_object);
					if (schema_query->refnamespace && completion_ref_schema)
						appendPQExpBuffer(&query_buffer,
										  " AND %s = nr.oid AND nr.nspname = '%s'",
										  schema_query->refnamespace,
										  e_ref_schema);
					else if (schema_query->refviscondition)
						appendPQExpBuffer(&query_buffer,
										  " AND %s",
										  schema_query->refviscondition);
				}
			}
		}
		else
		{
			Assert(simple_query);
			/* simple_query is an sprintf-style format string */
			appendPQExpBuffer(&query_buffer, simple_query,
							  e_object_like,
							  e_ref_object, e_ref_schema);
		}

		/* Limit the number of records in the result */
		appendPQExpBuffer(&query_buffer, "\nLIMIT %d",
						  completion_max_records);

		/* Finally, we can issue the query */
		result = exec_query(query_buffer.data);

		/* Clean up */
		termPQExpBuffer(&query_buffer);
		pg_free(schemaname);
		pg_free(objectname);
		pg_free(e_object_like);
		pg_free(e_schemaname);
		pg_free(e_ref_object);
		pg_free(e_ref_schema);
	}

	/* Return the next result, if any, but not if the query failed */
	if (result && PQresultStatus(result) == PGRES_TUPLES_OK)
	{
		int			nskip;

		while (list_index < PQntuples(result))
		{
			const char *item = NULL;
			const char *nsp = NULL;

			if (!PQgetisnull(result, list_index, 0))
				item = PQgetvalue(result, list_index, 0);
			if (PQnfields(result) > 1 &&
				!PQgetisnull(result, list_index, 1))
				nsp = PQgetvalue(result, list_index, 1);
			list_index++;

			/* In verbatim mode, we return all the items as-is */
			if (verbatim)
			{
				num_query_other++;
				return pg_strdup(item);
			}

			/*
			 * In normal mode, a name requiring quoting will be returned only
			 * if the input was empty or quoted.  Otherwise the user might see
			 * completion inserting a quote she didn't type, which is
			 * surprising.  This restriction also dodges some odd behaviors of
			 * some versions of readline/libedit.
			 */
			if (non_empty_object)
			{
				if (item && !objectquoted && identifier_needs_quotes(item))
					continue;
				if (nsp && !schemaquoted && identifier_needs_quotes(nsp))
					continue;
			}

			/* Count schema-only results for hack below */
			if (item == NULL && nsp != NULL)
				num_schema_only++;
			else
				num_query_other++;

			return requote_identifier(nsp, item, schemaquoted, objectquoted);
		}

		/*
		 * When the query result is exhausted, check for hard-wired keywords.
		 * These will only be returned if they match the input-so-far,
		 * ignoring case.
		 */
		nskip = list_index - PQntuples(result);
		if (schema_query && schema_query->keywords)
		{
			const char *const *itemp = schema_query->keywords;

			while (*itemp)
			{
				const char *item = *itemp++;

				if (nskip-- > 0)
					continue;
				list_index++;
				if (pg_strncasecmp(text, item, strlen(text)) == 0)
				{
					num_keywords++;
					return pg_strdup_keyword_case(item, text);
				}
			}
		}
		if (keywords)
		{
			const char *const *itemp = keywords;

			while (*itemp)
			{
				const char *item = *itemp++;

				if (nskip-- > 0)
					continue;
				list_index++;
				if (pg_strncasecmp(text, item, strlen(text)) == 0)
				{
					num_keywords++;
					return pg_strdup_keyword_case(item, text);
				}
			}
		}
	}

	/*
	 * Hack: if we returned only bare schema names, don't let Readline add a
	 * space afterwards.  Otherwise the schema will stop being part of the
	 * completion subject text, which is not what we want.
	 */
	if (num_schema_only > 0 && num_query_other == 0 && num_keywords == 0)
		rl_completion_append_character = '\0';

	/* No more matches, so free the result structure and return null */
	PQclear(result);
	result = NULL;
	return NULL;
}


/*
 * Set up completion_ref_object and completion_ref_schema
 * by parsing the given word.  These variables can then be
 * used in a query passed to _complete_from_query.
 */
static void
set_completion_reference(const char *word)
{
	bool		schemaquoted,
				objectquoted;

	parse_identifier(word,
					 &completion_ref_schema, &completion_ref_object,
					 &schemaquoted, &objectquoted);
}

/*
 * Set up completion_ref_object when it should just be
 * the given word verbatim.
 */
static void
set_completion_reference_verbatim(const char *word)
{
	completion_ref_schema = NULL;
	completion_ref_object = pg_strdup(word);
}


/*
 * This function returns in order one of a fixed, NULL pointer terminated list
 * of strings (if matching). This can be used if there are only a fixed number
 * SQL words that can appear at certain spot.
 */
static char *
complete_from_list(const char *text, int state)
{
	static int	string_length,
				list_index,
				matches;
	static bool casesensitive;
	const char *item;

	/* need to have a list */
	Assert(completion_charpp != NULL);

	/* Initialization */
	if (state == 0)
	{
		list_index = 0;
		string_length = strlen(text);
		casesensitive = completion_case_sensitive;
		matches = 0;
	}

	while ((item = completion_charpp[list_index++]))
	{
		/* First pass is case sensitive */
		if (casesensitive && strncmp(text, item, string_length) == 0)
		{
			matches++;
			return pg_strdup(item);
		}

		/* Second pass is case insensitive, don't bother counting matches */
		if (!casesensitive && pg_strncasecmp(text, item, string_length) == 0)
		{
			if (completion_case_sensitive)
				return pg_strdup(item);
			else

				/*
				 * If case insensitive matching was requested initially,
				 * adjust the case according to setting.
				 */
				return pg_strdup_keyword_case(item, text);
		}
	}

	/*
	 * No matches found. If we're not case insensitive already, lets switch to
	 * being case insensitive and try again
	 */
	if (casesensitive && matches == 0)
	{
		casesensitive = false;
		list_index = 0;
		state++;
		return complete_from_list(text, state);
	}

	/* If no more matches, return null. */
	return NULL;
}


/*
 * This function returns one fixed string the first time even if it doesn't
 * match what's there, and nothing the second time.  The string
 * to be used must be in completion_charp.
 *
 * If the given string is "", this has the effect of preventing readline
 * from doing any completion.  (Without this, readline tries to do filename
 * completion which is seldom the right thing.)
 *
 * If the given string is not empty, readline will replace whatever the
 * user typed with that string.  This behavior might be useful if it's
 * completely certain that we know what must appear at a certain spot,
 * so that it's okay to overwrite misspellings.  In practice, given the
 * relatively lame parsing technology used in this file, the level of
 * certainty is seldom that high, so that you probably don't want to
 * use this.  Use complete_from_list with a one-element list instead;
 * that won't try to auto-correct "misspellings".
 */
static char *
complete_from_const(const char *text, int state)
{
	Assert(completion_charp != NULL);
	if (state == 0)
	{
		if (completion_case_sensitive)
			return pg_strdup(completion_charp);
		else

			/*
			 * If case insensitive matching was requested initially, adjust
			 * the case according to setting.
			 */
			return pg_strdup_keyword_case(completion_charp, text);
	}
	else
		return NULL;
}


/*
 * This function appends the variable name with prefix and suffix to
 * the variable names array.
 */
static void
append_variable_names(char ***varnames, int *nvars,
					  int *maxvars, const char *varname,
					  const char *prefix, const char *suffix)
{
	if (*nvars >= *maxvars)
	{
		*maxvars *= 2;
		*varnames = pg_realloc_array(*varnames, char *, (*maxvars) + 1);
	}

	(*varnames)[(*nvars)++] = psprintf("%s%s%s", prefix, varname, suffix);
}


/*
 * This function supports completion with the name of a psql variable.
 * The variable names can be prefixed and suffixed with additional text
 * to support quoting usages. If need_value is true, only variables
 * that are currently set are included; otherwise, special variables
 * (those that have hooks) are included even if currently unset.
 */
static char **
complete_from_variables(const char *text, const char *prefix, const char *suffix,
						bool need_value)
{
	char	  **matches;
	char	  **varnames;
	int			nvars = 0;
	int			maxvars = 100;
	int			i;
	struct _variable *ptr;

	varnames = pg_malloc_array(char *, maxvars + 1);

	for (ptr = pset.vars->next; ptr; ptr = ptr->next)
	{
		if (need_value && !(ptr->value))
			continue;
		append_variable_names(&varnames, &nvars, &maxvars, ptr->name,
							  prefix, suffix);
	}

	varnames[nvars] = NULL;
	COMPLETE_WITH_LIST_CS((const char *const *) varnames);

	for (i = 0; i < nvars; i++)
		pg_free(varnames[i]);
	pg_free(varnames);

	return matches;
}


/*
 * This function returns in order one of a fixed, NULL pointer terminated list
 * of string that matches file names or optionally specified list of keywords.
 *
 * If completion_charpp is set to a null-terminated array of literal keywords,
 * those keywords are added to the completion results alongside filenames if
 * they case-insensitively match the current input.
 */
static char *
complete_from_files(const char *text, int state)
{
	static int	list_index;
	static bool files_done;
	const char *item;

	/* Initialization */
	if (state == 0)
	{
		list_index = 0;
		files_done = false;
	}

	if (!files_done)
	{
		char	   *result = _complete_from_files(text, state);

		/* Return a filename that matches */
		if (result)
			return result;

		/* There are no more matching files */
		files_done = true;
	}

	if (!completion_charpp)
		return NULL;

	/*
	 * Check for hard-wired keywords. These will only be returned if they
	 * match the input-so-far, ignoring case.
	 */
	while ((item = completion_charpp[list_index++]))
	{
		if (pg_strncasecmp(text, item, strlen(text)) == 0)
		{
			completion_force_quote = false;
			return pg_strdup_keyword_case(item, text);
		}
	}

	return NULL;
}

/*
 * This function wraps rl_filename_completion_function() to strip quotes from
 * the input before searching for matches and to quote any matches for which
 * the consuming command will require it.
 *
 * Caller must set completion_charp to a zero- or one-character string
 * containing the escape character.  This is necessary since \copy has no
 * escape character, but every other backslash command recognizes "\" as an
 * escape character.
 *
 * Caller must also set completion_force_quote to indicate whether to force
 * quotes around the result.  (The SQL COPY command requires that.)
 */
static char *
_complete_from_files(const char *text, int state)
{
#ifdef USE_FILENAME_QUOTING_FUNCTIONS

	/*
	 * If we're using a version of Readline that supports filename quoting
	 * hooks, rely on those, and invoke rl_filename_completion_function()
	 * without messing with its arguments.  Readline does stuff internally
	 * that does not work well at all if we try to handle dequoting here.
	 * Instead, Readline will call quote_file_name() and dequote_file_name()
	 * (see below) at appropriate times.
	 *
	 * ... or at least, mostly it will.  There are some paths involving
	 * unmatched file names in which Readline never calls quote_file_name(),
	 * and if left to its own devices it will incorrectly append a quote
	 * anyway.  Set rl_completion_suppress_quote to prevent that.  If we do
	 * get to quote_file_name(), we'll clear this again.  (Yes, this seems
	 * like it's working around Readline bugs.)
	 */
#ifdef HAVE_RL_COMPLETION_SUPPRESS_QUOTE
	rl_completion_suppress_quote = 1;
#endif

	/* If user typed a quote, force quoting (never remove user's quote) */
	if (*text == '\'')
		completion_force_quote = true;

	return rl_filename_completion_function(text, state);
#else

	/*
	 * Otherwise, we have to do the best we can.
	 */
	static const char *unquoted_text;
	char	   *unquoted_match;
	char	   *ret = NULL;

	/* If user typed a quote, force quoting (never remove user's quote) */
	if (*text == '\'')
		completion_force_quote = true;

	if (state == 0)
	{
		/* Initialization: stash the unquoted input. */
		unquoted_text = strtokx(text, "", NULL, "'", *completion_charp,
								false, true, pset.encoding);
		/* expect a NULL return for the empty string only */
		if (!unquoted_text)
		{
			Assert(*text == '\0');
			unquoted_text = text;
		}
	}

	unquoted_match = rl_filename_completion_function(unquoted_text, state);
	if (unquoted_match)
	{
		struct stat statbuf;
		bool		is_dir = (stat(unquoted_match, &statbuf) == 0 &&
							  S_ISDIR(statbuf.st_mode) != 0);

		/* Re-quote the result, if needed. */
		ret = quote_if_needed(unquoted_match, " \t\r\n\"`",
							  '\'', *completion_charp,
							  completion_force_quote,
							  pset.encoding);
		if (ret)
			free(unquoted_match);
		else
			ret = unquoted_match;

		/*
		 * If it's a directory, replace trailing quote with a slash; this is
		 * usually more convenient.  (If we didn't quote, leave this to
		 * libedit.)
		 */
		if (*ret == '\'' && is_dir)
		{
			char	   *retend = ret + strlen(ret) - 1;

			Assert(*retend == '\'');
			*retend = '/';
			/* Prevent libedit from adding a space, too */
			rl_completion_append_character = '\0';
		}
	}

	return ret;
#endif							/* USE_FILENAME_QUOTING_FUNCTIONS */
}


/* HELPER FUNCTIONS */


/*
 * Make a pg_strdup copy of s and convert the case according to
 * COMP_KEYWORD_CASE setting, using ref as the text that was already entered.
 */
static char *
pg_strdup_keyword_case(const char *s, const char *ref)
{
	char	   *ret,
			   *p;
	unsigned char first = ref[0];

	ret = pg_strdup(s);

	if (pset.comp_case == PSQL_COMP_CASE_LOWER ||
		((pset.comp_case == PSQL_COMP_CASE_PRESERVE_LOWER ||
		  pset.comp_case == PSQL_COMP_CASE_PRESERVE_UPPER) && islower(first)) ||
		(pset.comp_case == PSQL_COMP_CASE_PRESERVE_LOWER && !isalpha(first)))
	{
		for (p = ret; *p; p++)
			*p = pg_tolower((unsigned char) *p);
	}
	else
	{
		for (p = ret; *p; p++)
			*p = pg_toupper((unsigned char) *p);
	}

	return ret;
}


/*
 * escape_string - Escape argument for use as string literal.
 *
 * The returned value has to be freed.
 */
static char *
escape_string(const char *text)
{
	size_t		text_length;
	char	   *result;

	text_length = strlen(text);

	result = pg_malloc(text_length * 2 + 1);
	PQescapeStringConn(pset.db, result, text, text_length, NULL);

	return result;
}


/*
 * make_like_pattern - Convert argument to a LIKE prefix pattern.
 *
 * We escape _ and % in the given text by backslashing, append a % to
 * represent "any subsequent characters", and then pass the string through
 * escape_string() so it's ready to insert in a query.  The result needs
 * to be freed.
 */
static char *
make_like_pattern(const char *word)
{
	char	   *result;
	char	   *buffer = pg_malloc(strlen(word) * 2 + 2);
	char	   *bptr = buffer;

	while (*word)
	{
		if (*word == '_' || *word == '%')
			*bptr++ = '\\';
		if (IS_HIGHBIT_SET(*word))
		{
			/*
			 * Transfer multibyte characters without further processing, to
			 * avoid getting confused in unsafe client encodings.
			 */
			int			chlen = PQmblenBounded(word, pset.encoding);

			while (chlen-- > 0)
				*bptr++ = *word++;
		}
		else
			*bptr++ = *word++;
	}
	*bptr++ = '%';
	*bptr = '\0';

	result = escape_string(buffer);
	pg_free(buffer);
	return result;
}


/*
 * parse_identifier - Parse a possibly-schema-qualified SQL identifier.
 *
 * This involves splitting off the schema name if present, de-quoting,
 * and downcasing any unquoted text.  We are a bit laxer than the backend
 * in that we allow just portions of a name to be quoted --- that's because
 * psql metacommands have traditionally behaved that way.
 *
 * Outputs are a malloc'd schema name (NULL if none), malloc'd object name,
 * and booleans telling whether any part of the schema and object name was
 * double-quoted.
 */
static void
parse_identifier(const char *ident,
				 char **schemaname, char **objectname,
				 bool *schemaquoted, bool *objectquoted)
{
	size_t		buflen = strlen(ident) + 1;
	bool		enc_is_single_byte = (pg_encoding_max_length(pset.encoding) == 1);
	char	   *sname;
	char	   *oname;
	char	   *optr;
	bool		inquotes;

	/* Initialize, making a certainly-large-enough output buffer */
	sname = NULL;
	oname = pg_malloc(buflen);
	*schemaquoted = *objectquoted = false;
	/* Scan */
	optr = oname;
	inquotes = false;
	while (*ident)
	{
		unsigned char ch = (unsigned char) *ident++;

		if (ch == '"')
		{
			if (inquotes && *ident == '"')
			{
				/* two quote marks within a quoted identifier = emit quote */
				*optr++ = '"';
				ident++;
			}
			else
			{
				inquotes = !inquotes;
				*objectquoted = true;
			}
		}
		else if (ch == '.' && !inquotes)
		{
			/* Found a schema name, transfer it to sname / *schemaquoted */
			*optr = '\0';
			free(sname);		/* drop any catalog name */
			sname = oname;
			oname = pg_malloc(buflen);
			optr = oname;
			*schemaquoted = *objectquoted;
			*objectquoted = false;
		}
		else if (!enc_is_single_byte && IS_HIGHBIT_SET(ch))
		{
			/*
			 * Transfer multibyte characters without further processing.  They
			 * wouldn't be affected by our downcasing rule anyway, and this
			 * avoids possibly doing the wrong thing in unsafe client
			 * encodings.
			 */
			int			chlen = PQmblenBounded(ident - 1, pset.encoding);

			*optr++ = (char) ch;
			while (--chlen > 0)
				*optr++ = *ident++;
		}
		else
		{
			if (!inquotes)
			{
				/*
				 * This downcasing transformation should match the backend's
				 * downcase_identifier() as best we can.  We do not know the
				 * backend's locale, though, so it's necessarily approximate.
				 * We assume that psql is operating in the same locale and
				 * encoding as the backend.
				 */
				if (ch >= 'A' && ch <= 'Z')
					ch += 'a' - 'A';
				else if (enc_is_single_byte && IS_HIGHBIT_SET(ch) && isupper(ch))
					ch = tolower(ch);
			}
			*optr++ = (char) ch;
		}
	}

	*optr = '\0';
	*schemaname = sname;
	*objectname = oname;
}


/*
 * requote_identifier - Reconstruct a possibly-schema-qualified SQL identifier.
 *
 * Build a malloc'd string containing the identifier, with quoting applied
 * as necessary.  This is more or less the inverse of parse_identifier;
 * in particular, if an input component was quoted, we'll quote the output
 * even when that isn't strictly required.
 *
 * Unlike parse_identifier, we handle the case where a schema and no
 * object name is provided, producing just "schema.".
 */
static char *
requote_identifier(const char *schemaname, const char *objectname,
				   bool quote_schema, bool quote_object)
{
	char	   *result;
	size_t		buflen = 1;		/* count the trailing \0 */
	char	   *ptr;

	/*
	 * We could use PQescapeIdentifier for some of this, but not all, and it
	 * adds more notational cruft than it seems worth.
	 */
	if (schemaname)
	{
		buflen += strlen(schemaname) + 1;	/* +1 for the dot */
		if (!quote_schema)
			quote_schema = identifier_needs_quotes(schemaname);
		if (quote_schema)
		{
			buflen += 2;		/* account for quote marks */
			for (const char *p = schemaname; *p; p++)
			{
				if (*p == '"')
					buflen++;
			}
		}
	}
	if (objectname)
	{
		buflen += strlen(objectname);
		if (!quote_object)
			quote_object = identifier_needs_quotes(objectname);
		if (quote_object)
		{
			buflen += 2;		/* account for quote marks */
			for (const char *p = objectname; *p; p++)
			{
				if (*p == '"')
					buflen++;
			}
		}
	}
	result = pg_malloc(buflen);
	ptr = result;
	if (schemaname)
	{
		if (quote_schema)
			*ptr++ = '"';
		for (const char *p = schemaname; *p; p++)
		{
			*ptr++ = *p;
			if (*p == '"')
				*ptr++ = '"';
		}
		if (quote_schema)
			*ptr++ = '"';
		*ptr++ = '.';
	}
	if (objectname)
	{
		if (quote_object)
			*ptr++ = '"';
		for (const char *p = objectname; *p; p++)
		{
			*ptr++ = *p;
			if (*p == '"')
				*ptr++ = '"';
		}
		if (quote_object)
			*ptr++ = '"';
	}
	*ptr = '\0';
	return result;
}


/*
 * Detect whether an identifier must be double-quoted.
 *
 * Note we'll quote anything that's not ASCII; the backend's quote_ident()
 * does the same.  Perhaps this could be relaxed in future.
 */
static bool
identifier_needs_quotes(const char *ident)
{
	int			kwnum;

	/* Check syntax. */
	if (!((ident[0] >= 'a' && ident[0] <= 'z') || ident[0] == '_'))
		return true;
	if (strspn(ident, "abcdefghijklmnopqrstuvwxyz0123456789_$") != strlen(ident))
		return true;

	/*
	 * Check for keyword.  We quote keywords except for unreserved ones.
	 *
	 * It is possible that our keyword list doesn't quite agree with the
	 * server's, but this should be close enough for tab-completion purposes.
	 *
	 * Note: ScanKeywordLookup() does case-insensitive comparison, but that's
	 * fine, since we already know we have all-lower-case.
	 */
	kwnum = ScanKeywordLookup(ident, &ScanKeywords);

	if (kwnum >= 0 && ScanKeywordCategories[kwnum] != UNRESERVED_KEYWORD)
		return true;

	return false;
}


/*
 * Execute a query, returning NULL if there was any error.
 * This should be the preferred way of talking to the database in this file.
 */
static PGresult *
exec_query(const char *query)
{
	PGresult   *result;

	if (query == NULL || !pset.db || PQstatus(pset.db) != CONNECTION_OK)
		return NULL;

	result = PQexec(pset.db, query);

	if (PQresultStatus(result) != PGRES_TUPLES_OK)
	{
		/*
		 * Printing an error while the user is typing would be quite annoying,
		 * so we don't.  This does complicate debugging of this code; but you
		 * can look in the server log instead.
		 */
#ifdef NOT_USED
		pg_log_error("tab completion query failed: %s\nQuery was:\n%s",
					 PQerrorMessage(pset.db), query);
#endif
		PQclear(result);
		result = NULL;
	}

	return result;
}


/*
 * Parse all the word(s) before point.
 *
 * Returns a malloc'd array of character pointers that point into the malloc'd
 * data array returned to *buffer; caller must free() both of these when done.
 * *nwords receives the number of words found, ie, the valid length of the
 * return array.
 *
 * Words are returned right to left, that is, previous_words[0] gets the last
 * word before point, previous_words[1] the next-to-last, etc.
 */
static char **
get_previous_words(int point, char **buffer, int *nwords)
{
	char	  **previous_words;
	char	   *buf;
	char	   *outptr;
	int			words_found = 0;
	int			i;

	/*
	 * If we have anything in tab_completion_query_buf, paste it together with
	 * rl_line_buffer to construct the full query.  Otherwise we can just use
	 * rl_line_buffer as the input string.
	 */
	if (tab_completion_query_buf && tab_completion_query_buf->len > 0)
	{
		i = tab_completion_query_buf->len;
		buf = pg_malloc(point + i + 2);
		memcpy(buf, tab_completion_query_buf->data, i);
		buf[i++] = '\n';
		memcpy(buf + i, rl_line_buffer, point);
		i += point;
		buf[i] = '\0';
		/* Readjust point to reference appropriate offset in buf */
		point = i;
	}
	else
		buf = rl_line_buffer;

	/*
	 * Allocate an array of string pointers and a buffer to hold the strings
	 * themselves.  The worst case is that the line contains only
	 * non-whitespace WORD_BREAKS characters, making each one a separate word.
	 * This is usually much more space than we need, but it's cheaper than
	 * doing a separate malloc() for each word.
	 */
	previous_words = pg_malloc_array(char *, point);
	*buffer = outptr = (char *) pg_malloc(point * 2);

	/*
	 * First we look for a non-word char before the current point.  (This is
	 * probably useless, if readline is on the same page as we are about what
	 * is a word, but if so it's cheap.)
	 */
	for (i = point - 1; i >= 0; i--)
	{
		if (strchr(WORD_BREAKS, buf[i]))
			break;
	}
	point = i;

	/*
	 * Now parse words, working backwards, until we hit start of line.  The
	 * backwards scan has some interesting but intentional properties
	 * concerning parenthesis handling.
	 */
	while (point >= 0)
	{
		int			start,
					end;
		bool		inquotes = false;
		int			parentheses = 0;

		/* now find the first non-space which then constitutes the end */
		end = -1;
		for (i = point; i >= 0; i--)
		{
			if (!isspace((unsigned char) buf[i]))
			{
				end = i;
				break;
			}
		}
		/* if no end found, we're done */
		if (end < 0)
			break;

		/*
		 * Otherwise we now look for the start.  The start is either the last
		 * character before any word-break character going backwards from the
		 * end, or it's simply character 0.  We also handle open quotes and
		 * parentheses.
		 */
		for (start = end; start > 0; start--)
		{
			if (buf[start] == '"')
				inquotes = !inquotes;
			if (!inquotes)
			{
				if (buf[start] == ')')
					parentheses++;
				else if (buf[start] == '(')
				{
					if (--parentheses <= 0)
						break;
				}
				else if (parentheses == 0 &&
						 strchr(WORD_BREAKS, buf[start - 1]))
					break;
			}
		}

		/* Return the word located at start to end inclusive */
		previous_words[words_found++] = outptr;
		i = end - start + 1;
		memcpy(outptr, &buf[start], i);
		outptr += i;
		*outptr++ = '\0';

		/* Continue searching */
		point = start - 1;
	}

	/* Release parsing input workspace, if we made one above */
	if (buf != rl_line_buffer)
		pg_free(buf);

	*nwords = words_found;
	return previous_words;
}

/*
 * Look up the type for the GUC variable with the passed name.
 *
 * Returns NULL if the variable is unknown. Otherwise the returned string,
 * containing the type, has to be freed.
 */
static char *
get_guctype(const char *varname)
{
	PQExpBufferData query_buffer;
	char	   *e_varname;
	PGresult   *result;
	char	   *guctype = NULL;

	e_varname = escape_string(varname);

	initPQExpBuffer(&query_buffer);
	appendPQExpBuffer(&query_buffer,
					  "SELECT vartype FROM pg_catalog.pg_settings "
					  "WHERE pg_catalog.lower(name) = pg_catalog.lower('%s')",
					  e_varname);

	result = exec_query(query_buffer.data);
	termPQExpBuffer(&query_buffer);
	free(e_varname);

	if (PQresultStatus(result) == PGRES_TUPLES_OK && PQntuples(result) > 0)
		guctype = pg_strdup(PQgetvalue(result, 0, 0));

	PQclear(result);

	return guctype;
}

#ifdef USE_FILENAME_QUOTING_FUNCTIONS

/*
 * Quote a filename according to SQL rules, returning a malloc'd string.
 * completion_charp must point to escape character or '\0', and
 * completion_force_quote must be set correctly, as per comments for
 * complete_from_files().
 */
static char *
quote_file_name(char *fname, int match_type, char *quote_pointer)
{
	char	   *s;
	struct stat statbuf;

	/* Quote if needed. */
	s = quote_if_needed(fname, " \t\r\n\"`",
						'\'', *completion_charp,
						completion_force_quote,
						pset.encoding);
	if (!s)
		s = pg_strdup(fname);

	/*
	 * However, some of the time we have to strip the trailing quote from what
	 * we send back.  Never strip the trailing quote if the user already typed
	 * one; otherwise, suppress the trailing quote if we have multiple/no
	 * matches (because we don't want to add a quote if the input is seemingly
	 * unfinished), or if the input was already quoted (because Readline will
	 * do arguably-buggy things otherwise), or if the file does not exist, or
	 * if it's a directory.
	 */
	if (*s == '\'' &&
		completion_last_char != '\'' &&
		(match_type != SINGLE_MATCH ||
		 (quote_pointer && *quote_pointer == '\'') ||
		 stat(fname, &statbuf) != 0 ||
		 S_ISDIR(statbuf.st_mode)))
	{
		char	   *send = s + strlen(s) - 1;

		Assert(*send == '\'');
		*send = '\0';
	}

	/*
	 * And now we can let Readline do its thing with possibly adding a quote
	 * on its own accord.  (This covers some additional cases beyond those
	 * dealt with above.)
	 */
#ifdef HAVE_RL_COMPLETION_SUPPRESS_QUOTE
	rl_completion_suppress_quote = 0;
#endif

	/*
	 * If user typed a leading quote character other than single quote (i.e.,
	 * double quote), zap it, so that we replace it with the correct single
	 * quote.
	 */
	if (quote_pointer && *quote_pointer != '\'')
		*quote_pointer = '\0';

	return s;
}

/*
 * Dequote a filename, if it's quoted.
 * completion_charp must point to escape character or '\0', as per
 * comments for complete_from_files().
 */
static char *
dequote_file_name(char *fname, int quote_char)
{
	char	   *unquoted_fname;

	/*
	 * If quote_char is set, it's not included in "fname".  We have to add it
	 * or strtokx will not interpret the string correctly (notably, it won't
	 * recognize escapes).
	 */
	if (quote_char == '\'')
	{
		char	   *workspace = (char *) pg_malloc(strlen(fname) + 2);

		workspace[0] = quote_char;
		strcpy(workspace + 1, fname);
		unquoted_fname = strtokx(workspace, "", NULL, "'", *completion_charp,
								 false, true, pset.encoding);
		pg_free(workspace);
	}
	else
		unquoted_fname = strtokx(fname, "", NULL, "'", *completion_charp,
								 false, true, pset.encoding);

	/* expect a NULL return for the empty string only */
	if (!unquoted_fname)
	{
		Assert(*fname == '\0');
		unquoted_fname = fname;
	}

	/* readline expects a malloc'd result that it is to free */
	return pg_strdup(unquoted_fname);
}

#endif							/* USE_FILENAME_QUOTING_FUNCTIONS */

#endif							/* USE_READLINE */
